---
phase: quick
plan: 260402-u6u
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - phpstan.neon
  - tests/Feature/InspireCommandTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "All Pest tests pass with no failures"
    - "Pint reports no style violations"
    - "PHPStan reports no errors at level 5"
    - "composer test and composer lint scripts are available"
  artifacts:
    - path: "phpstan.neon"
      provides: "PHPStan config at level 5 targeting app/"
    - path: "composer.json"
      provides: "scripts for test, lint, analyse"
  key_links:
    - from: "composer.json"
      to: "./vendor/bin/pest"
      via: "scripts.test"
    - from: "composer.json"
      to: "./vendor/bin/phpstan"
      via: "scripts.analyse"
---

<objective>
Wire up three quality tools so the project has a single-command quality gate.

Purpose: Pest and Pint are already installed but not configured as composer scripts. PHPStan is not yet installed. This makes quality checks fragmented and easy to skip.
Output: `composer test` runs Pest (all green), `composer lint` auto-fixes style via Pint, `composer analyse` runs PHPStan at level 5 with zero errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PHPStan, fix the broken test, add composer scripts</name>
  <files>composer.json, tests/Feature/InspireCommandTest.php</files>
  <action>
    1. Install PHPStan as a dev dependency:
       `composer require --dev phpstan/phpstan`

    2. Delete `tests/Feature/InspireCommandTest.php`. This test calls `artisan inspire`
       which does not exist in this project (confirmed: 1 failing test). The command was
       scaffolded from Laravel Zero's default template and is not part of Copland.

    3. Add composer scripts to composer.json under a `"scripts"` key (or extend if already
       present):
       ```json
       "scripts": {
           "test": "./vendor/bin/pest --no-coverage",
           "lint": "./vendor/bin/pint",
           "lint:check": "./vendor/bin/pint --test",
           "analyse": "./vendor/bin/phpstan analyse"
       }
       ```
       Preserve any existing scripts entries.
  </action>
  <verify>
    <automated>cd /Users/garykovar/projects/codeable/copland && composer test 2>&1 | tail -5</automated>
  </verify>
  <done>All Pest tests pass (no failures). PHPStan binary present at vendor/bin/phpstan. Composer scripts defined.</done>
</task>

<task type="auto">
  <name>Task 2: Create phpstan.neon and apply Pint formatting</name>
  <files>phpstan.neon, (all app/ and tests/ files reformatted by Pint)</files>
  <action>
    1. Create `phpstan.neon` in the project root:
       ```neon
       parameters:
           level: 5
           paths:
               - app
           excludePaths:
               - vendor
       ```
       Level 5 is strict enough to be useful without requiring full type annotations on
       every method. If level 5 produces errors from existing code, reduce to level 4 and
       note the decision.

    2. Run Pint to auto-fix all style violations:
       `./vendor/bin/pint`
       This will reformat ~31 files flagged in the current `--test` output.

    3. Run PHPStan to confirm zero errors:
       `./vendor/bin/phpstan analyse`
       If errors remain, reduce `level` in phpstan.neon until the baseline is clean.
       Document the chosen level in the task output.
  </action>
  <verify>
    <automated>cd /Users/garykovar/projects/codeable/copland && ./vendor/bin/pint --test 2>&1 | tail -3 && ./vendor/bin/phpstan analyse 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `./vendor/bin/pint --test` reports no files to fix (exit 0)
    - `./vendor/bin/phpstan analyse` reports "[OK] No errors" (exit 0)
  </done>
</task>

</tasks>

<verification>
Run the full quality gate in sequence:

```bash
composer test       # All Pest tests green
composer lint:check # Pint: no violations
composer analyse    # PHPStan: no errors
```

All three commands must exit 0.
</verification>

<success_criteria>
- `composer test` exits 0, all tests pass
- `composer lint:check` exits 0, no style violations
- `composer analyse` exits 0, no static analysis errors
- `phpstan.neon` exists at project root
- `composer.json` contains test, lint, lint:check, and analyse scripts
</success_criteria>

<output>
After completion, create `.planning/quick/260402-u6u-add-quality-tooling-pest-testing-pint-co/260402-u6u-SUMMARY.md`
</output>
