---
phase: 21-per-run-artifacts-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/ClaudePlannerService.php
  - app/Services/ClaudeSelectorService.php
  - app/Services/RunOrchestratorService.php
  - app/Support/HomeDirectory.php
  - composer.json
autonomous: true
requirements:
  - TASK-04
user_setup: []

must_haves:
  truths:
    - "PHPStan level 5 reports 0 errors against the `app/` tree"
    - "`./vendor/bin/phpstan analyse --memory-limit=512M` exits 0 with `[OK] No errors`"
    - "Existing 132+ Pest test suite still passes after the fixes"
    - "`app/Support/RunLogStore.php` is NOT touched by this plan (TASK-04 invariant)"
    - "PHPStan can be invoked via a single composer script without per-developer flag knowledge"
  artifacts:
    - path: "composer.json"
      provides: "`scripts.analyse` (or new `scripts.phpstan`) includes `--memory-limit=512M`"
      contains: "memory-limit"
    - path: "app/Services/ClaudePlannerService.php"
      provides: "non-nullable `ModelUsage` return on `usageFromResponse()` (D-16 fix #1)"
      contains: "usageFromResponse"
    - path: "app/Services/ClaudeSelectorService.php"
      provides: "non-nullable `ModelUsage` return on `usageFromResponse()` (D-16 fix #2)"
      contains: "usageFromResponse"
    - path: "app/Services/RunOrchestratorService.php"
      provides: "defensive `$repoPath` / `$writerRepoSlug` initialization + line-317 redundant `!== null` removed (D-16 fixes #3, #4, #5)"
      contains: "$repoPath = null"
    - path: "app/Support/HomeDirectory.php"
      provides: "removed redundant `isset($pwinfo['dir'])` (D-16 fix #6)"
      contains: "is_array($pwinfo)"
  key_links:
    - from: "composer.json (scripts.analyse|phpstan)"
      to: "./vendor/bin/phpstan analyse"
      via: "composer script invocation"
      pattern: "memory-limit=512M"
    - from: "ClaudePlannerService::usageFromResponse"
      to: "AnthropicCostEstimator::forModel"
      via: "non-nullable return chain"
      pattern: ": ModelUsage"
    - from: "ClaudeSelectorService::usageFromResponse"
      to: "AnthropicCostEstimator::forModel"
      via: "non-nullable return chain"
      pattern: ": ModelUsage"
---

<objective>
Fix all 6 pre-existing PHPStan level-5 errors and make the bare `composer analyse` (or new `composer phpstan`) invocation succeed without hitting the 128M default OOM. After this plan, the comprehensive Pest suite in 21-03 can assert "PHPStan reports 0 errors" as a phase-gate acceptance criterion.

Purpose: SC4 (PHPStan level 5 stays clean) is currently RED — 6 errors against `app/`. The errors are mechanical (RESEARCH §PHPStan Error Catalog) — type-annotation drift, redundant null-checks, and undefined variables in the orchestrator's finally arm. Fixing them now is the prerequisite for landing Plan 21-03's comprehensive test suite with PHPStan-clean as part of its acceptance criteria.

Output:
- 2 single-character return-type edits (`?ModelUsage` → `ModelUsage`) in the two Claude service classes
- 3 small edits to `RunOrchestratorService::run()` (init block + line-317 guard + line-326 guard)
- 1 small edit to `HomeDirectory::resolve()` (drop redundant `isset()`)
- 1 composer.json script entry update so `--memory-limit=512M` is the default for the analyse invocation
- `app/Support/RunLogStore.php` is NOT in `files_modified` — TASK-04 invariant preserved
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md
@app/Services/ClaudePlannerService.php
@app/Services/ClaudeSelectorService.php
@app/Services/RunOrchestratorService.php
@app/Support/HomeDirectory.php
@app/Support/AnthropicCostEstimator.php
@composer.json
@phpstan.neon

<interfaces>
<!-- Type contracts the executor needs. Extracted from canonical references in RESEARCH §PHPStan Error Catalog. -->

From app/Support/AnthropicCostEstimator.php:9:
```
public static function forModel(string $model, int $inputTokens, int $outputTokens, ?int $cacheWriteTokens = null, ?int $cacheReadTokens = null): ModelUsage
```
Note: return type is non-nullable `ModelUsage`. The callers in `ClaudePlannerService::usageFromResponse` and `ClaudeSelectorService::usageFromResponse` currently declare `?ModelUsage` — that nullable annotation is unused (return.unusedType).

From posix_getpwuid() PHP stub:
On success returns a canonical 7-key array including `dir`. On failure returns `false`. The `is_array($pwinfo)` guard already filters the failure case, so `isset($pwinfo['dir'])` is redundant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 2 Claude-service return types + HomeDirectory isset() redundancy</name>
  <files>app/Services/ClaudePlannerService.php, app/Services/ClaudeSelectorService.php, app/Support/HomeDirectory.php</files>
  <read_first>
    - app/Services/ClaudePlannerService.php (lines 95-115 — `usageFromResponse` method, return type at line 102)
    - app/Services/ClaudeSelectorService.php (lines 75-95 — same-shape `usageFromResponse`, return type at line 82)
    - app/Support/AnthropicCostEstimator.php (line 9 — `forModel(): ModelUsage` non-nullable signature confirms the callers' `?` is wrong)
    - app/Support/HomeDirectory.php (lines 25-35 — `resolve()` posix branch, redundant `isset` at line 31)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md (§PHPStan Error Catalog, rows #1, #2, #6)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md (§`phpstan.neon`, verified excerpts for fixes #1, #2, #6)
  </read_first>
  <action>
    Apply three independent single-line edits per D-16:
    (1) In `ClaudePlannerService::usageFromResponse()` at line 102, change the return type from `?ModelUsage` to `ModelUsage`. The method body already always returns a non-null `ModelUsage` from `AnthropicCostEstimator::forModel()`.
    (2) In `ClaudeSelectorService::usageFromResponse()` at line 82, apply the identical edit (`?ModelUsage` → `ModelUsage`).
    (3) In `HomeDirectory::resolve()` at line 31, change `if (is_array($pwinfo) && isset($pwinfo['dir']) && $pwinfo['dir'] !== '')` to `if (is_array($pwinfo) && $pwinfo['dir'] !== '')` — the `isset()` clause is redundant because `posix_getpwuid()` either returns the canonical 7-key array or `false`, and `is_array()` already filters the `false` case.
    Do NOT touch any other method signatures. Do NOT touch import statements. Do NOT add docblocks. The total surface is 3 lines changed across 3 files.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse --memory-limit=512M --no-progress app/Services/ClaudePlannerService.php app/Services/ClaudeSelectorService.php app/Support/HomeDirectory.php 2>&amp;1 | grep -E "0 errors|No errors"</automated>
  </verify>
  <done>
    - `grep -n "?ModelUsage" app/Services/ClaudePlannerService.php` returns no match on line 102
    - `grep -n "?ModelUsage" app/Services/ClaudeSelectorService.php` returns no match on line 82
    - `grep -n "isset(\$pwinfo\['dir'\])" app/Support/HomeDirectory.php` returns no match
    - PHPStan analyse of these 3 files alone reports 0 errors
    - Full Pest suite (`./vendor/bin/pest`) still passes (no behavior change)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix RunOrchestratorService finally-arm PHPStan errors + add memory-limit composer script</name>
  <files>app/Services/RunOrchestratorService.php, composer.json</files>
  <read_first>
    - app/Services/RunOrchestratorService.php (lines 34-50 — `run()` opening + init block at lines 39-42; lines 311-345 — try/catch/finally structure; lines 317-332 — workspace cleanup + blocked-write arm)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md (§PHPStan Error Catalog rows #3, #4, #5; §"Per D-17, OOM fix" recommendation for composer-script approach)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md (§Pattern B "Initialize defensive nulls at top of run()"; §"Composer script entry")
    - composer.json (existing `scripts` block — `analyse` script entry at line ~60)
    - phpstan.neon (verify it is NOT touched — D-17 says composer-script approach only)
  </read_first>
  <action>
    Three orchestrator edits (RESEARCH fixes #3, #4, #5) plus one composer.json edit (D-17):
    (1) In `RunOrchestratorService::run()`, extend the existing init block at lines 39-42 to add `$repoPath = null;` and `$writerRepoSlug = null;` immediately after the existing `$caught = null;` line. (Per RESEARCH fix #4 and #5 — matches the existing pattern.) Do NOT add `$runId = null;` here — Plan 21-02 owns that.
    (2) At line 317, simplify `if (isset($workspacePath) && $workspacePath !== null)` to `if (isset($workspacePath))`. The `!== null` clause is logically redundant after `isset()` because `$workspacePath` is typed `string` when assigned (per fix #3).
    (3) At line 326, extend the finally-arm guard from `if ($this->taskWriter !== null && $selectedIssue !== null)` to `if ($this->taskWriter !== null && $selectedIssue !== null && $writerRepoSlug !== null)`. The third clause is the PHPStan-required narrowing now that `$writerRepoSlug` is initialized to null at the top of `run()`.
    (4) In `composer.json`, update the existing `scripts.analyse` entry from `"./vendor/bin/phpstan analyse"` to `"./vendor/bin/phpstan analyse --memory-limit=512M"` so a bare `composer analyse` invocation does not OOM at the 128M default. Do NOT add a separate `phpstan` alias — keep the single canonical script per project convention.
    Do NOT touch `phpstan.neon` — D-17 prefers the composer-script approach. Do NOT touch any other line in `RunOrchestratorService.php`; Plan 21-02 owns all subsequent edits to this file.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse --memory-limit=512M --no-progress 2>&amp;1 | grep -E "0 errors|No errors" &amp;&amp; ./vendor/bin/pest --no-coverage 2>&amp;1 | grep -E "Tests:.*passed|OK"</automated>
  </verify>
  <done>
    - `grep -nE '^\s+\$repoPath = null;' app/Services/RunOrchestratorService.php` matches (init block extended)
    - `grep -nE '^\s+\$writerRepoSlug = null;' app/Services/RunOrchestratorService.php` matches (init block extended)
    - `grep -c '\$workspacePath !== null' app/Services/RunOrchestratorService.php` returns 0 (redundant clause removed)
    - `grep -c 'writerRepoSlug !== null' app/Services/RunOrchestratorService.php` returns at least 1 (new finally-arm guard clause added)
    - `grep -c 'memory-limit=512M' composer.json` returns at least 1
    - `composer analyse` (with no manual flags) does NOT OOM and reports `[OK] No errors`
    - `git diff app/Support/RunLogStore.php` is empty (TASK-04 invariant)
    - `git diff phpstan.neon` is empty (D-17 — config file untouched)
    - Full Pest suite (`./vendor/bin/pest`) still passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → `composer analyse` | local-only static analysis invocation; no data crosses a trust boundary |
| `vendor/bin/phpstan` child process | reads source under `app/`; emits diagnostics to stdout; no filesystem writes outside its own tmp dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering | composer.json `scripts.analyse` entry | accept | Single-line update to an existing, version-controlled script entry. Diff-reviewed in PR. Same risk profile as any composer-script edit; no new attack surface. |
| T-21-01-02 | Denial of Service | PHPStan child process OOM at 128M default | mitigate | Bump `--memory-limit=512M` in the composer script per D-17. Verified during RESEARCH that 512M is sufficient for the full `app/` tree (no OOM observed at 512M). |
| T-21-01-03 | Information Disclosure | PHPStan output may include source-path traces | accept | Static-analysis tool emits standard diagnostics; no secrets read or written. Same risk profile as Pest output. |
| T-21-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs in this plan. All edits are to existing in-tree files. Composer script entry edit does not install a new package. |
</threat_model>

<verification>
After both tasks complete:
- `./vendor/bin/phpstan analyse --memory-limit=512M --no-progress` exits 0 with `[OK] No errors`
- `composer analyse` (no extra flags) exits 0 with `[OK] No errors`
- `./vendor/bin/pest --no-coverage` reports the existing 132+ tests all passing
- `git diff app/Support/RunLogStore.php` is empty (TASK-04 negative assertion)
- `git diff phpstan.neon` is empty (D-17 — config file deliberately untouched)
- The fix surface across both tasks totals ~10 lines of source change + 1 line of composer.json change (matches RESEARCH §"Total fix surface")
</verification>

<success_criteria>
- All 6 RESEARCH-catalogued PHPStan errors are eliminated
- `composer analyse` is the single canonical invocation and does not require manual `--memory-limit=512M`
- No behavioral changes to runtime code paths (return type narrowing + redundant-clause removal + defensive null init are all PHPStan-only edits)
- TASK-04 invariant preserved: `app/Support/RunLogStore.php` untouched
- Pest suite remains green
- Phase 21-03's acceptance criteria can now assert "PHPStan reports 0 errors" as a phase-gate check
</success_criteria>

<output>
Create `.planning/phases/21-per-run-artifacts-test-coverage/21-01-SUMMARY.md` when done. Include:
- The exact diff size per file (line counts)
- The composer.json `scripts.analyse` before/after one-liner
- Confirmation that `composer analyse` exits 0
- Confirmation that `git diff app/Support/RunLogStore.php` is empty
- Confirmation that `git diff phpstan.neon` is empty
</output>
