---
phase: 21-per-run-artifacts-test-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - app/Services/TaskDirectoryWriterService.php
  - app/Services/RunOrchestratorService.php
autonomous: true
requirements:
  - TASK-03
  - TASK-04
user_setup: []

must_haves:
  truths:
    - "`TaskDirectoryWriterService` exposes 3 new public methods (`writeRunStatus`, `writeRunOutcome`, `writeRunBlockedIfNotTerminal`) per D-12; the existing 4 methods are unchanged"
    - "A run derives `$runId` exactly once via `str_replace(':', '-', gmdate('Y-m-d\\TH:i:s\\Z'))` immediately after `$writerRepoSlug` derivation (D-01, D-02)"
    - "Every existing task-level `writeStatus(...)` call site in `RunOrchestratorService::run()` has an adjacent paired `writeRunStatus(...)` call with the same state (D-06)"
    - "The finally-arm `writeBlockedIfNotTerminal` is paired with `writeRunBlockedIfNotTerminal` in its own try/catch with guard order `$selectedIssue !== null && $runId !== null && $writerRepoSlug !== null` (D-08)"
    - "`outcome.md` is written once at terminal state from inside the existing JSONL-append finally arm via a new private `outcomePayload()` mapper (D-09, D-10, D-11)"
    - "Writer's `$lastState` map gains a second per-tuple keying `(repoSlug, taskId, runId)`; both maps coexist (D-07)"
    - "Writer remains silent — no `pushLog`/`progressCallback` calls inside the new methods (D-14)"
    - "`app/Support/RunLogStore.php` is NOT modified — JSONL log path untouched (TASK-04, D-15)"
  artifacts:
    - path: "app/Services/TaskDirectoryWriterService.php"
      provides: "3 new public methods + 1 new private `runDir()` helper"
      contains: "function writeRunStatus"
    - path: "app/Services/TaskDirectoryWriterService.php"
      provides: "writeRunOutcome public method"
      contains: "function writeRunOutcome"
    - path: "app/Services/TaskDirectoryWriterService.php"
      provides: "writeRunBlockedIfNotTerminal public method"
      contains: "function writeRunBlockedIfNotTerminal"
    - path: "app/Services/RunOrchestratorService.php"
      provides: "`$runId` derivation + 7 paired `writeRunStatus` calls + finally-arm paired blocked write + outcome.md write in JSONL finally arm"
      contains: "writeRunStatus"
    - path: "app/Services/RunOrchestratorService.php"
      provides: "new private `outcomePayload()` mapper that distills the JSONL payload into the 9 D-05 keys"
      contains: "private function outcomePayload"
  key_links:
    - from: "RunOrchestratorService::run() line ~115"
      to: "$runId local variable"
      via: "str_replace + gmdate inline derivation"
      pattern: "str_replace\\(':', '-', gmdate"
    - from: "RunOrchestratorService::run() paired-write sites (lines 118, 119, 123, 176, 188, 228, 279)"
      to: "TaskDirectoryWriterService::writeRunStatus"
      via: "nullsafe writer call adjacent to each existing writeStatus"
      pattern: "writeRunStatus"
    - from: "RunOrchestratorService::run() finally arm (lines 326-332)"
      to: "TaskDirectoryWriterService::writeRunBlockedIfNotTerminal"
      via: "sibling try/catch in same finally arm"
      pattern: "writeRunBlockedIfNotTerminal"
    - from: "RunOrchestratorService::run() JSONL append arm (lines 334-343)"
      to: "TaskDirectoryWriterService::writeRunOutcome"
      via: "sibling try/catch reading the already-built `$payload` array via outcomePayload()"
      pattern: "writeRunOutcome"
    - from: "TaskDirectoryWriterService::writeRunStatus + writeRunOutcome"
      to: "private atomicWrite() primitive"
      via: "tmp+rename for both files"
      pattern: "atomicWrite"
---

<objective>
Extend `TaskDirectoryWriterService` with the 3 new public methods required for per-run artifacts (D-12), and wire them into `RunOrchestratorService::run()` adjacent to the existing 7 task-level `writeStatus` call sites + the finally-arm blocked write + a new sibling outcome.md write inside the JSONL-append finally arm.

Purpose: TASK-03 (per-run subdirectory with PR URL + cost summary) and the per-run lifecycle write coverage (D-06) are the structural deliverables that close the gap between Phase 20's task-level `status.md` and what the Godot console's `load_runs()` already reads. The writer extension is pure additive surface — the existing 4 methods are not touched (D-12). The orchestrator threading uses the verified Phase 20 line numbers and the "paired adjacent" convention from D-06.

Output:
- `app/Services/TaskDirectoryWriterService.php` gains:
  - 3 new public methods (`writeRunStatus`, `writeRunOutcome`, `writeRunBlockedIfNotTerminal`)
  - 1 new private helper `runDir($repoSlug, $taskId, $runId)`
  - A second `$lastState` keying convention (3-tuple) coexisting with the existing 2-tuple
- `app/Services/RunOrchestratorService.php` gains:
  - `$runId = null;` in the init block (paired with Plan 21-01's `$repoPath`/`$writerRepoSlug` inits)
  - `$runId` derivation immediately after `$writerRepoSlug` derivation (~line 115)
  - 7 paired `writeRunStatus(...)` calls adjacent to the existing `writeStatus(...)` sites at lines 118, 119, 123, 176, 188, 228, 279
  - 1 paired `writeRunBlockedIfNotTerminal(...)` call adjacent to the existing `writeBlockedIfNotTerminal` arm at lines 326-332
  - 1 new sibling try/catch inside the JSONL-append finally arm that calls `writeRunOutcome(...)` via a new private `outcomePayload()` mapper
  - 1 new private method `outcomePayload($runId, $result, $payload, $startedAt, $caught): array`
- `app/Support/RunLogStore.php` is NOT in `files_modified` (TASK-04 invariant)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md
@.planning/phases/20-task-status-writer/20-VERIFICATION.md
@.planning/phases/20-task-status-writer/20-CONTEXT.md
@app/Services/TaskDirectoryWriterService.php
@app/Services/RunOrchestratorService.php
@app/Support/RunLogStore.php
@console-godot/scripts/TaskLoader.gd

<interfaces>
<!-- Contracts the executor needs. Extracted from existing in-tree code per PATTERNS.md key-line-anchor table. -->

From app/Services/TaskDirectoryWriterService.php (existing, MUST NOT change):
```
public function writeNewTask(string $repoSlug, string|int $taskId, ?string $title, ?string $body, string $repoPath, ?string $sourceUrl): void
public function writeStatus(string $repoSlug, string|int $taskId, string $state): void
public function writeBlockedIfNotTerminal(string $repoSlug, string|int $taskId): void
private function taskDir(string $repoSlug, string|int $taskId): string
private function atomicWrite(string $path, string $content): void
private function renderFrontmatter(array $pairs): string
private function ensureDirectoryExists(string $dir): void
private function extractBody(string $contents): string
private function now(): string
```

New surface to add (D-12):
```
public function writeRunStatus(string $repoSlug, string|int $taskId, string $runId, string $state): void
public function writeRunOutcome(string $repoSlug, string|int $taskId, string $runId, array $outcome): void
public function writeRunBlockedIfNotTerminal(string $repoSlug, string|int $taskId, string $runId): void
private function runDir(string $repoSlug, string|int $taskId, string $runId): string
```

Outcome.md frontmatter keys (D-05, 9 keys exactly):
```
run_id, status, pr_number, pr_url, cost_usd, started_at, finished_at, failure_reason, partial
```

Status mapping inside `outcomePayload()` (RESEARCH §Outcome.md Mapping Table):
```
'succeeded' -> 'pr_open'
'crashed'   -> 'crashed'
default     -> 'blocked'   // 'failed' | 'skipped' -> 'blocked'
```

Existing JSONL payload shape consumed by `outcomePayload()` (from `payloadFromResult` lines 347-377 + `partialPayload` lines 379-409):
```
$payload['status']                                 // 'succeeded' | 'failed' | 'skipped' | 'crashed'
$payload['partial']                                // bool
$payload['started_at']                             // DATE_ATOM string (needs Z-form normalization)
$payload['finished_at']                            // DATE_ATOM string
$payload['failure_reason']                         // ?string
$payload['pr']['number']                           // ?int
$payload['pr']['url']                              // ?string
$payload['usage']['total']                         // ?ModelUsage (combined; null when all stages null)
$payload['usage']['total']->estimatedCostUsd       // float when total present
```

Verified Phase 20 line numbers (from .planning/phases/20-task-status-writer/20-VERIFICATION.md — still authoritative):
- `$writerRepoSlug` derivation: line 113
- `$runId` derivation goes immediately after: line ~115
- Existing writeStatus call sites needing paired writes: 118, 119, 123, 176, 188, 228, 279
- Existing finally-arm `writeBlockedIfNotTerminal` block: lines 326-332
- Existing JSONL append block: lines 334-343 (inside try/catch starting at line 334)

TaskLoader contract (console-godot/scripts/TaskLoader.gd lines 277-299):
The Godot `load_runs()` reads each `runs/<run-id>/status.md` and parses `state` + `updated_at` frontmatter scalars. The new per-run `status.md` MUST use the same schema as task-level `status.md` (D-04). `outcome.md` is NOT read by TaskLoader in this phase — it is for human/audit consumption now; Phase 22 may extend TaskLoader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `writeRunStatus`, `writeRunOutcome`, `writeRunBlockedIfNotTerminal`, and `runDir` to TaskDirectoryWriterService</name>
  <files>app/Services/TaskDirectoryWriterService.php</files>
  <read_first>
    - app/Services/TaskDirectoryWriterService.php (entire file, 161 lines — every primitive the new methods reuse: `writeStatus` at 42-69, `writeBlockedIfNotTerminal` at 71-80, `writeNewTask` at 17-40, `taskDir` at 91-97, `atomicWrite` at 113-124, `renderFrontmatter` at 126-136, `ensureDirectoryExists`, `extractBody`, `now`)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md (§"Method 1 — writeRunStatus" through §"Method 3 — writeRunOutcome" — three verbatim shapes; §"Required new private helper" for `runDir`; §"Pattern reused — renderFrontmatter" and §"Pattern reused — atomicWrite")
    - .planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md (D-04 per-run status.md schema; D-05 9 outcome.md keys; D-07 `$lastState` 3-tuple key; D-12 exact signatures; D-13 reuse `atomicWrite`; D-14 silent writer)
    - .planning/phases/20-task-status-writer/20-CONTEXT.md (D-01..D-17 inherited verbatim — state vocabulary, hand-rolled YAML, atomic-rename, silent writer)
  </read_first>
  <behavior>
    - `writeRunStatus('owner/repo', 42, '2026-05-27T19-15-22Z', 'planning')` creates `~/.copland/tasks/owner__repo/42/runs/2026-05-27T19-15-22Z/status.md` containing `state: "planning"` + `updated_at` frontmatter + a 1-row transitions table
    - A second `writeRunStatus(..., 'executing')` against the same `(repoSlug, taskId, runId)` appends a row to the existing transitions table (not overwrite)
    - `writeRunStatus` with the integer task ID `42` and `writeRunStatus` with the string GID `'1234567890123'` both succeed without truncation or collision (D-18 axis "both ID forms")
    - `writeRunStatus` updates `$lastState["{$repoSlug}/{$taskId}/runs/{$runId}"]` — a 3-tuple key distinct from the existing task-level 2-tuple key (D-07)
    - `writeRunBlockedIfNotTerminal('owner/repo', 42, '2026-05-27T19-15-22Z')` is a no-op when the last per-run state is `'pr_open'` or `'blocked'`, otherwise calls `writeRunStatus(..., 'blocked')`
    - `writeRunBlockedIfNotTerminal` is a no-op when no prior `writeRunStatus` was called for the tuple (D-15 inherited: never invents state)
    - `writeRunOutcome('owner/repo', 42, '2026-05-27T19-15-22Z', $outcome)` creates `~/.copland/tasks/owner__repo/42/runs/2026-05-27T19-15-22Z/outcome.md` whose frontmatter contains exactly the 9 D-05 keys: `run_id`, `status`, `pr_number`, `pr_url`, `cost_usd`, `started_at`, `finished_at`, `failure_reason`, `partial`
    - `writeRunOutcome` writes via the existing `atomicWrite()` primitive — no `.tmp` files remain after success
    - The new private `runDir($repoSlug, $taskId, $runId)` returns `taskDir(...)."/runs/{$runId}"` and is a one-liner
    - All 3 new methods are silent — no `pushLog` / `progressCallback` / `error_log` calls inside (D-14)
    - The 4 existing public methods are byte-for-byte unchanged
  </behavior>
  <action>
    Extend `TaskDirectoryWriterService` per D-12 / D-13 / PATTERNS.md §"Method 1..3" verbatim shapes:
    (1) Add public method `writeRunStatus(string $repoSlug, string|int $taskId, string $runId, string $state): void` — identical to the existing `writeStatus` shape (lines 42-69) but uses `$this->runDir($repoSlug, $taskId, $runId)` instead of `taskDir`, and writes `$this->lastState["{$repoSlug}/{$taskId}/runs/{$runId}"] = $state;` (3-tuple key per D-07). The frontmatter keys `state` + `updated_at` and the transitions-table append logic are copied verbatim from `writeStatus`.
    (2) Add public method `writeRunBlockedIfNotTerminal(string $repoSlug, string|int $taskId, string $runId): void` — identical to the existing `writeBlockedIfNotTerminal` shape (lines 71-80) but reads from the 3-tuple key in `$lastState` and delegates to `writeRunStatus(...)` instead of `writeStatus(...)`. Same early-return guard: `$current === null || $current === 'pr_open' || $current === 'blocked'`.
    (3) Add public method `writeRunOutcome(string $repoSlug, string|int $taskId, string $runId, array $outcome): void` — single one-shot atomic write to `runDir/outcome.md`. Body shape: `"---\n{frontmatter}---\n\n{body}"` where `$body = isset($outcome['_body']) ? (string) $outcome['_body'] : '';` (and `unset($outcome['_body']);` before rendering frontmatter so the body marker key does NOT appear in the YAML head). Use the existing `renderFrontmatter()` for the frontmatter pairs (per PATTERNS.md §"Method 3 — writeRunOutcome").
    (4) Add private method `runDir(string $repoSlug, string|int $taskId, string $runId): string` returning `$this->taskDir($repoSlug, $taskId)."/runs/{$runId}"` — one-liner per D-13.
    Place the three new public methods AFTER `writeBlockedIfNotTerminal` and BEFORE the first private method (`taskDir`). Place `runDir` adjacent to the existing `taskDir` helper. Do NOT modify any line of the existing 4 methods or any private helper. Do NOT change the constructor signature. Do NOT add new constructor parameters.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse --memory-limit=512M --no-progress app/Services/TaskDirectoryWriterService.php 2>&amp;1 | grep -E "0 errors|No errors" &amp;&amp; grep -cE 'function writeRun(Status|Outcome|BlockedIfNotTerminal)' app/Services/TaskDirectoryWriterService.php</automated>
  </verify>
  <done>
    - `grep -c 'public function writeRunStatus' app/Services/TaskDirectoryWriterService.php` returns 1
    - `grep -c 'public function writeRunOutcome' app/Services/TaskDirectoryWriterService.php` returns 1
    - `grep -c 'public function writeRunBlockedIfNotTerminal' app/Services/TaskDirectoryWriterService.php` returns 1
    - `grep -c 'private function runDir' app/Services/TaskDirectoryWriterService.php` returns 1
    - `grep -cE '\$this->lastState\["\{?\$?(repoSlug)\}?/\{?\$?(taskId)\}?/runs/' app/Services/TaskDirectoryWriterService.php` returns at least 2 (the 3-tuple key write + the 3-tuple key read in `writeRunBlockedIfNotTerminal`)
    - `grep -v '^#' app/Services/TaskDirectoryWriterService.php | grep -cE 'pushLog|progressCallback' ` returns 0 (writer remains silent per D-14)
    - The 4 existing method signatures match the pre-change byte-for-byte: `grep -nE 'public function (writeNewTask|writeStatus|writeBlockedIfNotTerminal)' app/Services/TaskDirectoryWriterService.php` returns lines whose method names + parameter lists are unchanged
    - PHPStan reports 0 errors against this file specifically
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread `$runId` through RunOrchestratorService::run() + paired writes + outcome.md mapper</name>
  <files>app/Services/RunOrchestratorService.php</files>
  <read_first>
    - app/Services/RunOrchestratorService.php (lines 34-50 — `run()` opening + init block at 39-42 already extended in Plan 21-01; lines 109-125 — `$writerRepoSlug` derivation at 113 + first 3 paired-write sites at 117-119, 123; lines 170-200 — call sites at 176, 188; lines 220-285 — call sites at 228, 279; lines 311-345 — try/catch/finally structure including blocked arm at 326-332 and JSONL append arm at 334-343; lines 347-409 — `payloadFromResult` and `partialPayload` analogs for the new `outcomePayload` mapper)
    - app/Services/TaskDirectoryWriterService.php (after Task 1 — verify the 3 new method signatures exist before wiring against them)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md (§Pattern A `$runId` derivation; §Pattern B init block extension; §Pattern C paired-write convention with full call-site table; §Pattern D finally-arm pairing; §Pattern E outcome.md write adjacent to JSONL append; §Pattern F `outcomePayload()` mapper)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md (§Outcome.md Mapping Table — exact 9-key mapping including the `succeeded → pr_open` status transform and the DATE_ATOM → Z-form timestamp normalization)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md (D-01 run-id format, D-02 orchestrator-owned, D-06 8-transition pairing, D-08 finally-arm guard order, D-09 outcome.md timing, D-10 nullsafe, D-11 own try/catch never re-throws, D-15 JSONL untouched)
    - app/Support/RunLogStore.php (CONFIRM not modified — TASK-04 negative assertion. Read once to know the surface that must NOT change.)
  </read_first>
  <behavior>
    - `RunOrchestratorService::run()` derives `$runId` exactly once via `str_replace(':', '-', gmdate('Y-m-d\TH:i:s\Z'))` immediately after `$writerRepoSlug` derivation; the value is in-scope for every subsequent paired write and for the finally arm
    - Each of the 7 existing task-level `writeStatus(...)` call sites (lines 118, 119, 123, 176, 188, 228, 279) has an adjacent `writeRunStatus(..., $runId, $state)` call with identical `$writerRepoSlug`, `$selectedIssue['number']`, and `$state` arguments
    - The finally-arm `writeBlockedIfNotTerminal` block (lines 326-332) has a sibling block immediately after that calls `writeRunBlockedIfNotTerminal($writerRepoSlug, $selectedIssue['number'], $runId)` with guard `$this->taskWriter !== null && $selectedIssue !== null && $runId !== null && $writerRepoSlug !== null` and its own try/catch logging via `pushLog("      Warning: per-run blocked-state write failed: {$e->getMessage()}");`
    - The JSONL append arm (lines 334-343) has a sibling block immediately after that calls `writeRunOutcome($writerRepoSlug, $selectedIssue['number'], $runId, $outcome)` where `$outcome = $this->outcomePayload($runId, $result, $payload, $startedAt, $caught);` and the same guard order; own try/catch logging via `pushLog("      Warning: outcome write failed: {$e->getMessage()}");`
    - New private `outcomePayload(string $runId, ?RunResult $result, array $payload, string $startedAt, ?Throwable $caught): array` returns exactly the 9 D-05 keys, with: `status` mapped via `match` (`succeeded → pr_open`, `crashed → crashed`, default → `blocked`); `pr_number` flattened from `$payload['pr']['number']`; `pr_url` flattened from `$payload['pr']['url']` cast to string; `cost_usd` from `$payload['usage']['total']?->estimatedCostUsd ?? 0.0`; `started_at` and `finished_at` normalized from DATE_ATOM to Z-form via `gmdate('Y-m-d\TH:i:s\Z', strtotime(...))`; `failure_reason` cast to string; `partial` rendered as `'true'` or `'false'`
    - `app/Support/RunLogStore.php` is NOT modified — TASK-04 negative invariant
    - PHPStan level 5 stays at 0 errors after these edits (Plan 21-01 cleared the baseline)
    - Existing Pest suite (132+ tests, including the Phase 20 smoke at `tests/Feature/TaskDirectoryWriterServiceTest.php`) still passes — no behavioral regression for callers that do NOT pass a writer dependency (nullsafe `?->` preserves this)
  </behavior>
  <action>
    Apply six edit clusters in order. Use the verbatim shapes from PATTERNS.md §Pattern A through §Pattern F.
    (1) **Init block extension**: at the end of the existing init block (where Plan 21-01 added `$repoPath = null;` and `$writerRepoSlug = null;`), add a third defensive null: `$runId = null;`. Per D-08, this makes the finally-arm `$runId !== null` guard sound for crashes that fire before line 113.
    (2) **`$runId` derivation**: immediately after the existing `$writerRepoSlug` derivation at line 113 (PATTERNS.md §Pattern A), add the one-liner `$runId = str_replace(':', '-', gmdate('Y-m-d\TH:i:s\Z'));` with a brief comment citing D-01 and D-02. The derivation runs inside the `try` block after `$selectedIssue` is known non-null.
    (3) **Paired writes adjacent to each existing `writeStatus` call**: at each of the 7 verified line numbers (118, 119, 123, 176, 188, 228, 279) insert a single new line immediately after the existing `writeStatus(...)` call. Each new line has the form `$this->taskWriter?->writeRunStatus($writerRepoSlug, $selectedIssue['number'], $runId, '<state>');` where `<state>` matches the existing call's state argument exactly. Do NOT add a paired call for `writeNewTask` (line 117) — only the 8 lifecycle states get paired per D-06 / D-04.
    (4) **Finally-arm paired blocked write**: immediately after the existing `writeBlockedIfNotTerminal` arm (closing brace at line ~332), add a sibling `if (...) { try { ... } catch (Throwable $e) { ... } }` block per PATTERNS.md §Pattern D. Guard: `if ($this->taskWriter !== null && $selectedIssue !== null && $runId !== null && $writerRepoSlug !== null)`. Body: `$this->taskWriter->writeRunBlockedIfNotTerminal($writerRepoSlug, $selectedIssue['number'], $runId);`. Catch logs `"      Warning: per-run blocked-state write failed: {$e->getMessage()}"`. Never re-throw.
    (5) **Outcome.md write adjacent to JSONL append**: inside the existing JSONL-append try/catch arm (lines 334-343), hoist `$payload` to a `null`-initialized local declared immediately before the `try {` (so the new sibling arm can read it). Then immediately after the JSONL arm's closing brace, add a new sibling `if (...) { try { ... } catch (Throwable $e) { ... } }` block per PATTERNS.md §Pattern E. Guard: `if ($this->taskWriter !== null && $selectedIssue !== null && $runId !== null && $writerRepoSlug !== null && $payload !== null)`. Body: `$outcome = $this->outcomePayload($runId, $result, $payload, $startedAt, $caught); $this->taskWriter->writeRunOutcome($writerRepoSlug, $selectedIssue['number'], $runId, $outcome);`. Catch logs `"      Warning: outcome write failed: {$e->getMessage()}"`. Never re-throw. Do NOT touch the `$runLogStore->append($payload)` call — TASK-04 / D-15.
    (6) **`outcomePayload()` private mapper**: add a new private method below `partialPayload` (after line 409) with signature `private function outcomePayload(string $runId, ?RunResult $result, array $payload, string $startedAt, ?Throwable $caught): array`. Body per PATTERNS.md §Pattern F: a `match` expression on `$payload['status']` produces the 3-value `$status`; two `gmdate('Y-m-d\TH:i:s\Z', strtotime(...))` calls produce `$startedAtZ` and `$finishedAtZ`; `$totalCost = $payload['usage']['total']?->estimatedCostUsd ?? 0.0;`. Return an associative array with exactly the 9 D-05 keys in the order `run_id, status, pr_number, pr_url, cost_usd, started_at, finished_at, failure_reason, partial`. Per RESEARCH §Outcome.md Mapping Table: `partial` is emitted as the string `'true'` or `'false'` since `renderFrontmatter` coerces all values to string and PHP's default boolean cast `(string) true` gives `'1'` not `'true'`. Optional body marker: if the planner chooses to include the per-stage usage table per D-05's "Body MAY include" allowance, render it into `'_body'` here — recommended but not required.
    Do NOT modify `payloadFromResult` (lines 347-377). Do NOT modify `partialPayload` (lines 379-409). Do NOT modify any line inside `app/Support/RunLogStore.php`. Do NOT change the existing 4 task-level writer call sites; the new per-run calls are strictly additive sibling lines.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse --memory-limit=512M --no-progress 2>&amp;1 | grep -E "0 errors|No errors" &amp;&amp; ./vendor/bin/pest --no-coverage 2>&amp;1 | grep -E "Tests:.*passed|OK" &amp;&amp; test -z "$(git diff app/Support/RunLogStore.php)"</automated>
  </verify>
  <done>
    - `grep -cE "str_replace\(':', '-', gmdate" app/Services/RunOrchestratorService.php` returns 1 (single `$runId` derivation site)
    - `grep -cE 'writeRunStatus\(\$writerRepoSlug' app/Services/RunOrchestratorService.php` returns 7 (one per existing task-level state write)
    - `grep -c 'writeRunBlockedIfNotTerminal(' app/Services/RunOrchestratorService.php` returns 1 (single finally-arm sibling)
    - `grep -c 'writeRunOutcome(' app/Services/RunOrchestratorService.php` returns 1 (single outcome.md write in JSONL arm)
    - `grep -c 'private function outcomePayload' app/Services/RunOrchestratorService.php` returns 1
    - `grep -cE "Warning: per-run blocked-state write failed" app/Services/RunOrchestratorService.php` returns 1
    - `grep -cE "Warning: outcome write failed" app/Services/RunOrchestratorService.php` returns 1
    - `git diff app/Support/RunLogStore.php` produces empty output (TASK-04 / D-15)
    - `git diff phpstan.neon` produces empty output (D-17)
    - `composer analyse` reports `[OK] No errors`
    - `./vendor/bin/pest --no-coverage` reports the existing test suite still passing
    - The 7 existing `writeStatus(\$writerRepoSlug` call sites (count via `grep -c 'writeStatus(\$writerRepoSlug' app/Services/RunOrchestratorService.php`) still returns 7 — Plan 21-02 does NOT remove or alter any existing task-level write
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator runtime → `~/.copland/tasks/<repo>/<id>/runs/<run-id>/` filesystem | per-run subdirectory writes; same trust boundary as the Phase 20 task-level writes |
| `$caught->getMessage()` text → `outcome.md` `failure_reason` frontmatter | caught-exception messages cross into a YAML-serialized file; same shape as JSONL `failure_reason` which is the existing baseline |
| `$runId` value → directory path component | orchestrator-derived from `gmdate(...)` + `str_replace`; no user input enters the value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | Path traversal via `$runId` | mitigate | `$runId` is mechanically constrained by `gmdate('Y-m-d\TH:i:s\Z')` (digits + literal T + Z + dashes) and `str_replace(':', '-', ...)` (digits + dashes + T + Z). No user input. The character set excludes `.`, `/`, and shell metacharacters. RESEARCH §Security Domain confirms no traversal risk. |
| T-21-02-02 | Tampering | Race between `outcome.md` and `status.md` writes in the same `runs/<run-id>/` dir | mitigate | Both new methods reuse the existing `atomicWrite()` private primitive (D-13). Tmp+rename is POSIX-atomic within a single directory. Reader (TaskLoader) never observes partial state. |
| T-21-02-03 | Information Disclosure | `failure_reason` from `$caught->getMessage()` may include caller-supplied error context | accept | Same risk profile as the existing JSONL log (`payloadFromResult` line 359 already writes `$result->failureReason`; `partialPayload` line 391 already writes `$caught?->getMessage()`). Phase 21 introduces no new failure-text source. The hand-rolled YAML escaper at `renderFrontmatter` line 131 handles embedded `"` and `\` correctly. |
| T-21-02-04 | Denial of Service | Long-running process writes one extra file (`outcome.md`) per run plus N `status.md` updates | accept | At most ~10 atomic writes per run (8 transitions + 1 finally arm + 1 outcome). Each closes its fd before rename. No leak risk. |
| T-21-02-05 | Tampering | Outcome.md write failure could mask original `$caught` exception | mitigate | Per D-11, the outcome.md write lives in its own try/catch with `pushLog` warning — never re-throws. The original `$caught` exception (re-thrown by line 314) is unaffected by writer-side failures. Same pattern as the existing JSONL arm (lines 334-343). |
| T-21-02-06 | Tampering | New `outcomePayload()` mapper accepts the already-built `$payload` array as source-of-truth | mitigate | Per RESEARCH §Open Questions item 2, accepting the JSONL `$payload` (rather than rebuilding from `$result + $caught`) collapses two source-of-truth paths into one. If the JSONL write fails before `$payload` is built, the outcome.md write is skipped via the `$payload !== null` guard. |
| T-21-02-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs in this plan. All edits are to existing in-tree files. No `composer require` invocation. |
</threat_model>

<verification>
After both tasks complete:
- `composer analyse` exits 0 with `[OK] No errors`
- `./vendor/bin/pest --no-coverage` reports the existing 132+ test suite still passing (Plan 21-03 expands the writer test file; Plan 21-02 must not regress what is there today)
- `git diff app/Support/RunLogStore.php` produces empty output (TASK-04 negative assertion)
- `git diff phpstan.neon` produces empty output (D-17)
- Grep gates from the Done blocks above all pass (writer surface, orchestrator surface, JSONL untouched)
- `grep -cE 'function writeRun(Status|Outcome|BlockedIfNotTerminal)' app/Services/TaskDirectoryWriterService.php` returns 3 — exactly the new public methods exist
- `grep -cE 'writeRunStatus\(\$writerRepoSlug' app/Services/RunOrchestratorService.php` returns 7 — all 7 paired writes wired adjacent to the existing task-level calls
</verification>

<success_criteria>
- 3 new writer public methods + 1 new private `runDir` helper added; 4 existing methods unchanged
- `$runId` threaded through all per-run call sites in `RunOrchestratorService::run()` (1 derivation + 7 paired writes + 1 finally-arm blocked + 1 outcome.md write)
- New `outcomePayload()` mapper produces the 9 D-05 frontmatter keys with correct status mapping (`succeeded → pr_open`) and timestamp normalization (DATE_ATOM → Z-form)
- TASK-04 invariant preserved: `app/Support/RunLogStore.php` and `phpstan.neon` both untouched
- PHPStan level 5 stays clean
- Existing Pest suite still passes; new methods are exercised by the comprehensive test plan in 21-03
</success_criteria>

<output>
Create `.planning/phases/21-per-run-artifacts-test-coverage/21-02-SUMMARY.md` when done. Include:
- The 3 new method signatures added to `TaskDirectoryWriterService`
- The exact line where `$runId` is derived in `RunOrchestratorService` (post-Plan-21-01 line numbers)
- The 7 paired-write line numbers (post-edit positions)
- The finally-arm sibling block line numbers for the new blocked-write and outcome.md arms
- Confirmation that `git diff app/Support/RunLogStore.php` is empty
- Confirmation that `composer analyse` exits 0
- The `outcomePayload()` status-mapping `match` expression as it shipped
</output>
