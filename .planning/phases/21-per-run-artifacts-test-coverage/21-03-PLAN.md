---
phase: 21-per-run-artifacts-test-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - tests/Feature/TaskDirectoryWriterServiceTest.php
autonomous: true
requirements:
  - TASK-03
  - TASK-04
  - TASK-05
user_setup: []

must_haves:
  truths:
    - "All 7 public methods of `TaskDirectoryWriterService` (4 existing + 3 new from Plan 21-02) are exercised by Pest tests against a temporary `HOME` so no developer-machine state is touched"
    - "Coverage spans all 11 axes documented in CONTEXT D-18: both ID forms (GitHub int + Asana 13-digit GID), all 8 lifecycle states for both task-level and per-run writes, both `*BlockedIfNotTerminal` terminal-state guards, exact frontmatter-key assertions for `writeNewTask` (7 keys) and `writeRunOutcome` (9 keys), atomic-rename correctness, idempotent dir-create, transitions-table append-only behavior, and `$lastState` per-tuple isolation"
    - "Test count grows from the Phase 20 baseline of 1 it-block to 12-18 it-blocks in the same file (`tests/Feature/TaskDirectoryWriterServiceTest.php`)"
    - "Every it-block uses the canonical 4-line `$_SERVER['HOME']` swap idiom and never `rmdir`s the temp tree (OS-level cleanup handles it)"
    - "`./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php` exits 0 and reports 12-18 tests passed"
    - "`composer analyse` (which now invokes PHPStan with `--memory-limit=512M` per Plan 21-01) exits 0 with `[OK] No errors`"
    - "TASK-04 negative assertion holds: `git diff app/Support/RunLogStore.php` over the entire Phase 21 branch is empty"
  artifacts:
    - path: "tests/Feature/TaskDirectoryWriterServiceTest.php"
      provides: "comprehensive Pest coverage of TaskDirectoryWriterService — ~12-18 it-blocks covering all D-18 axes"
      min_lines: 250
      contains: "writeRunStatus"
  key_links:
    - from: "every new it-block"
      to: "$_SERVER['HOME'] temporary directory under sys_get_temp_dir()"
      via: "4-line save/mkdir/swap/restore idiom"
      pattern: "sys_get_temp_dir\\(\\).'/copland-"
    - from: "test cases asserting timestamps"
      to: "TaskDirectoryWriterService clock seam"
      via: "constructor `clock:` parameter (single value or counter closure)"
      pattern: "clock:"
    - from: "test cases asserting per-run writes"
      to: "writeRunStatus / writeRunOutcome / writeRunBlockedIfNotTerminal"
      via: "Plan 21-02 public surface"
      pattern: "writeRun"
---

<objective>
Grow the Phase 20 single-test smoke (`tests/Feature/TaskDirectoryWriterServiceTest.php`, 53 lines) into a comprehensive Pest suite (~12-18 it-blocks) that exercises the entire `TaskDirectoryWriterService` surface — 4 existing methods plus the 3 new methods added by Plan 21-02 — against a temporary `HOME` so no developer-machine state is touched. This is the deliverable that closes TASK-03 (run-dir artifacts verified), TASK-04 (JSONL untouched verified via negative assertion), and TASK-05 (Pest coverage of the writer end-to-end).

Purpose: ROADMAP SC3 ("Pest tests exercise the task-directory writer end-to-end using a temporary HOME") and SC4 ("PHPStan level 5 stays clean and the existing 132+ test suite continues to pass") become green after this plan. The comprehensive suite is the wave-3 acceptance gate for Phase 21. It also asserts the TASK-04 negative invariant (`app/Support/RunLogStore.php` is byte-for-byte untouched on the Phase 21 branch) as part of its verify command.

Output:
- `tests/Feature/TaskDirectoryWriterServiceTest.php` expanded from 1 to 12-18 it-blocks covering all 11 D-18 axes
- Every it-block uses the canonical 4-line `$_SERVER['HOME']` swap idiom from RESEARCH §Pest Temp-HOME Pattern
- The existing Phase 20 it-block is preserved as case #1 (no regression to the baseline)
- New per-run cases use the writer's `clock:` seam for deterministic timestamps; the counter-closure pattern is used where multiple distinct timestamps must appear in a single test (e.g., transitions-table append-only axis)
- TASK-04 phase-gate command is documented in the plan's verify block: `git diff main -- app/Support/RunLogStore.php` returns empty
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-VALIDATION.md
@app/Services/TaskDirectoryWriterService.php
@console-godot/scripts/TaskLoader.gd
@tests/Feature/TaskDirectoryWriterServiceTest.php
@tests/Unit/GlobalConfigTest.php
@tests/Unit/RunLogStoreTest.php
@tests/Pest.php

<interfaces>
<!-- Public writer surface after Plan 21-02. Tests pin against these signatures. -->

```
public function __construct(private $clock = null, private ?string $homeOverride = null)

public function writeNewTask(string $repoSlug, string|int $taskId, ?string $title, ?string $body, string $repoPath, ?string $sourceUrl): void
public function writeStatus(string $repoSlug, string|int $taskId, string $state): void
public function writeBlockedIfNotTerminal(string $repoSlug, string|int $taskId): void

public function writeRunStatus(string $repoSlug, string|int $taskId, string $runId, string $state): void
public function writeRunOutcome(string $repoSlug, string|int $taskId, string $runId, array $outcome): void
public function writeRunBlockedIfNotTerminal(string $repoSlug, string|int $taskId, string $runId): void
```

Expected on-disk layout under `$_SERVER['HOME'] = $home`:
```
$home/.copland/tasks/<repo-slug-normalized>/<task-id>/task.md
$home/.copland/tasks/<repo-slug-normalized>/<task-id>/status.md
$home/.copland/tasks/<repo-slug-normalized>/<task-id>/runs/<run-id>/status.md
$home/.copland/tasks/<repo-slug-normalized>/<task-id>/runs/<run-id>/outcome.md
```

Repo-slug normalization (Phase 20 D-04, inherited): `owner/repo` slash becomes `__` on disk; e.g., `binarygary/copland` → `binarygary__copland`. Confirm against existing Phase 20 test (line 22-24 of the smoke test, which already asserts this).

Task-level `status.md` frontmatter keys (Phase 20 D-05, inherited):
```
state, updated_at
```
Plus a `## Transitions` body table with one row per `writeStatus` call.

Per-run `status.md` frontmatter keys (Phase 21 D-04 — identical schema to task-level):
```
state, updated_at
```
Plus a `## Transitions` body table with one row per `writeRunStatus` call.

`task.md` frontmatter keys (Phase 20 D-03, 7 keys total):
```
id, title, repo_slug, repo_path, source_url, created_at
```
(plus the body text after the closing `---`). Note: this is 6 keys; the 7th "key" in CONTEXT D-18 wording refers to body presence. Confirm against `writeNewTask` source (`app/Services/TaskDirectoryWriterService.php:17-40`).

`outcome.md` frontmatter keys (Phase 21 D-05, 9 keys):
```
run_id, status, pr_number, pr_url, cost_usd, started_at, finished_at, failure_reason, partial
```

Asana invariants (Phase 20 D-06, inherited):
- task ID is a 13-digit string GID (e.g., `'1209876543210'`)
- `repo_slug` uses `basename(repo_path)` instead of `owner/repo` (no slash collision)
- `source_url` is `""` (empty string) — Asana has no `html_url`

State vocabulary (Phase 20 D-02, inherited — Phase 21 D-04 confirms identical):
```
new, selected, planning, planned, executing, verifying, pr_open, blocked
```
Note: `merged` is explicitly NOT written (Phase 20 D-17, inherited).

Run-ID format (Phase 21 D-01): `2026-05-27T19-15-22Z` — 20 chars, digits + `T` + dashes + `Z`. In tests, pin to a literal string (e.g., `'2026-05-27T19-15-22Z'`) — no need to call `gmdate(...)` in tests since the writer accepts `$runId` as a parameter (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand `tests/Feature/TaskDirectoryWriterServiceTest.php` to 12-18 it-blocks covering all D-18 axes</name>
  <files>tests/Feature/TaskDirectoryWriterServiceTest.php</files>
  <read_first>
    - tests/Feature/TaskDirectoryWriterServiceTest.php (entire file, 53 lines — the Phase 20 smoke test that becomes case #1 of the expanded suite; do NOT delete or restructure this test, only add new it-blocks after it)
    - tests/Unit/GlobalConfigTest.php (entire file — the 11-case multi-it organizational analog per PATTERNS.md §Pattern H; same 4-line temp-HOME idiom in every case, no `beforeEach`/`afterEach`)
    - tests/Unit/RunLogStoreTest.php (55 lines — canonical temp-HOME idiom reference; the precise variable names `$originalHome` / `$home` and the `mkdir(..., 0755, true)` shape)
    - tests/Pest.php (verifies `TestCase` is bound to `Feature/` only — the expanded file lives under `Feature/` and inherits this binding)
    - app/Services/TaskDirectoryWriterService.php (entire file, ~190 lines after Plan 21-02 — verify the 3 new public methods exist and confirm their exact signatures before writing tests against them)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-RESEARCH.md (§Pest Temp-HOME Pattern — canonical idiom and per-test-file structure recommendation; §Clock seam usage — counter-closure pattern for multi-timestamp tests)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-PATTERNS.md (§Pattern G Phase 20 smoke; §Pattern H multi-case organization with the 18-case suggested table; §Pattern I counter-closure clock seam)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md (§D-18 — 11 coverage axes; §D-19 — writer-only scope, no orchestrator integration tests)
    - console-godot/scripts/TaskLoader.gd (lines 218-256 for the frontmatter parser the writer output must match; lines 277-299 for the `load_runs()` shape per-run `status.md` must satisfy — for the "frontmatter key assertion" axes)
  </read_first>
  <action>
    Expand `tests/Feature/TaskDirectoryWriterServiceTest.php` to ~12-18 independent `it()` blocks covering the 11 D-18 axes per PATTERNS.md §Pattern H suggested table. Preserve the existing Phase 20 smoke test as case #1 unchanged (do not edit its assertions or its idiom — it is already canonical). Add the new cases AFTER it in the same file. Every new case uses the canonical 4-line temp-HOME idiom:

    ```
    $originalHome = $_SERVER['HOME'] ?? null;
    $home = sys_get_temp_dir().'/copland-<slug>-'.uniqid();
    mkdir($home, 0755, true);
    $_SERVER['HOME'] = $home;
    // arrange / act / assert
    $_SERVER['HOME'] = $originalHome;
    ```

    Distribute the 11 D-18 axes across new it-blocks as follows (case numbers continue from the existing case #1):

    Case 2 — `'writes task.md for an Asana-shaped task with empty source_url and string GID'`: covers D-18 axis "both ID forms (Asana 13-digit GID)" and "Asana `source_url: ''` invariant". Use `$writer->writeNewTask('copland', '1209876543210', 'Asana title', 'Asana body', '/tmp/copland', null);` and assert: (a) the file exists at `$home/.copland/tasks/copland/1209876543210/task.md`; (b) the frontmatter contains `source_url: ""`; (c) the frontmatter contains `id: "1209876543210"` (string-form, no integer cast loss).

    Case 3 — `'writes task.md frontmatter with all 7 keys matching the TaskLoader contract'`: covers D-18 axis "`writeNewTask` exact frontmatter key assertion". Assert that the rendered file contains each of `id`, `title`, `repo_slug`, `repo_path`, `source_url`, `created_at` (use `expect($contents)->toContain(...)` for each key) plus the body text.

    Case 4 — `'writeStatus produces an 8-row transitions table across the full lifecycle'`: covers D-18 axis "all 8 lifecycle states for task-level writeStatus". Use the counter-closure clock per PATTERNS.md §Pattern I to produce 8 distinct timestamps. Call `writeStatus` in sequence for `'new', 'selected', 'planning', 'planned', 'executing', 'verifying', 'pr_open', 'blocked'`. Assert the final `status.md` contains 8 distinct table rows (count via `substr_count($contents, '|')` or `preg_match_all`) and that the frontmatter `state` is the last value written (`'blocked'`).

    Case 5 — `'writeBlockedIfNotTerminal is a no-op after pr_open'`: D-18 axis "writeBlockedIfNotTerminal early-return on terminal states (pr_open)". Call `writeStatus(..., 'pr_open')` then `writeBlockedIfNotTerminal(...)`; assert the transitions table still has exactly 1 row (no `'blocked'` appended).

    Case 6 — `'writeBlockedIfNotTerminal is a no-op after blocked'`: D-18 axis "writeBlockedIfNotTerminal early-return on terminal states (blocked)". Same shape as case 5 but with `'blocked'` as the prior state.

    Case 7 — `'writeBlockedIfNotTerminal transitions executing -> blocked'`: D-18 axis "writeBlockedIfNotTerminal transitions to blocked from non-terminal". Call `writeStatus(..., 'executing')` then `writeBlockedIfNotTerminal(...)`; assert the transitions table has 2 rows and the final `state` is `'blocked'`.

    Case 8 — `'writeRunStatus produces a per-run transitions table for the full lifecycle'`: D-18 axis "all 8 lifecycle states for per-run writeRunStatus". Same pattern as case 4 but call `writeRunStatus($repoSlug, $taskId, '2026-05-27T19-15-22Z', $state)` for each of the 8 states. Assert the file `$home/.copland/tasks/<repo>/<id>/runs/2026-05-27T19-15-22Z/status.md` contains 8 distinct table rows and that the frontmatter `state` is `'blocked'`.

    Case 9 — `'writeRunStatus accepts a 13-digit string task id and a Z-form run id'`: D-18 axis "both ID forms in per-run context". Use Asana-shaped `$taskId = '1209876543210'` and `$runId = '2026-05-27T19-15-22Z'`. Assert the file exists at the correct nested path and contains the expected frontmatter.

    Case 10 — `'writeRunBlockedIfNotTerminal respects per-run pr_open as terminal'`: D-18 axis "writeRunBlockedIfNotTerminal terminal-state guard". Call `writeRunStatus(..., 'pr_open')` then `writeRunBlockedIfNotTerminal(...)`; assert the per-run transitions table still has exactly 1 row.

    Case 11 — `'writeRunBlockedIfNotTerminal transitions verifying -> blocked'`: D-18 axis "writeRunBlockedIfNotTerminal transitions to blocked". Same shape as case 7 but for the per-run method.

    Case 12 — `'writeRunOutcome emits all 9 D-05 frontmatter keys'`: D-18 axis "writeRunOutcome frontmatter key coverage". Call `writeRunOutcome(...)` with an array containing each of the 9 D-05 keys (`run_id`, `status`, `pr_number`, `pr_url`, `cost_usd`, `started_at`, `finished_at`, `failure_reason`, `partial`). Assert each key appears verbatim as a frontmatter line in the resulting `outcome.md`. Use `expect($contents)->toContain("run_id: \"2026-05-27T19-15-22Z\"")` style for at least 3 keys; use a loop with `expect($contents)->toMatch("/^{$key}: /m")` (PHP regex multiline) for full coverage.

    Case 13 — `'writeRunOutcome accepts an optional body with a per-stage usage table'`: optional D-18 axis "writeRunOutcome body discretion per D-05". Pass `'_body' => "## Usage\n\n| Stage | Model |..."` in the outcome array; assert the body markdown appears AFTER the closing `---` and the `_body` key does NOT appear in the frontmatter (it should be unset before frontmatter rendering, per PATTERNS.md §"Method 3 — writeRunOutcome").

    Case 14 — `'atomic write leaves no .tmp residue after a successful write'`: D-18 axis "Atomic-rename correctness". After any `writeStatus` or `writeRunOutcome` call, glob the writer's target directory for `*.tmp` and assert the result is empty.

    Case 15 — `'writing twice into the same task/run dir is idempotent'`: D-18 axis "Idempotent directory creation". Call `writeRunStatus(..., 'new')` twice in succession with the same `$runId`; assert no exception is thrown and the resulting transitions table has 2 rows (append-only, not error).

    Case 16 — `'three sequential writeStatus calls produce a 3-row table not an overwrite'`: D-18 axis "Transitions-table append-only". Use a counter-closure clock yielding 3 distinct timestamps; call `writeStatus(..., 'new')`, `writeStatus(..., 'selected')`, `writeStatus(..., 'planning')` in sequence; assert the final `status.md` contains exactly 3 transition rows.

    Case 17 — `'lastState map keeps task-level and per-run tuples isolated'`: D-18 axis "$lastState per-tuple isolation". Call `writeStatus('owner/repo', 42, 'pr_open')` then `writeRunBlockedIfNotTerminal('owner/repo', 42, '2026-05-27T19-15-22Z')` against the SAME writer instance. Assert: (a) the task-level `status.md` final state is `'pr_open'` (untouched by the per-run call); (b) `writeRunBlockedIfNotTerminal` is a no-op because the per-run `$lastState` 3-tuple key is missing (D-15 inherited: never invents state — the writer was never told a per-run state exists, so it does not write one).

    Case 18 — `'writeStatus and writeRunStatus on the same task do not cross-pollute lastState'`: D-18 axis "lastState per-tuple isolation (forward direction)". Call `writeStatus('owner/repo', 42, 'executing')` then `writeRunStatus('owner/repo', 42, '2026-05-27T19-15-22Z', 'planning')` then `writeBlockedIfNotTerminal('owner/repo', 42)`. Assert: (a) task-level final state is `'blocked'` (executing → blocked is a valid non-terminal-to-terminal transition); (b) the per-run final state remains `'planning'` (per-run `writeBlockedIfNotTerminal` was NOT called, so per-run state is unchanged).

    For each it-block that asserts specific frontmatter content, read the produced file via `file_get_contents($home.'/.copland/tasks/...')` and assert with Pest's `expect()` API. For each it-block that asserts row counts, use `substr_count` or `preg_match_all` against the body section.

    Do NOT introduce a `beforeEach` / `afterEach` block for HOME swapping — every existing HOME-using test in the codebase uses inline save/restore (RESEARCH §Pest Temp-HOME Pattern verified across 7 files). Do NOT introduce a top-of-file `makeTempHome()` helper — RESEARCH §"Helper extraction" recommends skipping it for ~12 tests. Do NOT use `Mockery` for the clock seam — use the writer's native `clock:` constructor parameter with a `fn () => '...'` or counter closure (PATTERNS.md §Pattern I).

    Do NOT delete the existing case #1. Do NOT modify `app/Support/RunLogStore.php`, `app/Services/TaskDirectoryWriterService.php`, `app/Services/RunOrchestratorService.php`, or any other source file from this plan — Plan 21-03 is test-only. Do NOT call any orchestrator method in any test — D-19 forbids orchestrator integration tests in Phase 21.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php --no-coverage 2>&amp;1 | grep -E "Tests:.*passed|OK" &amp;&amp; ./vendor/bin/pest --no-coverage 2>&amp;1 | grep -E "Tests:.*passed|OK" &amp;&amp; composer analyse 2>&amp;1 | grep -E "0 errors|No errors" &amp;&amp; test -z "$(git diff main -- app/Support/RunLogStore.php)"</automated>
  </verify>
  <done>
    - `grep -c "^it\(" tests/Feature/TaskDirectoryWriterServiceTest.php` returns at least 12 (target 12-18; preserves the existing case #1)
    - `grep -cE "writeRunStatus|writeRunOutcome|writeRunBlockedIfNotTerminal" tests/Feature/TaskDirectoryWriterServiceTest.php` returns at least 6 (the 3 new methods are each invoked in at least 2 cases)
    - `grep -c 'sys_get_temp_dir' tests/Feature/TaskDirectoryWriterServiceTest.php` returns at least 12 (every new it-block opens its own temp HOME)
    - `grep -v '^#' tests/Feature/TaskDirectoryWriterServiceTest.php | grep -cE 'beforeEach|afterEach'` returns 0 (no `beforeEach`/`afterEach` HOME-swap pattern introduced)
    - `grep -cE '\$originalHome' tests/Feature/TaskDirectoryWriterServiceTest.php` is roughly twice the it-block count (one save + one restore per case)
    - `./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php --no-coverage` exits 0 with all it-blocks passing
    - `./vendor/bin/pest --no-coverage` (full suite) exits 0 — no regression against the existing 132+ tests
    - `composer analyse` exits 0 with `[OK] No errors`
    - `git diff main -- app/Support/RunLogStore.php` produces empty output (TASK-04 phase-gate negative assertion)
    - `git diff main -- phpstan.neon` produces empty output (D-17 phase-gate; the composer-script approach left phpstan.neon untouched)
    - The new file size is at least 250 lines (12+ it-blocks at ~20-25 lines each minimum)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pest test runner → `$_SERVER['HOME']` global | tests mutate a process-global; restored inline at the end of each it-block |
| Pest test runner → `sys_get_temp_dir()` filesystem | each it-block creates a uniquely-named subdirectory; OS handles cleanup |
| `$_SERVER['HOME']` swap → developer's real `~/.copland/` | TASK-05 invariant; verified by the fact that `HomeDirectory::resolve()` reads `$_SERVER['HOME']` first (lines 17-23) and the writer's `taskDir()` chains through it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Information Disclosure | A failing test could leak the developer's real `~/.copland/` path if `$_SERVER['HOME']` swap is forgotten | mitigate | Every new it-block uses the canonical 4-line idiom verified across 7 existing test files. The grep-gate in `<done>` (`$originalHome` count ≈ 2 × it-block count) confirms every case both saves and restores. If a developer pattern-breaks, the gate fails. |
| T-21-03-02 | Tampering | Concurrent Pest workers swapping `$_SERVER['HOME']` could race | accept | Pest 3.x default runs single-threaded for a single file. Even if parallel mode is enabled later, each worker is a separate PHP process with its own `$_SERVER` superglobal. No cross-process race. |
| T-21-03-03 | DoS | `sys_get_temp_dir()` may fill with stale test directories over time | accept | Existing test suite has used this pattern across 7 files without cleanup. OS-level temp eviction handles it. Same posture as the rest of the codebase. |
| T-21-03-04 | Tampering | TASK-04 negative assertion (`git diff RunLogStore.php`) is a phase-gate check, not an it-block | mitigate | Documented in the task `<verify>` block as `test -z "$(git diff main -- app/Support/RunLogStore.php)"`. Verifier runs this against the full Phase 21 branch — not just this plan's commits. Same invariant repeated in Plan 21-01 and 21-02 acceptance criteria for defense in depth. |
| T-21-03-05 | Tampering | A test might accidentally call orchestrator code and create an integration test | mitigate | The action explicitly forbids orchestrator method calls per D-19. Pest's `use` statements at the top of the file should NOT import `RunOrchestratorService` — grep for `use App\\Services\\RunOrchestratorService` should return 0 if the rule is followed. |
| T-21-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs in this plan. All edits are to one existing test file. Pest and Mockery are already in `require-dev` per `composer.json`. |
</threat_model>

<verification>
After the task completes:
- `./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php --no-coverage` exits 0 with 12-18 tests passing
- `./vendor/bin/pest --no-coverage` (full suite) exits 0 — the existing 132+ baseline plus the new writer cases all green
- `composer analyse` exits 0 with `[OK] No errors`
- `git diff main -- app/Support/RunLogStore.php` produces empty output (TASK-04 phase-gate negative assertion)
- `git diff main -- phpstan.neon` produces empty output (D-17)
- `grep -c '^it\(' tests/Feature/TaskDirectoryWriterServiceTest.php` returns at least 12
- All 11 D-18 coverage axes are represented by at least one it-block; the suggested 18-case table in PATTERNS.md §Pattern H is the target shape (12 is the floor, 18 is the recommended count)
</verification>

<success_criteria>
- Test file expanded from 1 to 12-18 it-blocks against a temporary HOME (TASK-05)
- All 7 public writer methods exercised at least once (4 existing + 3 new from Plan 21-02)
- All 11 D-18 coverage axes represented
- TASK-04 negative assertion preserved across the entire Phase 21 branch
- PHPStan stays clean
- Full Pest suite remains green
- Phase 21 is now ready for `/gsd:verify-work` — all 4 ROADMAP success criteria reachable
</success_criteria>

<output>
Create `.planning/phases/21-per-run-artifacts-test-coverage/21-03-SUMMARY.md` when done. Include:
- Final it-block count and the slug list (one per case) so a verifier can map each case to a D-18 axis
- Final test file line count
- `./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php` output tail (the green summary line)
- `composer analyse` output tail (the `[OK] No errors` line)
- Confirmation that `git diff main -- app/Support/RunLogStore.php` is empty
- Confirmation that `git diff main -- phpstan.neon` is empty
- Note any axes that ended up split across multiple it-blocks vs combined (the 12-18 range allows the executor latitude)
</output>
