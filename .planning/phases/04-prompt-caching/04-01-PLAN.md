---
phase: 04-prompt-caching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/Services/ClaudeExecutorService.php, app/Support/ExecutorProgressFormatter.php, app/Support/AnthropicApiClient.php]
autonomous: true
requirements: [COST-01]

must_haves:
  truths:
    - "Round 1 of an executor run creates a cache entry for the system prompt"
    - "Rounds 2-12 of an executor run read from the system prompt cache"
    - "CLI output shows cache creation and read token counts for verification"
  artifacts:
    - path: "app/Services/ClaudeExecutorService.php"
      provides: "System prompt with cache_control"
    - path: "app/Support/ExecutorProgressFormatter.php"
      provides: "Updated reporting including cache tokens"
  key_links:
    - from: "app/Services/ClaudeExecutorService.php"
      to: "app/Support/AnthropicApiClient.php"
      via: "messages() call with TextBlockParam array"
---

<objective>
Implement Anthropic Prompt Caching for the executor loop by adding a cache marker to the system prompt. Surface cache token usage in the CLI output to verify the implementation.

Purpose: Reduce input token costs by ~90% for rounds 2-12 of the executor loop.
Output: Prompt caching enabled on the executor system prompt and visible in progress logs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prompt-caching/04-CONTEXT.md
@app/Services/ClaudeExecutorService.php
@app/Support/AnthropicApiClient.php
@app/Support/ExecutorProgressFormatter.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ClaudeExecutorService to use cached system prompt</name>
  <files>app/Services/ClaudeExecutorService.php</files>
  <action>
    Modify `ClaudeExecutorService::executeWithPolicy()` to wrap the `$systemPrompt` in a `TextBlockParam` with `CacheControlEphemeral`.
    - Import `Anthropic\Messages\TextBlockParam` and `Anthropic\Messages\CacheControlEphemeral`.
    - Construct `$system` as an array containing one `TextBlockParam` created via `TextBlockParam::with(text: $systemPrompt, cacheControl: CacheControlEphemeral::with())`.
    - Pass this `$system` array to the `apiClient->messages()` call instead of the raw string.
    - Reference D-01, D-04, D-05 from 04-CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "TextBlockParam" app/Services/ClaudeExecutorService.php && grep -q "CacheControlEphemeral" app/Services/ClaudeExecutorService.php</automated>
  </verify>
  <done>System prompt is wrapped in cache control block in ClaudeExecutorService.</done>
</task>

<task type="auto">
  <name>Task 2: Update AnthropicApiClient and ProgressFormatter to surface cache tokens</name>
  <files>app/Support/AnthropicApiClient.php, app/Support/ExecutorProgressFormatter.php, app/Services/ClaudeExecutorService.php</files>
  <action>
    Modify the reporting chain to surface `cache_creation_input_tokens` and `cache_read_input_tokens` in the CLI output.
    - In `app/Support/ExecutorProgressFormatter.php`: Update `response()` to accept optional `$cacheWrite` and `$cacheRead` integers and include them in the formatted string (e.g., "received response with N tool call(s) after Ts [cache: +W, R]").
    - In `app/Services/ClaudeExecutorService.php`: Extract `cacheCreationInputTokens` and `cacheReadInputTokens` from `$response->usage` and pass them to `ExecutorProgressFormatter::response()`.
    - In `app/Support/AnthropicApiClient.php`: Although not strictly required by the SDK for array-based system prompts, ensure the method signature and docblock are clear about the `system` parameter being `string|array`. (This satisfies D-05).
  </action>
  <verify>
    <automated>grep -q "cacheWrite" app/Support/ExecutorProgressFormatter.php && grep -q "cacheRead" app/Support/ExecutorProgressFormatter.php</automated>
  </verify>
  <done>Cache token usage is extracted from API responses and displayed in the executor progress logs.</done>
</task>

</tasks>

<verification>
1. Run a mock-based test or a small real run if possible.
2. Verify that Round 1 shows non-zero cache creation tokens (if not already cached).
3. Verify that Round 2+ shows non-zero cache read tokens.
4. Verify that total cost estimation (even if flat-rate for now) still works.
</verification>

<success_criteria>
- ClaudeExecutorService sends system prompt as a TextBlockParam array with cache_control.
- Rounds 2-12 show cache_read_input_tokens in the CLI progress output.
- The system prompt remains static and cached even as messages grow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompt-caching/04-01-SUMMARY.md`
</output>
