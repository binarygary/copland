---
phase: 02-executor-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Data/PlanResult.php
  - app/Services/ClaudePlannerService.php
  - resources/prompts/planner.md
autonomous: true
requirements: [RELY-03]
user_setup: []
must_haves:
  truths:
    - Planner output includes a structured `blocked_write_paths` array instead of relying on free-text guardrails for write blocking.
    - The shared plan contract can carry `blocked_write_paths` from planner output into execution-time objects.
  artifacts:
    - app/Data/PlanResult.php
    - app/Services/ClaudePlannerService.php
    - resources/prompts/planner.md
  key_links:
    - planner.md JSON schema -> ClaudePlannerService parsing -> PlanResult blockedWritePaths field
---

<objective>
Extend the plan contract to carry structured blocked write paths.

Purpose: Phase 2 cannot enforce structured write blocking until the planner schema and shared contract object can represent `blocked_write_paths`.
Output: Planner prompt schema, planner parsing, and `PlanResult` support for a dedicated `blocked_write_paths` field.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-executor-hardening/02-CONTEXT.md
@.planning/phases/02-executor-hardening/02-RESEARCH.md
@app/Data/PlanResult.php
@app/Services/ClaudePlannerService.php
@resources/prompts/planner.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add blockedWritePaths to the shared plan contract</name>
  <files>app/Data/PlanResult.php</files>
  <read_first>app/Data/PlanResult.php, .planning/phases/02-executor-hardening/02-CONTEXT.md</read_first>
  <action>Add a new readonly array property `public readonly array $blockedWritePaths,` to `PlanResult` immediately after `filesToChange` so the contract order stays grouped by file restrictions. Do not remove `guardrails`; keep it as a separate advisory field. Ensure constructor call sites will be able to pass the new array positionally.</action>
  <verify>php -l app/Data/PlanResult.php</verify>
  <acceptance_criteria>
    - app/Data/PlanResult.php contains `public readonly array $blockedWritePaths,`
    - app/Data/PlanResult.php still contains `public readonly array $guardrails,`
  </acceptance_criteria>
  <done>The shared plan contract has a dedicated field for structured blocked write paths.</done>
</task>

<task type="auto">
  <name>Task 2: Teach planner prompt and parser about blocked_write_paths</name>
  <files>app/Services/ClaudePlannerService.php, resources/prompts/planner.md</files>
  <read_first>app/Services/ClaudePlannerService.php, resources/prompts/planner.md, app/Data/PlanResult.php</read_first>
  <action>Update `resources/prompts/planner.md` so the planning rules and output JSON schema include a `blocked_write_paths` array alongside `files_to_change`, with guidance that it contains plan-scoped paths the executor must not write even if broader guardrails mention other concerns. Then update `ClaudePlannerService::planTask()` to parse `blocked_write_paths` with `PlanFieldNormalizer::list($json['blocked_write_paths'] ?? [])` and pass it into the new `PlanResult` constructor slot.</action>
  <verify>php -l app/Services/ClaudePlannerService.php</verify>
  <acceptance_criteria>
    - resources/prompts/planner.md contains `"blocked_write_paths": []`
    - app/Services/ClaudePlannerService.php contains `blockedWritePaths: PlanFieldNormalizer::list($json['blocked_write_paths'] ?? []),`
    - app/Services/ClaudePlannerService.php still contains `guardrails: PlanFieldNormalizer::list($json['guardrails'] ?? []),`
  </acceptance_criteria>
  <done>Planner schema and parser now emit structured blocked write paths into the shared contract.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Data/PlanResult.php
- [ ] php -l app/Services/ClaudePlannerService.php
- [ ] rg -n '"blocked_write_paths": \[\]' resources/prompts/planner.md
</verification>

<success_criteria>
- All tasks completed
- PlanResult includes `blockedWritePaths`
- Planner schema and parser carry `blocked_write_paths`
- `guardrails` remains present as a separate advisory field
</success_criteria>

<output>
After completion, create `.planning/phases/02-executor-hardening/02-02-SUMMARY.md`
</output>
