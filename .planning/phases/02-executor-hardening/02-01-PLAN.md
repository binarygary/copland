---
phase: 02-executor-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Config/RepoConfig.php
  - app/Commands/RunCommand.php
  - tests/Unit/RepoConfigTest.php
autonomous: true
requirements: [RELY-02]
user_setup: []
must_haves:
  truths:
    - Repo configuration exposes a read-file line cap with a default value of 300.
    - The executor runtime receives the configured read-file line cap through repoProfile data.
  artifacts:
    - app/Config/RepoConfig.php
    - app/Commands/RunCommand.php
    - tests/Unit/RepoConfigTest.php
  key_links:
    - RepoConfig::readFileMaxLines -> RunCommand repoProfile -> ClaudeExecutorService policy construction
---

<objective>
Add repo-scoped configuration plumbing for bounded executor reads.

Purpose: Phase 2 requires the read-file cap to be configurable per repository, and the current runtime only passes round limits and blocked paths into executor policy.
Output: A new repo config key/accessor, `RunCommand` wiring that passes the value into `repoProfile`, and a regression test covering the default config template.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-executor-hardening/02-CONTEXT.md
@.planning/phases/02-executor-hardening/02-RESEARCH.md
@app/Config/RepoConfig.php
@app/Commands/RunCommand.php
@tests/Unit/RepoConfigTest.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add repo-level read-file limit config and accessor</name>
  <files>app/Config/RepoConfig.php, tests/Unit/RepoConfigTest.php</files>
  <read_first>app/Config/RepoConfig.php, tests/Unit/RepoConfigTest.php, .planning/phases/02-executor-hardening/02-CONTEXT.md</read_first>
  <action>Add a new `.copland.yml` default key named `read_file_max_lines: 300` immediately after `max_executor_rounds` in `RepoConfig::ensureExists()`. Add a typed accessor `public function readFileMaxLines(): int` returning `(int) ($this->data['read_file_max_lines'] ?? 300)`. Update `tests/Unit/RepoConfigTest.php` to assert the accessor returns `300` by default and the generated config file contains the exact line `read_file_max_lines: 300`.</action>
  <verify>./vendor/bin/pest tests/Unit/RepoConfigTest.php</verify>
  <acceptance_criteria>
    - app/Config/RepoConfig.php contains `read_file_max_lines: 300`
    - app/Config/RepoConfig.php contains `public function readFileMaxLines(): int`
    - tests/Unit/RepoConfigTest.php contains `expect($config->readFileMaxLines())->toBe(300);`
    - tests/Unit/RepoConfigTest.php contains `read_file_max_lines: 300`
  </acceptance_criteria>
  <done>Repo config exposes the Phase 2 read limit with verified default coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Pass read-file limit through RunCommand repoProfile</name>
  <files>app/Commands/RunCommand.php</files>
  <read_first>app/Commands/RunCommand.php, app/Config/RepoConfig.php, app/Services/ClaudeExecutorService.php</read_first>
  <action>Update the `$repoProfile` array in `RunCommand::handle()` to include a new key `'read_file_max_lines' => $repoConfig->readFileMaxLines(),` alongside the existing executor policy values. Do not change any other repoProfile keys or orchestration flow.</action>
  <verify>php -l app/Commands/RunCommand.php</verify>
  <acceptance_criteria>
    - app/Commands/RunCommand.php contains `'read_file_max_lines' => $repoConfig->readFileMaxLines(),`
    - app/Commands/RunCommand.php still contains `'max_executor_rounds' => $repoConfig->maxExecutorRounds(),`
  </acceptance_criteria>
  <done>Executor runtime wiring now carries the configurable read limit into repoProfile.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] ./vendor/bin/pest tests/Unit/RepoConfigTest.php
- [ ] php -l app/Config/RepoConfig.php
- [ ] php -l app/Commands/RunCommand.php
</verification>

<success_criteria>
- All tasks completed
- Repo config defaults include `read_file_max_lines: 300`
- RunCommand repoProfile contains the new `read_file_max_lines` key
- No existing RepoConfig expectations regress
</success_criteria>

<output>
After completion, create `.planning/phases/02-executor-hardening/02-01-SUMMARY.md`
</output>
