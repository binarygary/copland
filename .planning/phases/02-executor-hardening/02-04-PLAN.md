---
phase: 02-executor-hardening
plan: 04
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - app/Services/PlanValidatorService.php
  - app/Support/PlanArtifactStore.php
  - tests/Unit/PlanArtifactStoreTest.php
autonomous: true
requirements: [RELY-03]
user_setup: []
must_haves:
  truths:
    - Plans carrying `blocked_write_paths` are validated and persisted as structured data.
    - Stored plan artifacts include `blocked_write_paths` so execution/debug tooling can inspect the same contract the executor enforces.
  artifacts:
    - app/Services/PlanValidatorService.php
    - app/Support/PlanArtifactStore.php
    - tests/Unit/PlanArtifactStoreTest.php
  key_links:
    - PlanResult blockedWritePaths -> PlanValidatorService checks
    - PlanResult blockedWritePaths -> PlanArtifactStore JSON payload
---

<objective>
Finish blocked-write contract plumbing in validation and artifact storage.

Purpose: Structured write restrictions are only reliable if the validator and saved plan artifacts preserve the same machine-readable field the executor uses.
Output: Validation rules and stored plan artifacts updated for `blocked_write_paths`, plus regression coverage for persisted plan JSON.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-executor-hardening/02-CONTEXT.md
@.planning/phases/02-executor-hardening/02-RESEARCH.md
@app/Services/PlanValidatorService.php
@app/Support/PlanArtifactStore.php
@tests/Unit/PlanArtifactStoreTest.php
@app/Data/PlanResult.php
@.planning/phases/02-executor-hardening/02-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate structured blocked write paths in PlanValidatorService</name>
  <files>app/Services/PlanValidatorService.php</files>
  <read_first>app/Services/PlanValidatorService.php, app/Data/PlanResult.php, .planning/phases/02-executor-hardening/02-CONTEXT.md</read_first>
  <action>Add validation for `blockedWritePaths` in `PlanValidatorService::validate()`. Normalize the intent of the new field by checking each blocked write path against the repo-level blocked path list and by rejecting any plan where a path appears in both `$plan->filesToChange` and `$plan->blockedWritePaths`. Use explicit error messages that mention `blocked_write_paths` so failures are understandable at plan time rather than at executor runtime.</action>
  <verify>php -l app/Services/PlanValidatorService.php</verify>
  <acceptance_criteria>
    - app/Services/PlanValidatorService.php contains `blocked_write_paths`
    - app/Services/PlanValidatorService.php contains an error path for files listed in both `files_to_change` and `blocked_write_paths`
  </acceptance_criteria>
  <done>Plan validation now treats `blocked_write_paths` as a first-class contract field.</done>
</task>

<task type="auto">
  <name>Task 2: Persist blocked write paths in plan artifacts and test coverage</name>
  <files>app/Support/PlanArtifactStore.php, tests/Unit/PlanArtifactStoreTest.php</files>
  <read_first>app/Support/PlanArtifactStore.php, tests/Unit/PlanArtifactStoreTest.php, app/Data/PlanResult.php</read_first>
  <action>Update `PlanArtifactStore::save()` so the serialized `plan` payload includes `'blocked_write_paths' => $plan->blockedWritePaths,` adjacent to `files_to_change`. Then update `tests/Unit/PlanArtifactStoreTest.php` fixture construction and assertions so stored JSON is checked for the exact `blocked_write_paths` key and expected path values.</action>
  <verify>./vendor/bin/pest tests/Unit/PlanArtifactStoreTest.php</verify>
  <acceptance_criteria>
    - app/Support/PlanArtifactStore.php contains `'blocked_write_paths' => $plan->blockedWritePaths,`
    - tests/Unit/PlanArtifactStoreTest.php contains `blocked_write_paths`
    - tests/Unit/PlanArtifactStoreTest.php asserts the stored JSON includes the expected blocked path value
  </acceptance_criteria>
  <done>Saved plan artifacts preserve `blocked_write_paths` for later execution and debugging.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] ./vendor/bin/pest tests/Unit/PlanArtifactStoreTest.php
- [ ] php -l app/Services/PlanValidatorService.php
- [ ] php -l app/Support/PlanArtifactStore.php
</verification>

<success_criteria>
- All tasks completed
- Plan validation checks `blocked_write_paths`
- Plan artifacts serialize `blocked_write_paths`
- Regression coverage proves the persisted contract includes blocked write paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-executor-hardening/02-04-SUMMARY.md`
</output>
