---
phase: 02-executor-hardening
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - app/Support/ExecutorPolicy.php
  - app/Services/ClaudeExecutorService.php
  - tests/Unit/ExecutorPolicyTest.php
autonomous: true
requirements: [RELY-02, RELY-03]
user_setup: []
must_haves:
  truths:
    - `read_file` returns only the first configured number of lines plus a truncation notice when a file exceeds the limit.
    - `write_file` and `replace_in_file` reject plan-scoped `blocked_write_paths` even when the file also appears in `files_to_change`.
  artifacts:
    - app/Support/ExecutorPolicy.php
    - app/Services/ClaudeExecutorService.php
    - tests/Unit/ExecutorPolicyTest.php
  key_links:
    - RunCommand repoProfile read_file_max_lines -> ExecutorPolicy -> ClaudeExecutorService::readFile
    - PlanResult blockedWritePaths -> ExecutorPolicy write enforcement -> ClaudeExecutorService write and replace flows
---

<objective>
Implement bounded reads and structured write blocking in executor runtime.

Purpose: Phase 2’s user-visible behavior change happens in executor runtime, where read truncation and blocked write enforcement must be deterministic and machine-readable.
Output: Executor policy support for read caps and blocked write lists, executor read/write behavior updated to use it, and targeted policy regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-executor-hardening/02-CONTEXT.md
@.planning/phases/02-executor-hardening/02-RESEARCH.md
@app/Support/ExecutorPolicy.php
@app/Services/ClaudeExecutorService.php
@tests/Unit/ExecutorPolicyTest.php
@.planning/phases/02-executor-hardening/02-01-PLAN.md
@.planning/phases/02-executor-hardening/02-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend ExecutorPolicy with read limit and blocked-write helpers</name>
  <files>app/Support/ExecutorPolicy.php, tests/Unit/ExecutorPolicyTest.php</files>
  <read_first>app/Support/ExecutorPolicy.php, tests/Unit/ExecutorPolicyTest.php, app/Data/PlanResult.php</read_first>
  <action>Update `ExecutorPolicy` to accept a third constructor argument `private int $readFileMaxLines = 300`. Add a public accessor `readFileMaxLines(): int`. Add a new method `assertWritePathNotBlocked(string $path, array $blockedWritePaths): string` that normalizes the input path and throws `PolicyViolationException("Write to '{$normalized}' blocked by blocked_write_paths")` when the path matches or is nested beneath any blocked write path. Extend `tests/Unit/ExecutorPolicyTest.php` with one passing assertion for an allowed path and one failing assertion for a blocked path using the new helper.</action>
  <verify>./vendor/bin/pest tests/Unit/ExecutorPolicyTest.php</verify>
  <acceptance_criteria>
    - app/Support/ExecutorPolicy.php contains `private int $readFileMaxLines = 300`
    - app/Support/ExecutorPolicy.php contains `public function readFileMaxLines(): int`
    - app/Support/ExecutorPolicy.php contains `assertWritePathNotBlocked`
    - tests/Unit/ExecutorPolicyTest.php contains `blocked_write_paths` or the new helper assertion text proving blocked writes are tested
  </acceptance_criteria>
  <done>ExecutorPolicy can supply the read cap and enforce plan-scoped blocked write paths.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce truncation and blocked writes in ClaudeExecutorService</name>
  <files>app/Services/ClaudeExecutorService.php</files>
  <read_first>app/Services/ClaudeExecutorService.php, app/Support/ExecutorPolicy.php, app/Data/PlanResult.php</read_first>
  <action>Update `executeWithRepoProfile()` to pass `readFileMaxLines: (int) ($repoProfile['read_file_max_lines'] ?? 300)` into `ExecutorPolicy`. In `readFile()`, split the file into lines, return the full content when line count is `<= $policy->readFileMaxLines()`, and otherwise return the first `N` lines plus a footer that explicitly states truncation after `{N}` lines and that additional lines were omitted. In both `writeFile()` and `replaceInFile()`, call `assertWritePathNotBlocked($path, $plan->blockedWritePaths)` after `assertWritePathAllowed()` and remove the current free-text guardrail matching loop from `writeFile()`. Also include `blocked_write_paths` in the executor contract JSON that is sent to the model so the runtime contract reflects the new structured field.</action>
  <verify>php -l app/Services/ClaudeExecutorService.php</verify>
  <acceptance_criteria>
    - app/Services/ClaudeExecutorService.php contains `'blocked_write_paths' => $plan->blockedWritePaths`
    - app/Services/ClaudeExecutorService.php contains `read_file_max_lines` handling through `readFileMaxLines()`
    - app/Services/ClaudeExecutorService.php contains a truncation message including `truncated after`
    - app/Services/ClaudeExecutorService.php no longer contains `str_contains(strtolower($guardrail), 'block')`
  </acceptance_criteria>
  <done>Executor runtime now truncates large reads visibly and enforces structured blocked write paths.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] ./vendor/bin/pest tests/Unit/ExecutorPolicyTest.php
- [ ] php -l app/Support/ExecutorPolicy.php
- [ ] php -l app/Services/ClaudeExecutorService.php
</verification>

<success_criteria>
- All tasks completed
- Executor policy exposes a configurable read cap
- Executor read_file returns truncated content with explicit notice when over limit
- Executor write and replace paths reject `blocked_write_paths` without parsing guardrail text
</success_criteria>

<output>
After completion, create `.planning/phases/02-executor-hardening/02-03-SUMMARY.md`
</output>
