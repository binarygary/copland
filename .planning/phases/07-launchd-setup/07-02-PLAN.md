---
phase: 07-launchd-setup
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - app/Commands/SetupCommand.php
  - tests/Feature/SetupCommandTest.php
autonomous: false
requirements: [SCHED-03]
user_setup:
  - "Run `copland setup` on macOS and confirm the LaunchAgent loads successfully with launchctl."
must_haves:
  truths:
    - "`copland setup` reloads the LaunchAgent idempotently instead of failing when the plist already exists."
    - "The command surfaces enough output for a user to verify the installed LaunchAgent and scheduled job."
    - "A real launchctl load succeeds on the user's macOS machine after installation."
  artifacts:
    - path: "app/Commands/SetupCommand.php"
      provides: "launchctl reload/install behavior"
    - path: "tests/Feature/SetupCommandTest.php"
      provides: "coverage for setup command messaging and launchctl shell orchestration"
  key_links:
    - from: "app/Commands/SetupCommand.php"
      to: "tests/Feature/SetupCommandTest.php"
      via: "command output and launchctl invocation expectations"
    - from: "app/Commands/SetupCommand.php"
      to: "LaunchAgent plist file"
      via: "unload/reload lifecycle"
---

<objective>
Finish `copland setup` by wiring launchctl reload behavior and verifying the command end-to-end.

Purpose: Writing a plist is not enough; Phase 7 requires a usable installer that registers the LaunchAgent successfully and guides the user through the final macOS verification step.
Output: Reload-capable setup command, feature coverage, and a blocking human verification checkpoint for real launchctl registration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-launchd-setup/07-CONTEXT.md
@.planning/phases/07-launchd-setup/07-01-PLAN.md
@.planning/research/FEATURES.md
@app/Commands/SetupCommand.php
@app/Support/LaunchdPlist.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add idempotent launchctl reload behavior to `copland setup`</name>
  <files>app/Commands/SetupCommand.php</files>
  <read_first>app/Commands/SetupCommand.php, .planning/phases/07-launchd-setup/07-CONTEXT.md, .planning/research/FEATURES.md</read_first>
  <action>Update `SetupCommand` so that after writing the plist it attempts an unload-if-present / load sequence with `launchctl` for the installed LaunchAgent path. The command should tolerate the "not loaded" case during unload, then load the new plist, and print a concise success summary that includes the plist path, label, and a manual `launchctl start <label>` command for immediate testing. Keep the implementation user-scoped and macOS-specific; do not add Linux fallbacks in this phase.</action>
  <verify>php -l app/Commands/SetupCommand.php</verify>
  <acceptance_criteria>
    - app/Commands/SetupCommand.php contains `launchctl`
    - app/Commands/SetupCommand.php attempts unload before load or an equivalent idempotent reload sequence
    - app/Commands/SetupCommand.php prints a manual start/test command using the LaunchAgent label
  </acceptance_criteria>
  <done>`copland setup` can refresh an existing LaunchAgent definition instead of only writing files.</done>
</task>

<task type="auto">
  <name>Task 2: Add feature coverage for command output and launchctl orchestration</name>
  <files>tests/Feature/SetupCommandTest.php</files>
  <read_first>app/Commands/SetupCommand.php, tests/Feature/RunCommandTest.php, .planning/phases/07-launchd-setup/07-01-PLAN.md</read_first>
  <action>Add `tests/Feature/SetupCommandTest.php` that exercises the setup command with a temporary HOME directory and a mocked shell runner or equivalent seam. Assert that the command writes a plist under `Library/LaunchAgents`, triggers the launchctl reload flow in the expected order, and prints the installed plist path plus the manual `launchctl start` verification command. Keep the test isolated from the real machine launchctl state.</action>
  <verify>./vendor/bin/pest tests/Feature/SetupCommandTest.php</verify>
  <acceptance_criteria>
    - tests/Feature/SetupCommandTest.php asserts plist creation under `Library/LaunchAgents`
    - tests/Feature/SetupCommandTest.php asserts launchctl reload behavior
    - tests/Feature/SetupCommandTest.php asserts user-facing verification output
  </acceptance_criteria>
  <done>The setup command's installer flow is covered without hitting the real user's LaunchAgents directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full `copland setup` LaunchAgent installer with plist generation and launchctl reload behavior.</what-built>
  <how-to-verify>
    1. Run `copland setup` on your macOS machine from the Copland checkout.
    2. Confirm it writes a plist under `~/Library/LaunchAgents/`.
    3. Confirm the command reports a successful launchctl load/reload.
    4. Run the printed `launchctl start ...` or equivalent verification command.
    5. Verify the LaunchAgent starts without launchctl errors and that Copland can still resolve `~/.copland.yml`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Commands/SetupCommand.php
- [ ] ./vendor/bin/pest tests/Feature/SetupCommandTest.php
- [ ] Human verification confirms launchctl registration works on macOS
</verification>

<success_criteria>
- All tasks completed
- `copland setup` reloads the user LaunchAgent idempotently
- Automated tests cover plist writing and launchctl orchestration
- Real launchctl registration is manually verified on macOS
</success_criteria>

<output>
After completion, create `.planning/phases/07-launchd-setup/07-02-SUMMARY.md`
</output>
