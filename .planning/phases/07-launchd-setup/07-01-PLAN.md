---
phase: 07-launchd-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Commands/SetupCommand.php
  - app/Support/LaunchdPlist.php
  - tests/Unit/LaunchdPlistTest.php
autonomous: true
requirements: [SCHED-03]
user_setup: []
must_haves:
  truths:
    - "`copland setup` can generate a stable per-user LaunchAgent plist with explicit HOME and PATH values."
    - "The generated plist runs the local `copland run` command on a nightly StartCalendarInterval."
    - "Launchd stdout/stderr paths are written under the user's Copland log directory."
  artifacts:
    - path: "app/Commands/SetupCommand.php"
      provides: "CLI entrypoint for launchd installation"
    - path: "app/Support/LaunchdPlist.php"
      provides: "Deterministic plist generation for the Copland LaunchAgent"
    - path: "tests/Unit/LaunchdPlistTest.php"
      provides: "Coverage for plist structure and required launchd keys"
  key_links:
    - from: "app/Commands/SetupCommand.php"
      to: "app/Support/LaunchdPlist.php"
      via: "plist payload generation"
    - from: "app/Support/LaunchdPlist.php"
      to: "app/Support/HomeDirectory.php"
      via: "HOME-based LaunchAgent and log paths"
---

<objective>
Create the LaunchAgent generation foundation for `copland setup`.

Purpose: Phase 7 needs a deterministic plist generator and CLI surface before launchctl reload behavior can be layered on top.
Output: A new `setup` command plus a reusable plist builder and unit coverage for the generated launchd contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-launchd-setup/07-CONTEXT.md
@.planning/research/FEATURES.md
@app/Support/HomeDirectory.php
@app/Commands/RunCommand.php
@app/Commands/IssuesCommand.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a reusable LaunchdPlist builder for the user LaunchAgent</name>
  <files>app/Support/LaunchdPlist.php</files>
  <read_first>app/Support/HomeDirectory.php, .planning/phases/07-launchd-setup/07-CONTEXT.md, .planning/research/FEATURES.md</read_first>
  <action>Create `App\Support\LaunchdPlist` that builds the full XML plist for a stable per-user LaunchAgent label, `~/Library/LaunchAgents/...plist`, `StartCalendarInterval`, `EnvironmentVariables` with explicit `HOME` and a launchd-safe `PATH`, `ProgramArguments` that execute the local project `copland` entrypoint with the `run` subcommand, and explicit `StandardOutPath` / `StandardErrorPath` under the user's `.copland/logs/launchd/` directory. The builder should accept concrete schedule inputs (`hour`, `minute`) and concrete runtime paths (`home`, `projectRoot`, `phpBinary`) instead of reading globals internally so the output is easy to test.</action>
  <verify>php -l app/Support/LaunchdPlist.php</verify>
  <acceptance_criteria>
    - app/Support/LaunchdPlist.php contains `StartCalendarInterval`
    - app/Support/LaunchdPlist.php contains `EnvironmentVariables`
    - app/Support/LaunchdPlist.php contains `StandardOutPath`
    - app/Support/LaunchdPlist.php contains `StandardErrorPath`
  </acceptance_criteria>
  <done>A dedicated support class can generate the exact LaunchAgent plist Copland needs.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `copland setup` command shell and plist file installation</name>
  <files>app/Commands/SetupCommand.php</files>
  <read_first>app/Commands/IssuesCommand.php, app/Commands/RunCommand.php, app/Support/HomeDirectory.php, app/Support/LaunchdPlist.php</read_first>
  <action>Create `App\Commands\SetupCommand` with a signature that supports a single nightly schedule via concrete time inputs such as `--hour` and `--minute` or an equivalent single-time surface. The command must resolve the user HOME directory, compute the LaunchAgent plist path under `~/Library/LaunchAgents`, create the `.copland/logs/launchd` directory if missing, generate the plist using `LaunchdPlist`, and write the plist file idempotently. Print the plist path, job label, scheduled time, and the exact launchctl verification command the user can run. Do not invoke `launchctl` yet in this plan.</action>
  <verify>php -l app/Commands/SetupCommand.php</verify>
  <acceptance_criteria>
    - app/Commands/SetupCommand.php contains `protected $signature = 'setup`
    - app/Commands/SetupCommand.php writes to `Library/LaunchAgents`
    - app/Commands/SetupCommand.php creates `.copland/logs/launchd`
    - app/Commands/SetupCommand.php prints the installed plist path and label
  </acceptance_criteria>
  <done>The CLI can install or update the LaunchAgent plist on disk for the current user.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for plist structure and required launchd fields</name>
  <files>tests/Unit/LaunchdPlistTest.php</files>
  <read_first>app/Support/LaunchdPlist.php, tests/Unit/GlobalConfigTest.php, .planning/phases/07-launchd-setup/07-CONTEXT.md</read_first>
  <action>Add `tests/Unit/LaunchdPlistTest.php` that verifies the generated XML includes a stable label, explicit `HOME` and `PATH`, `ProgramArguments` for the local `copland run` command, a single nightly `StartCalendarInterval`, and launchd stdout/stderr log paths under `.copland/logs/launchd`. Use fixed input values in the test so exact substrings can be asserted without relying on the current machine state.</action>
  <verify>./vendor/bin/pest tests/Unit/LaunchdPlistTest.php</verify>
  <acceptance_criteria>
    - tests/Unit/LaunchdPlistTest.php asserts `StartCalendarInterval` output
    - tests/Unit/LaunchdPlistTest.php asserts `HOME` and `PATH` environment variables
    - tests/Unit/LaunchdPlistTest.php asserts `copland` and `run` appear in `ProgramArguments`
  </acceptance_criteria>
  <done>The plist contract is regression-tested before launchctl integration is added.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Support/LaunchdPlist.php
- [ ] php -l app/Commands/SetupCommand.php
- [ ] ./vendor/bin/pest tests/Unit/LaunchdPlistTest.php
</verification>

<success_criteria>
- All tasks completed
- `copland setup` can write a valid user LaunchAgent plist to disk
- The plist includes explicit HOME, PATH, log paths, and nightly StartCalendarInterval scheduling
- Plist generation is covered by unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-launchd-setup/07-01-SUMMARY.md`
</output>
