---
phase: 10-orchestrator-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/RunOrchestratorService.php
  - tests/Unit/RunOrchestratorServiceTest.php
autonomous: true
requirements: [TEST-02]
user_setup: []
must_haves:
  truths:
    - "`RunOrchestratorService` returns the expected result for the happy path and roadmap-listed early exits."
    - "`RunOrchestratorService` runs workspace cleanup from `finally` even when execution crashes."
    - "Orchestrator tests run offline with mocked collaborators and no real git, filesystem, or API work."
  artifacts:
    - path: "tests/Unit/RunOrchestratorServiceTest.php"
      provides: "service-level orchestrator coverage for happy path, early exits, and cleanup"
    - path: "app/Services/RunOrchestratorService.php"
      provides: "minimal test seam only if needed for artifact/log store isolation"
  key_links:
    - from: "tests/Unit/RunOrchestratorServiceTest.php"
      to: "app/Services/RunOrchestratorService.php"
      via: "mocked collaborators and deterministic run-log/artifact assertions"
---

<objective>
Add service-level automated coverage for the orchestrator pipeline.

Purpose: Phase 10 closes the remaining high-risk control-flow gap by proving `RunOrchestratorService` handles the happy path, early exits, crash cleanup, and run logging correctly without real external side effects.
Output: A deterministic orchestrator test suite, plus any minimal seam required to isolate internally-constructed stores.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-orchestrator-tests/10-CONTEXT.md
@app/Services/RunOrchestratorService.php
@app/Data/RunResult.php
@app/Support/RunLogStore.php
@app/Support/PlanArtifactStore.php
@tests/Unit/ClaudeExecutorServiceTest.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add any minimal orchestrator seam required for deterministic service tests</name>
  <files>app/Services/RunOrchestratorService.php</files>
  <read_first>app/Services/RunOrchestratorService.php, .planning/phases/10-orchestrator-tests/10-CONTEXT.md, app/Support/RunLogStore.php, app/Support/PlanArtifactStore.php</read_first>
  <action>Inspect `RunOrchestratorService` and only add narrow seams where internal side effects block deterministic unit tests. The likely case is store creation for `PlanArtifactStore` and `RunLogStore`; if needed, inject callables or collaborator objects so tests can capture saved artifacts and appended payloads without touching real user-scoped paths. Preserve the public `run(...)` API and avoid architectural refactors beyond what the tests need.</action>
  <verify>php -l app/Services/RunOrchestratorService.php</verify>
  <acceptance_criteria>
    - app/Services/RunOrchestratorService.php remains production-compatible
    - any new seam is minimal and directly justified by test isolation
    - orchestrator control flow remains unchanged outside the new seam
  </acceptance_criteria>
  <done>The orchestrator stays production-safe and becomes deterministic to test.</done>
</task>

<task type="auto">
  <name>Task 2: Add service-level orchestrator tests for happy path, early exits, and cleanup</name>
  <files>tests/Unit/RunOrchestratorServiceTest.php</files>
  <read_first>app/Services/RunOrchestratorService.php, app/Data/RunResult.php, .planning/phases/10-orchestrator-tests/10-CONTEXT.md, tests/Unit/ClaudeExecutorServiceTest.php</read_first>
  <action>Create `tests/Unit/RunOrchestratorServiceTest.php` with mocked collaborators for every injected service. Add at least these scenarios: one happy-path run that reaches draft PR creation and label removal; one selector skip or planner decline early-exit case; one validation or verification failure path that returns a failed/skipped result without continuing; and one thrown-exception case proving `workspace->cleanup(...)` still runs from `finally` and the crash is rethrown after partial logging data is assembled. Assert both the returned `RunResult` shape and the key collaborator calls. Keep the suite fully offline with no real git, GitHub, filesystem, or Anthropic calls.</action>
  <verify>./vendor/bin/pest tests/Unit/RunOrchestratorServiceTest.php</verify>
  <acceptance_criteria>
    - tests/Unit/RunOrchestratorServiceTest.php covers the happy path
    - tests/Unit/RunOrchestratorServiceTest.php covers roadmap-listed early exits
    - tests/Unit/RunOrchestratorServiceTest.php proves `finally` cleanup runs on crash
    - tests/Unit/RunOrchestratorServiceTest.php uses mocked collaborators only
    - tests/Unit/RunOrchestratorServiceTest.php performs no real API, git, or filesystem side effects outside controlled test fixtures
  </acceptance_criteria>
  <done>Run orchestration behavior is regression-tested at the service boundary.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Services/RunOrchestratorService.php
- [ ] ./vendor/bin/pest tests/Unit/RunOrchestratorServiceTest.php
</verification>

<success_criteria>
- All tasks completed
- `RunOrchestratorService` has service-level coverage for the roadmap-required paths
- Tests remain offline and deterministic
- Any orchestrator seam added is minimal and production-safe
</success_criteria>

<output>
After completion, create `.planning/phases/10-orchestrator-tests/10-01-SUMMARY.md`
</output>
