---
phase: 06-multi-repo-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/Config/GlobalConfig.php, app/Commands/RunCommand.php]
autonomous: true
requirements: [SCHED-01]

must_haves:
  truths:
    - "GlobalConfig includes a commented-out 'repos' list in the default template"
    - "GlobalConfig::configuredRepos() returns a normalized list of slugs and paths"
    - "RunCommand is refactored with a private runRepo() method that takes a slug and path"
  artifacts:
    - path: "app/Config/GlobalConfig.php"
      provides: "Configured repositories list"
    - path: "app/Commands/RunCommand.php"
      provides: "Single-repo execution extraction"
  key_links:
    - from: "app/Commands/RunCommand.php"
      to: "app/Config/GlobalConfig.php"
      via: "configuredRepos() call"
---

<objective>
Update GlobalConfig to support the "repos" list and refactor RunCommand to extract the single-repo run logic into a reusable method.

Purpose: Prepare the codebase for sequential multi-repo execution by making the core run logic repo-agnostic.
Output: Refactored GlobalConfig and RunCommand.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/06-multi-repo-runner/06-CONTEXT.md
@app/Config/GlobalConfig.php
@app/Commands/RunCommand.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GlobalConfig with repos support</name>
  <files>app/Config/GlobalConfig.php</files>
  <action>
    - Update `ensureExists()` to include a commented-out `repos` section in the default template (slugs and objects with paths per D-01, D-02, D-07).
    - Implement `configuredRepos(): array` to normalize the `repos` list into a collection of objects with `slug` and `path`.
    - Normalization logic:
        - If string: slug = string, path = current working directory (if slug matches current repo) or throw/null.
        - If array: slug = ['slug'], path = ['path'] ?? current working directory.
    - Ensure `repos()` helper remains for raw access.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Unit/GlobalConfigTest.php</automated>
  </verify>
  <done>GlobalConfig provides a normalized list of configured repositories with slugs and paths.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor RunCommand to extract runRepo()</name>
  <files>app/Commands/RunCommand.php</files>
  <action>
    - Extract the core orchestration logic from `handle()` into a private `runRepo(string $repo, string $path, GlobalConfig $globalConfig, ProgressReporter $progress)` method.
    - Ensure `runRepo` returns a `RunResult` (from `app/Data/RunResult.php`).
    - The `runRepo` method should handle:
        - `RepoConfig` instantiation at the given `$path`.
        - `RunOrchestratorService` setup and execution.
        - Usage rendering (or return usage data for later aggregation).
    - Maintain existing single-repo behavior in `handle()` for now by calling `runRepo` with the resolved slug and `getcwd()`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/RunCommandTest.php</automated>
  </verify>
  <done>RunCommand logic is extracted into a reusable runRepo() method without breaking existing functionality.</done>
</task>

</tasks>

<verification>
Check that the unit tests for GlobalConfig pass with various `repos` structures.
Check that `copland run` still works for a single repository (current directory).
</verification>

<success_criteria>
1. `app/Config/GlobalConfig.php` has `configuredRepos()` returning normalized data.
2. `app/Commands/RunCommand.php` has a `runRepo()` method containing the core orchestration logic.
3. Existing single-repo runs continue to function correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-repo-runner/06-01-SUMMARY.md`
</output>
