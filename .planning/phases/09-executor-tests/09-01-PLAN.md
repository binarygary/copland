---
phase: 09-executor-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/ClaudeExecutorService.php
  - tests/Unit/ClaudeExecutorServiceTest.php
autonomous: true
requirements: [TEST-01]
user_setup: []
must_haves:
  truths:
    - "`ClaudeExecutorService` dispatches tool_use blocks through the real executor tool flow."
    - "`ClaudeExecutorService` returns a failed execution result when no-progress thrashing trips the abort threshold."
    - "`ClaudeExecutorService` captures blocked write policy violations as failed tool results instead of crashing the loop."
    - "Executor tests run offline with scripted multi-round fake responses."
  artifacts:
    - path: "tests/Unit/ClaudeExecutorServiceTest.php"
      provides: "service-level executor coverage for tool dispatch, thrashing abort, and blocked write handling"
    - path: "app/Services/ClaudeExecutorService.php"
      provides: "minimal test seam only if needed to keep executor tests deterministic"
  key_links:
    - from: "tests/Unit/ClaudeExecutorServiceTest.php"
      to: "app/Services/ClaudeExecutorService.php"
      via: "scripted fake Anthropic responses and temp-workspace tool execution"
---

<objective>
Add service-level automated coverage for the executor loop.

Purpose: Phase 9 closes the largest remaining service-level testing gap by proving `ClaudeExecutorService` handles multi-round tool execution, abort conditions, and write-policy failures correctly without real API calls.
Output: A deterministic executor test suite, plus any minimal test seam required to script executor responses cleanly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-executor-tests/09-CONTEXT.md
@app/Services/ClaudeExecutorService.php
@app/Support/ExecutorRunState.php
@app/Support/ExecutorPolicy.php
@tests/Unit/ExecutorRunStateTest.php
@tests/Unit/ExecutorPolicyTest.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add any minimal executor seam required for deterministic service tests</name>
  <files>app/Services/ClaudeExecutorService.php</files>
  <read_first>app/Services/ClaudeExecutorService.php, .planning/phases/09-executor-tests/09-CONTEXT.md, tests/Unit/ExecutorRunStateTest.php, tests/Unit/ExecutorPolicyTest.php</read_first>
  <action>Inspect `ClaudeExecutorService` and only add a narrow seam if the current implementation makes service-level testing impractical. Favor preserving the public `execute()` and `executeWithRepoProfile()` APIs. Acceptable seams include a prompt-source override, command-runner seam, or similar narrow collaborator injection that keeps production behavior unchanged while allowing offline, deterministic tests. Do not refactor the executor architecture beyond what the tests need.</action>
  <verify>php -l app/Services/ClaudeExecutorService.php</verify>
  <acceptance_criteria>
    - app/Services/ClaudeExecutorService.php remains production-compatible
    - any new seam is minimal and directly justified by test determinism
    - executor still routes tool calls through its existing dispatch flow
  </acceptance_criteria>
  <done>The executor remains stable in production and testable offline.</done>
</task>

<task type="auto">
  <name>Task 2: Add service-level executor tests for dispatch, thrashing, and blocked writes</name>
  <files>tests/Unit/ClaudeExecutorServiceTest.php</files>
  <read_first>app/Services/ClaudeExecutorService.php, app/Support/ExecutorRunState.php, app/Support/ExecutorPolicy.php, tests/Unit/AnthropicApiClientTest.php, .planning/phases/09-executor-tests/09-CONTEXT.md</read_first>
  <action>Create `tests/Unit/ClaudeExecutorServiceTest.php` with lightweight fake response helpers and an offline fake `AnthropicApiClient` sequence. Add at least these cases: one successful tool-dispatch path where a scripted `tool_use` response triggers the expected executor tool handler and the following round ends successfully; one no-progress multi-round sequence that causes the executor to abort for thrashing and returns a failed `ExecutionResult`; and one blocked `write_file` attempt that is caught as a policy failure, recorded in the tool call log as an error, and returned in the execution result without throwing out of the service. Use a temp workspace where real file operations are exercised, and keep the whole suite free of real API/network calls.</action>
  <verify>./vendor/bin/pest tests/Unit/ClaudeExecutorServiceTest.php</verify>
  <acceptance_criteria>
    - tests/Unit/ClaudeExecutorServiceTest.php covers a successful tool dispatch path
    - tests/Unit/ClaudeExecutorServiceTest.php covers no-progress thrashing abort
    - tests/Unit/ClaudeExecutorServiceTest.php covers blocked-write policy failure handling
    - tests/Unit/ClaudeExecutorServiceTest.php uses scripted multi-round fake responses
    - tests/Unit/ClaudeExecutorServiceTest.php performs no real API calls
  </acceptance_criteria>
  <done>Executor behavior is regression-tested at the service boundary.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Services/ClaudeExecutorService.php
- [ ] ./vendor/bin/pest tests/Unit/ClaudeExecutorServiceTest.php
</verification>

<success_criteria>
- All tasks completed
- `ClaudeExecutorService` has service-level coverage for the roadmap-required behaviors
- Tests remain offline and deterministic
- Any executor seam added is minimal and production-safe
</success_criteria>

<output>
After completion, create `.planning/phases/09-executor-tests/09-01-SUMMARY.md`
</output>
