---
phase: 22-end-to-end-smoke-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - README.md
  - console-godot/README.md
  - console-godot/TODO.md
autonomous: true
requirements:
  - CONS-02
  - CONS-03
must_haves:
  truths:
    - "Root README.md has a new console section (after Commands, before Workflow) covering: Godot 4.2+ install, `php ./copland console` launch, ASCII pane layout, three-pane explanation, keyboard table, and a `Where data lives` subsection"
    - "Root README.md has the three D-06 incidental stale-bit fixes: `setup` → `automate` at the two locations, `:190` absolute path → relative `docs/overnight-setup.md`, `:158` status-note refresh mentioning `copland console`"
    - "console-godot/README.md is rewritten in place to document the full Phase-21 path contract, slug normalization, 8-state vocabulary, `copland console` launch, and divergences (incl. removed `merged` state and new `outcome.md`)"
    - "console-godot/README.md preserves the Visual direction paragraph and Non-goals section verbatim"
    - "console-godot/TODO.md keeps all three deferred items with refreshed wording, explicitly re-tagged as v2.1"
    - "Both READMEs document the canonical-purpose split between `~/.copland/tasks/` (live console state) and `~/.copland/logs/runs.jsonl` (append-only audit trail)"
    - "Doc paths and values quote concrete artifacts observed by Plan 22-01's smoke (real slug `binarygary__copland`, run-id shape from Phase 21 D-01)"
  artifacts:
    - path: "README.md"
      provides: "Root project README with full inline console walkthrough + D-06 stale-bit fixes"
      contains: "copland console"
    - path: "console-godot/README.md"
      provides: "Rewritten prototype README matching shipped Phase 19-21 reality"
      contains: "runs/<run-id>/outcome.md"
    - path: "console-godot/TODO.md"
      provides: "Lightly-refreshed deferred-items list re-tagged as v2.1"
      contains: "v2.1"
  key_links:
    - from: "README.md (new console section)"
      to: "console-godot/README.md"
      via: "cross-reference for deeper detail"
      pattern: "console-godot/README.md"
    - from: "Both READMEs (Where data lives / path contract)"
      to: "Phase 20+21 on-disk schema (`~/.copland/tasks/<repo>/<id>/{task.md, status.md, runs/<run-id>/{status.md, outcome.md}}`)"
      via: "D-09 canonical-purpose split"
      pattern: "~/.copland/tasks"
---

<objective>
Bring both READMEs in line with what shipped in Phases 19-21, document the canonical-purpose split between `~/.copland/tasks/` and `~/.copland/logs/runs.jsonl`, and freshen `console-godot/TODO.md`. All doc edits reference concrete paths/values observed on disk during Plan 22-01's smoke.

Purpose: Satisfies CONS-02 (root README documents Godot install, `copland console`, three panes) and CONS-03 (`console-godot/README.md` matches shipped, divergences from original prototype design are called out). Closes the v2.0 milestone.

Output: Three modified markdown files. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md
@.planning/phases/22-end-to-end-smoke-documentation/22-01-SUMMARY.md
@.planning/phases/20-task-status-writer/20-CONTEXT.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md
@README.md
@console-godot/README.md
@console-godot/TODO.md
@console-godot/scripts/TaskLoader.gd
@docs/overnight-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `console-godot/README.md` in place to match shipped (per D-08b, D-09)</name>
  <files>console-godot/README.md</files>
  <read_first>
    - console-godot/README.md (the current restored-verbatim prototype text — full file)
    - .planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md (D-08b — required content list; D-09 — canonical-purpose split; D-07 — `merged` removed; "Existing Code Insights" — Visual direction paragraph and Non-goals section preserved verbatim, ASCII layout + keyboard table stay)
    - .planning/phases/22-end-to-end-smoke-documentation/22-01-SUMMARY.md (the captured observed values from the smoke — real `&lt;task_id&gt;`, real `&lt;run-id&gt;`, outcome.md frontmatter keys actually observed)
    - .planning/phases/20-task-status-writer/20-CONTEXT.md (D-02 8-state vocabulary; D-05 GitHub `owner/repo` → `owner__repo` slug rule; D-06 Asana → registered repo's basename)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md (D-01 run-id ISO-with-dashes shape; D-03 per-run dir layout; D-05 outcome.md 9-key frontmatter list)
    - console-godot/scripts/TaskLoader.gd (post-22-01 STATES, frontmatter parser contract at lines 218-256)
  </read_first>
  <action>
    Rewrite `console-godot/README.md` in place (full overwrite, not append) with the following sections in this order. Use level-2 headings (`##`) except the title (`#`).

    1. Title and intro paragraph (one paragraph): keep the existing `# Copland Console — Godot Prototype` heading and the existing "Visual control plane for the Copland overnight agent. **Read-only** at this stage; the CLI remains the source of truth for the task lifecycle." intro verbatim.

    2. `## Run`: replace the current Godot-project-manager-only steps with two subsections. First subsection "From the CLI (recommended)": single bullet `php ./copland console` from the project root, with one sentence noting it preflights Godot and `console-godot/` then shells out via macOS `open -a Godot` (Phase 19 D-04/D-05). Second subsection "From Godot's project manager": preserve the existing 3 steps (Install Godot 4.2+ → Import → F5) since per D-08b "the prototype README's path still works but `copland console` is now the documented happy path".

    3. `## Path contract`: a fenced tree block showing the full Phase-21 layout:
       ```
       ~/.copland/tasks/&lt;repo-slug-safe&gt;/&lt;task_id&gt;/
       ├── task.md                 # frontmatter: id, title, repo_path, repo_slug, created_at  + body
       ├── status.md               # frontmatter: state, updated_at  + transitions table
       └── runs/
           └── &lt;run-id&gt;/           # POSIX-safe ISO timestamp (colons → dashes), e.g. 2026-05-27T14-48-59-613Z
               ├── status.md       # frontmatter: state, updated_at
               └── outcome.md      # frontmatter: 9 keys per Phase 21 D-05
       ```
       Below the tree, one sentence noting that the frontmatter parser at `scripts/TaskLoader.gd:218-256` only handles top-level scalar pairs with single/double-quote stripping — writer output must stay inside those limits.

    4. `## Slug normalization`: two bullets. First: GitHub `owner/repo` becomes `owner__repo` on disk (Phase 20 D-05). Second: Asana → the registered repo's path basename (Phase 20 D-06). Example sentence: "`binarygary/copland` becomes `binarygary__copland/` under `~/.copland/tasks/`."

    5. `## States`: a sentence "The writer emits exactly 8 states; `TaskLoader.gd` STATES matches them exactly:" followed by an inline code listing of the 8 in order: `new`, `selected`, `planning`, `planned`, `executing`, `verifying`, `pr_open`, `blocked`. One follow-up sentence: "A `merged` state was originally in STATES but never written by the orchestrator — it was removed in Phase 22 per D-07. PR-merge polling that would write `merged` is deferred to v2.1."

    6. `## Layout`: copy verbatim the existing ASCII pane diagram from the current `console-godot/README.md:14-24` (the `┌─ COPLAND ───...` block). No edits.

    7. `## Keyboard`: copy verbatim the existing keyboard table from the current `console-godot/README.md:28-34`. No edits.

    8. `## Data — real vs. sample`: two bullets. First "**Real**: scans `~/.copland/tasks/&lt;repo-slug-safe&gt;/&lt;task_id&gt;/{task.md, status.md, runs/&lt;run-id&gt;/{status.md, outcome.md}}` and populates the manifest from disk." Second "**Sample**: `scripts/TaskLoader.gd::sample_tasks()` provides hardcoded fallback tasks when `~/.copland/tasks/` is empty or missing — useful for iterating on visuals without running the CLI."

    9. `## Where data lives — `tasks/` vs `runs.jsonl``: prose paragraph documenting the D-09 canonical-purpose split. Cover all four points from D-09 verbatim in intent: `~/.copland/tasks/` is live console state (markdown + YAML frontmatter, mutates per lifecycle transition, source of truth for the Godot console); `~/.copland/logs/runs.jsonl` is append-only audit trail (one JSON record per `copland run`, never modified after append, not consumed by the console). End with the D-09 rule-of-thumb sentence: "What's running right now? → `tasks/`. What happened over the last 30 nights? → `runs.jsonl`."

    10. `## Divergences from the original prototype design`: bullet list of exactly three items:
        - `merged` state was originally listed in `TaskLoader.gd` STATES but the writer never emitted it; removed in Phase 22.
        - `runs/&lt;run-id&gt;/outcome.md` is a Phase 21 addition the original prototype didn't anticipate; its 9 frontmatter keys are: list the actual keys observed in the Plan 22-01 smoke (read from `22-01-SUMMARY.md`).
        - The original "Real / Sample" path string was `~/.copland/tasks/&lt;repo&gt;/&lt;id&gt;/{task.md, status.md}`; the shipped layout is the full tree shown in `## Path contract` above.

    11. `## Non-goals (this prototype)`: copy verbatim from current README (the three bullets — no editing/task-creation, no shell-out/AI/GitHub, no animations — plus the closing sentence "These belong in the CLI; the console is purely a visual layer.").

    12. `## Visual direction`: copy verbatim from current README — the 1930s machine-age / Art Deco paragraph. Per D-08b this is owner-confirmed direction and must be preserved verbatim.

    Do NOT introduce a screenshot, do NOT add a fixture/sample-data reference (D-04), do NOT add a "future work" / v2.1 list (that belongs in `TODO.md`).
  </action>
  <verify>
    <automated>grep -q "php ./copland console" console-godot/README.md &amp;&amp; grep -q "owner__repo" console-godot/README.md &amp;&amp; grep -q "runs/&lt;run-id&gt;/outcome.md" console-godot/README.md &amp;&amp; grep -q "1930s machine-age" console-godot/README.md &amp;&amp; ! grep -q '"merged"' console-godot/README.md &amp;&amp; grep -q "What's running right now" console-godot/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains a `## Path contract` section with a fenced tree showing `task.md`, `status.md`, `runs/<run-id>/status.md`, and `runs/<run-id>/outcome.md`.
    - File contains the literal string `owner__repo` (slug normalization rule from Phase 20 D-05).
    - File lists the 8 writer states (`new, selected, planning, planned, executing, verifying, pr_open, blocked`) and explicitly notes `merged` was removed.
    - File contains the verbatim 1930s machine-age / Art Deco paragraph from the previous version (owner-confirmed, preserved per D-08b).
    - File contains the verbatim Non-goals section from the previous version.
    - File contains the verbatim ASCII pane diagram and verbatim keyboard table from the previous version.
    - File contains a `## Where data lives` section covering D-09's canonical-purpose split, including the "What's running right now?" rule-of-thumb sentence.
    - File contains a `## Divergences from the original prototype design` section with exactly 3 bullets (the `merged` removal, the new `outcome.md` with its 9 frontmatter keys quoted from `22-01-SUMMARY.md`, and the path-string update).
    - File does NOT contain the substring `"merged"` as a STATES entry (the only mentions of `merged` are in the divergences explanation).
    - `php ./copland console` launch instruction appears as the first/primary run path; Godot-project-manager path is preserved as the secondary path.
  </acceptance_criteria>
  <done>console-godot/README.md describes what actually shipped in Phases 19-21, calls out divergences from the original prototype explicitly, and documents the D-09 canonical-purpose split — CONS-03 fully covered.</done>
</task>

<task type="auto">
  <name>Task 2: Light staleness pass on `console-godot/TODO.md` (per D-08)</name>
  <files>console-godot/TODO.md</files>
  <read_first>
    - console-godot/TODO.md (full current file — the three sections: Run drill-in selection, Live-tail of an executing run, UI scale on Retina)
    - .planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md (D-08 — light update, keep all three deferred items, freshen wording, re-tag as v2.1, no items added/removed; specifics — Phase 21 ships runs so the "your `runs/` directories are empty today" claim is now false)
    - .planning/REQUIREMENTS.md (§"Future Requirements (Deferred from `console-godot/TODO.md`)" — the three items already mapped to v2.1)
  </read_first>
  <action>
    Edit `console-godot/TODO.md` in place. Preserve the three existing top-level sections in their existing order: `## Run drill-in selection`, `## Live-tail of an executing run`, `## UI scale on Retina`. Apply these specific changes — no others:

    1. Top of file: replace the existing `# Copland Console — Deferred` heading with `# Copland Console — Deferred to v2.1` so the v2.1 tag is visible at the top.

    2. `## Run drill-in selection` body: rewrite the second paragraph. The current text reads "Currently the drill-in renders runs as a static list. Worth wiring once tasks have actually been run — your `runs/` directories are empty today." Replace with: "Currently the drill-in renders runs as a static list. As of Phase 21, real runs do materialize per-run subdirectories under `~/.copland/tasks/&lt;repo&gt;/&lt;id&gt;/runs/&lt;run-id&gt;/` with `status.md` and `outcome.md` — wiring the keyboard interaction over that data is the v2.1 deliverable." Keep the first paragraph ("In the task drill-in view, allow ↑/↓ to select among run rows when runs exist, and `ENTER` to open a deeper view for that run (run id, status, path, prompts, tool calls if/when those are emitted).") verbatim.

    3. `## Live-tail of an executing run` body: keep the first paragraph verbatim. In the second paragraph, the existing text already references "NDJSON in `~/.copland/tasks/&lt;repo&gt;/&lt;task&gt;/runs/&lt;run-id&gt;/events.log`" — leave that unchanged. Append one sentence to the third paragraph: "Target milestone: v2.1." Keep the rest verbatim.

    4. `## UI scale on Retina` body: keep verbatim. Append one sentence at the end: "Informational only; not a v2.0 gap, revisit if/when pixel-perfect rendering becomes a goal."

    Do NOT add new sections. Do NOT remove any of the three sections. Do NOT renumber, restructure, or reorder. Do NOT introduce a checklist or status emoji. The whole edit is ~10-15 lines of changed text per D-08.
  </action>
  <verify>
    <automated>grep -q "Deferred to v2.1" console-godot/TODO.md &amp;&amp; grep -q "Phase 21" console-godot/TODO.md &amp;&amp; grep -q "Target milestone: v2.1" console-godot/TODO.md &amp;&amp; grep -c "^## " console-godot/TODO.md | grep -q '^3$' &amp;&amp; ! grep -q "your \`runs/\` directories are empty today" console-godot/TODO.md</automated>
  </verify>
  <acceptance_criteria>
    - File still has exactly three level-2 sections in this order: `## Run drill-in selection`, `## Live-tail of an executing run`, `## UI scale on Retina`.
    - Top-of-file H1 is now `# Copland Console — Deferred to v2.1`.
    - The literal string `your \`runs/\` directories are empty today` no longer appears anywhere in the file.
    - The drill-in section explicitly references "Phase 21" and the per-run dir layout under `runs/<run-id>/` with `status.md` and `outcome.md`.
    - The live-tail section contains the literal sentence `Target milestone: v2.1.`
    - The Retina section contains the literal sentence beginning `Informational only; not a v2.0 gap`.
    - `git diff --stat console-godot/TODO.md` shows roughly 5-15 lines changed (a "light update" per D-08, not a rewrite).
  </acceptance_criteria>
  <done>TODO.md no longer claims `runs/` is empty (Phase 21 disproves it), keeps the three deferred items intact, and is explicitly tagged as v2.1 — operator reading it understands these are out of v2.0 scope.</done>
</task>

<task type="auto">
  <name>Task 3: Root README — add full console walkthrough + D-06 incidental stale-bit fixes</name>
  <files>README.md</files>
  <read_first>
    - README.md (full current file — note exact wording at lines 142, 158, 167, 190 for the D-06 fixes; note the existing Commands section ends at line ~159 with the `status` paragraph and Workflow begins at line 160)
    - console-godot/README.md (the version produced by Task 1 above — source for the ASCII layout, keyboard table, and pane explanations referenced inline)
    - .planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md (D-05 — required content for the new section; D-06 — three stale-bit fixes, scope-guarded; D-09 — canonical-purpose split for "Where data lives"; specifics — placement is after Commands, before Workflow)
    - .planning/phases/22-end-to-end-smoke-documentation/22-01-SUMMARY.md (the captured observed values — real run-id shape and any concrete strings to reference)
    - docs/overnight-setup.md (verify file exists at the relative path `docs/overnight-setup.md` so the D-06 fix to line 190 is valid)
  </read_first>
  <action>
    Apply four discrete edits to `README.md`. Each edit is bounded; do NOT take the opportunity to rewrite anything outside these four scopes (per D-06 scope guard — Asana / multi-provider sections stay untouched).

    EDIT 1 (D-06 fix A — `setup` → `automate` rename at line 140-ish): The current text at `README.md:140` reads `### \`php ./copland setup --hour=2 --minute=0\``. Replace `setup` with `automate` so the heading reads `### \`php ./copland automate --hour=2 --minute=0\``. Do NOT touch the body of that section (it still accurately describes installing a LaunchAgent).

    EDIT 2 (D-06 fix B — second `setup` → `automate` reference at line ~167): The current text in the Workflow section at `README.md:167` reads "Run `php ./copland run` manually, or install nightly automation with `php ./copland setup`." Replace `php ./copland setup` with `php ./copland automate`.

    EDIT 3 (D-06 fix C — broken absolute path at line 190): The current text at `README.md:190` reads `For the full nightly automation flow, see [docs/overnight-setup.md](/Users/binarygary/projects/binarygary/copland/docs/overnight-setup.md).` Replace the link target with the relative path `docs/overnight-setup.md`, producing: `For the full nightly automation flow, see [docs/overnight-setup.md](docs/overnight-setup.md).`

    EDIT 4 (D-06 fix D — refresh `status`-not-implemented paragraph at line ~156-158): The current text reads `### \`php ./copland status\`` followed by `\`status\` exists as a command name, but it is not implemented yet. The stable morning-review path today is \`~/.copland/logs/runs.jsonl\`.` Replace ONLY the body paragraph (keep the heading) with: `\`status\` exists as a command name, but it is not implemented yet. For at-a-glance "what's running right now" use \`php ./copland console\` (the Godot visual surface, see "Console" section below). For "what happened over the last 30 nights" the canonical audit trail remains \`~/.copland/logs/runs.jsonl\`.`

    EDIT 5 (D-05 — the new Console section): Insert a NEW level-2 section titled `## Console` immediately after the Commands section ends (after the `php ./copland status` block from Edit 4) and immediately before the existing `## Workflow` heading. Body in this order:

    a. One-sentence intro: "Copland ships with a read-only Godot visual console for at-a-glance state of the overnight agent."

    b. Subsection `### Prerequisites`: single bullet "Install Godot 4.2 or newer from https://godotengine.org/."

    c. Subsection `### Launch`: fenced bash block containing `php ./copland console`. Follow with one sentence: "This preflights Godot and the `console-godot/` project then shells out via macOS `open -a Godot` (macOS only)."

    d. Subsection `### Layout`: copy verbatim the ASCII pane diagram from `console-godot/README.md` (the `┌─ COPLAND ───...` block — same block used in Task 1).

    e. Subsection `### Panes`: three bullets, one sentence each — Workflow States ("counts per state across all tasks under `~/.copland/tasks/`"), Task Manifest ("title + state badge for each task, scoped by the selected state filter"), Dossier ("drill-in detail for the selected task — body markdown plus the run history from `runs/`").

    f. Subsection `### Keyboard`: copy verbatim the keyboard table from `console-godot/README.md` (the `↑/↓` / `TAB` / `ENTER` / `ESC` / `Q` rows).

    g. Subsection `### Where data lives`: prose paragraph documenting the D-09 canonical-purpose split. Cover the same four points as Task 1 section 9 but tuned for the root-README audience: `~/.copland/tasks/<repo>/<id>/` is live console state (mutates per lifecycle transition; source of truth for the Godot console; ephemeral mid-run; terminal state pins). `~/.copland/logs/runs.jsonl` is append-only audit trail (one JSON record per `copland run`; never modified after append; not consumed by the console; canonical for cost analytics and retrospective grep). End with the D-09 rule-of-thumb sentence: "What's running right now? → `tasks/`. What happened over the last 30 nights? → `runs.jsonl`. They overlap by design — the same run produces records in both — but each surface is the wrong tool for the other question." Finish with one cross-reference sentence: "Deeper detail in [`console-godot/README.md`](console-godot/README.md)."

    Do NOT touch the Asana section, the multi-provider section, the Prerequisites / Installation / Global Configuration / Repo Configuration / Safety Model / Morning Review / Scope Notes sections beyond the four bounded D-06 edits described above.
  </action>
  <verify>
    <automated>! grep -E "php \./copland setup" README.md &amp;&amp; ! grep -F "/Users/binarygary/projects/binarygary/copland/docs/overnight-setup.md" README.md &amp;&amp; grep -q "^## Console" README.md &amp;&amp; grep -q "^### Prerequisites" README.md &amp;&amp; grep -q "^### Launch" README.md &amp;&amp; grep -q "^### Layout" README.md &amp;&amp; grep -q "^### Panes" README.md &amp;&amp; grep -q "^### Keyboard" README.md &amp;&amp; grep -q "^### Where data lives" README.md &amp;&amp; grep -q "php ./copland console" README.md &amp;&amp; grep -q "What's running right now" README.md &amp;&amp; grep -q "What happened over the last 30 nights" README.md &amp;&amp; grep -q "copland automate" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "php \\./copland setup" README.md` returns NO matches (Edit 1 + Edit 2 both applied).
    - `grep -F "/Users/binarygary/projects/binarygary/copland/docs/overnight-setup.md" README.md` returns NO matches (Edit 3 applied — broken absolute path is gone).
    - The relative link `[docs/overnight-setup.md](docs/overnight-setup.md)` is present.
    - The `### \`php ./copland status\`` body explicitly mentions `php ./copland console` and `~/.copland/logs/runs.jsonl` (Edit 4 applied).
    - A new `## Console` section exists in the file, located AFTER the line containing `### \`php ./copland status\`` and BEFORE the line containing `## Workflow`.
    - The `## Console` section contains all six subsections in order: `### Prerequisites`, `### Launch`, `### Layout`, `### Panes`, `### Keyboard`, `### Where data lives`.
    - The Launch subsection contains the literal string `php ./copland console` inside a fenced code block.
    - The Layout subsection contains the verbatim ASCII pane diagram from `console-godot/README.md` (specifically the line containing `┌─ COPLAND`).
    - The Keyboard subsection contains a table with rows for `↑` / `↓`, `TAB`, `ENTER`, `ESC`, `Q`.
    - The Where-data-lives subsection contains the literal strings `What's running right now?` and `What happened over the last 30 nights?`.
    - The Asana section, multi-provider section, and other untouched sections are byte-identical to their pre-edit content (verifiable via `git diff` — only the four bounded scopes show changes).
  </acceptance_criteria>
  <done>Root README documents Godot install, `php ./copland console` launch, the three panes, the keyboard, and the canonical-purpose split — CONS-02 fully covered. The three D-06 stale bits (`setup` rename ×2, broken absolute path, status-note refresh) are fixed. No out-of-scope rewrites.</done>
</task>

</tasks>

<verification>
- `grep "php ./copland setup" README.md` returns no matches (rename applied at both locations).
- `grep "/Users/binarygary/projects" README.md` returns no matches (broken absolute path replaced).
- `README.md` contains a `## Console` section with all six subsections (`Prerequisites`, `Launch`, `Layout`, `Panes`, `Keyboard`, `Where data lives`).
- `console-godot/README.md` contains `## Path contract`, `## Slug normalization`, `## States`, `## Where data lives`, and `## Divergences from the original prototype design` sections; preserves verbatim ASCII layout, keyboard table, Visual direction paragraph, and Non-goals section.
- `console-godot/TODO.md` keeps three top-level sections, is re-tagged v2.1, no longer claims `runs/` directories are empty.
- Both READMEs document the D-09 canonical-purpose split with the rule-of-thumb sentence.
- All three modified files reference paths that match the on-disk schema observed in Plan 22-01's smoke (slug `binarygary__copland`, run-id POSIX-safe shape, 9-key outcome.md frontmatter).
</verification>

<success_criteria>
- CONS-02 satisfied: root README documents Godot 4.2+ install, `copland console` launch, and the three console panes inline.
- CONS-03 satisfied: `console-godot/README.md` matches what shipped (paths, "real" data definition, divergences from original prototype called out explicitly).
- Roadmap success criterion 4 satisfied: relationship between `~/.copland/tasks/` (live state) and `~/.copland/logs/runs.jsonl` (audit trail) is documented in both READMEs.
- v2.0 Godot Console milestone is doc-complete; phase ready to close.
</success_criteria>

<output>
Create `.planning/phases/22-end-to-end-smoke-documentation/22-02-SUMMARY.md` when done. Include diff-stat counts for the three modified files and confirm which D-06 stale-bit fixes landed.
</output>
