---
phase: 22-end-to-end-smoke-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - console-godot/scripts/TaskLoader.gd
autonomous: false
requirements:
  - CONS-01
must_haves:
  truths:
    - "console-godot/scripts/TaskLoader.gd STATES no longer contains 'merged'"
    - "A real `php ./copland run` against binarygary/copland completes without exception"
    - "~/.copland/tasks/binarygary__copland/<task_id>/{task.md, status.md, runs/<run-id>/{status.md, outcome.md}} exists on disk after the run"
    - "`copland console` launches without parser exceptions"
    - "All 10 items in the D-02 operator checklist are signed off"
  artifacts:
    - path: "console-godot/scripts/TaskLoader.gd"
      provides: "Vocabulary-aligned STATES constant (8 entries, no 'merged')"
      contains: "const STATES"
  key_links:
    - from: "console-godot/scripts/TaskLoader.gd STATES"
      to: "App\\Services\\TaskDirectoryWriterService emitted states"
      via: "Phase 20 D-02 8-state vocabulary"
      pattern: "new, selected, planning, planned, executing, verifying, pr_open, blocked"
---

<objective>
Eliminate the one remaining writer-vs-loader vocabulary divergence in the Godot prototype, then run a live, operator-driven end-to-end smoke against `binarygary/copland` and sign off against the 10-item D-02 operator checklist.

Purpose: Satisfies CONS-01 — proves the Phase 19-21 stack produces a `~/.copland/tasks/<repo>/<id>/...` tree that `TaskLoader.gd` renders cleanly without schema drift. Provides the on-disk evidence Plan 22-02 will document.

Output: One-line GDScript edit (autonomous) + a live overnight-agent run with operator sign-off (checkpoint:human-verify).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md
@.planning/phases/20-task-status-writer/20-CONTEXT.md
@.planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md
@console-godot/scripts/TaskLoader.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `merged` from TaskLoader.gd STATES (per D-07)</name>
  <files>console-godot/scripts/TaskLoader.gd</files>
  <read_first>
    - console-godot/scripts/TaskLoader.gd (full file — verify STATES block at lines 20-30 is unchanged from the read above; the surrounding code must not be touched)
    - .planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md (D-07 — the locked decision; D-02 last checklist item — "no state badge in the manifest contains a value outside the 8 written states")
    - .planning/phases/20-task-status-writer/20-CONTEXT.md (D-02 — the writer's 8-state vocabulary that STATES must now match exactly)
  </read_first>
  <action>
    Edit `console-godot/scripts/TaskLoader.gd` lines 20-30. Delete exactly the line containing `"merged",` (currently line 28). The resulting STATES constant must contain exactly these 8 entries in this order (matching the Phase 20 D-02 writer vocabulary): `new`, `selected`, `planning`, `planned`, `executing`, `verifying`, `pr_open`, `blocked`. Preserve the trailing comma style on the final entry (`"blocked",`) used throughout the file. Do NOT touch any other line in the file — no whitespace tweaks, no doc-comment edits, no sample_tasks() changes, no `_read_task()` changes. This is strictly a one-line deletion.
  </action>
  <verify>
    <automated>grep -c '"merged"' console-godot/scripts/TaskLoader.gd | grep -q '^0$' &amp;&amp; grep -A 12 "const STATES" console-godot/scripts/TaskLoader.gd | grep -E '"(new|selected|planning|planned|executing|verifying|pr_open|blocked)"' | wc -l | grep -q '^8$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"merged"' console-godot/scripts/TaskLoader.gd` returns no matches (0 occurrences total in the file).
    - The STATES constant block contains exactly 8 string entries — `"new"`, `"selected"`, `"planning"`, `"planned"`, `"executing"`, `"verifying"`, `"pr_open"`, `"blocked"` — in that order.
    - `git diff console-godot/scripts/TaskLoader.gd` shows exactly one removed line (`-    "merged",`) and zero added lines.
    - No other files in the working tree are modified by this task.
  </acceptance_criteria>
  <done>STATES is aligned with the Phase 20 D-02 writer vocabulary; the writer-vs-loader divergence flagged in 22-CONTEXT.md D-07 is gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live overnight-agent smoke + 10-item operator sign-off (per D-01, D-02, D-03)</name>
  <files>(no repo files modified — observation only; on-disk artifacts under `~/.copland/tasks/binarygary__copland/...` are written by the live run)</files>
  <read_first>
    - .planning/phases/22-end-to-end-smoke-documentation/22-CONTEXT.md (D-01, D-02 — the verbatim checklist; D-03 — dogfood Copland itself manually; D-04 — no snapshot/commit of the resulting tree)
    - .planning/phases/20-task-status-writer/20-CONTEXT.md (D-02 8-state vocabulary; D-05/D-06 slug normalization producing `binarygary__copland`)
    - .planning/phases/21-per-run-artifacts-test-coverage/21-CONTEXT.md (D-01 run-id format `YYYY-MM-DDTHH-MM-SS-...Z`; D-03 per-run dir layout; D-05 outcome.md 9-key frontmatter)
    - console-godot/scripts/TaskLoader.gd (post-Task-1 state — confirm `merged` is gone before launching the console)
  </read_first>
  <what-built>
    Task 1 produced the STATES alignment. This task is OPERATOR-DRIVEN observation, not implementation:
    1. The operator stages exactly one small `agent-ready` GitHub issue on `binarygary/copland`. The issue must NOT touch `console-godot/` or `app/Services/TaskDirectoryWriterService*` (per D-03 + code_context section of 22-CONTEXT.md) — pick a PHPStan tweak, doc typo, or one-line refactor.
    2. The operator runs `php ./copland run binarygary/copland` from the project root and lets it complete.
    3. The operator inspects `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/` on disk against the schema bullets below.
    4. The operator launches `php ./copland console` and visually attests against the pane-rendering bullets below.
    5. After sign-off, the live `~/.copland/tasks/` tree stays on disk; nothing is committed (D-04).
  </what-built>
  <how-to-verify>
    Walk through every item below. Tick each box only when literally observed. If any item fails, fix the underlying issue (or retry with a different issue if `blocked`) before resuming the phase — do NOT paper over with a forced state. This checklist is the verbatim D-02 acceptance contract for CONS-01.

    On-disk schema (run from project root after `php ./copland run` completes):
    - [ ] `php ./copland run binarygary/copland` completed without exception (PR opened OR a `blocked` payload written — `pr_open` is the target per D-03, retry with a different issue if `blocked`)
    - [ ] `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/task.md` exists with the 5 TaskLoader-required frontmatter keys (`id`, `title`, `repo_path`, `repo_slug`, `created_at`) plus a `body` section
    - [ ] `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/status.md` shows the terminal state (`pr_open` for a happy-path smoke; `blocked` only if retry exhausted) in frontmatter, AND the transitions table contains all states that fired during the run (at least 3 rows for a happy path)
    - [ ] `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/runs/&lt;run-id&gt;/status.md` exists with `state` + `updated_at` frontmatter; the run-id directory name matches the Phase 21 D-01 POSIX-safe shape (`YYYY-MM-DDTHH-MM-SS-...Z`, no colons)
    - [ ] `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/runs/&lt;run-id&gt;/outcome.md` exists with the 9 frontmatter keys from Phase 21 D-05

    Console rendering (launch from project root, separate terminal):
    - [ ] `php ./copland console` launches without errors — Godot project opens, no parser exceptions visible in Godot's stderr or output panel
    - [ ] Workflow States pane shows non-zero counts for at least one state
    - [ ] Task Manifest pane lists the newly-created task with its real title from `task.md` (NOT a sample-fallback title like "Wire footer status bar to live token usage")
    - [ ] Dossier pane, when the task is selected, shows the task body markdown AND at least one run row sourced from `runs/`
    - [ ] No state badge anywhere in the manifest contains a value outside the 8 written states (`new`, `selected`, `planning`, `planned`, `executing`, `verifying`, `pr_open`, `blocked`) — i.e., the Task-1 STATES alignment is visible end-to-end

    Capture during sign-off (paste into the executor's PR body / summary so 22-02 docs can reference concrete observed values):
    - The actual `&lt;task_id&gt;` produced
    - The actual `&lt;run-id&gt;` directory name (proves the Phase 21 D-01 format)
    - The terminal state recorded in `status.md` frontmatter (expected: `pr_open`)
    - The transitions row count in `status.md`
    - The 9 frontmatter keys observed in `outcome.md`
  </how-to-verify>
  <acceptance_criteria>
    - All 10 checklist items above are explicitly ticked by the operator.
    - The captured observed values (`&lt;task_id&gt;`, `&lt;run-id&gt;`, terminal state, transition count, outcome.md keys) are recorded in the plan's SUMMARY.md so Plan 22-02 can quote them.
    - No file under `console-godot/sample-data/` is created (D-04 — no snapshot).
    - Working tree at the end of this task contains ONLY the one-line edit from Task 1 — no new committed files, no edits to `~/.copland/tasks/` content tracked in git (it lives outside the repo by design).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the captured observed values, OR describe which checklist item(s) failed and what was retried.</resume-signal>
</task>

</tasks>

<verification>
- `grep '"merged"' console-godot/scripts/TaskLoader.gd` returns no matches.
- STATES contains exactly 8 entries matching the Phase 20 D-02 vocabulary.
- All 10 D-02 operator checklist items are signed off with captured observed values.
- No fixture directory under `console-godot/sample-data/` exists.
- `git diff --stat` against `main` shows exactly one file changed (`console-godot/scripts/TaskLoader.gd`) with one line removed and zero added.
</verification>

<success_criteria>
CONS-01 is satisfied: a real overnight-agent run produced a `~/.copland/tasks/binarygary__copland/&lt;task_id&gt;/` tree that `TaskLoader.gd` rendered without errors and without schema drift, with task titles, statuses, and run metadata visible in the console panes — observed by the operator, signed off, and recorded.
</success_criteria>

<output>
Create `.planning/phases/22-end-to-end-smoke-documentation/22-01-SUMMARY.md` when done. The SUMMARY must include the captured observed values from the operator sign-off (real `<task_id>`, real `<run-id>`, terminal state, transition count, outcome.md keys observed) so Plan 22-02's docs can cite concrete values from disk.
</output>
