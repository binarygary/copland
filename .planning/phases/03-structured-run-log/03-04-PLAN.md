---
phase: 03-structured-run-log
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - app/Services/RunOrchestratorService.php
  - app/Support/RunProgressSnapshot.php
autonomous: true
requirements: [OBS-01]
user_setup: []
must_haves:
  truths:
    - Thrown or interrupted runs still append a partial structured log record rather than leaving no local evidence.
    - Partial records reuse existing snapshot usage fields instead of inventing a separate incomplete-run cost path.
  artifacts:
    - app/Services/RunOrchestratorService.php
    - app/Support/RunProgressSnapshot.php
  key_links:
    - RunProgressSnapshot partial usage -> RunOrchestratorService finalization -> RunLogStore partial record
---

<objective>
Guarantee partial log persistence for crash and interruption paths.

Purpose: The roadmap explicitly requires that mid-run crashes still leave a readable local record by morning.
Output: Finalization logic that appends partial records on thrown/aborted runs using existing snapshot usage and step log data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-run-log/03-CONTEXT.md
@.planning/phases/03-structured-run-log/03-RESEARCH.md
@app/Services/RunOrchestratorService.php
@app/Support/RunProgressSnapshot.php
@app/Support/RunLogStore.php
@app/Commands/RunCommand.php
@.planning/phases/03-structured-run-log/03-03-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture enough partial-run state to log incomplete outcomes</name>
  <files>app/Support/RunProgressSnapshot.php, app/Services/RunOrchestratorService.php</files>
  <read_first>app/Support/RunProgressSnapshot.php, app/Services/RunOrchestratorService.php, app/Commands/RunCommand.php</read_first>
  <action>Extend partial-run state only as needed so an incomplete run can still log repo/issue context, available usage values, and a clear partial/crash marker. Prefer reusing `RunProgressSnapshot` for live usage and keeping partial metadata lightweight. Do not duplicate the full `RunResult` contract just for crash paths.</action>
  <verify>php -l app/Support/RunProgressSnapshot.php</verify>
  <acceptance_criteria>
    - partial-run state includes enough data to build a structured log record when no final `RunResult` exists
    - existing SIGINT/usage behavior remains compatible
  </acceptance_criteria>
  <done>Crash and interruption paths have enough structured state to be logged coherently.</done>
</task>

<task type="auto">
  <name>Task 2: Append partial log records from guaranteed finalization paths</name>
  <files>app/Services/RunOrchestratorService.php</files>
  <read_first>app/Services/RunOrchestratorService.php, app/Support/RunLogStore.php, app/Support/RunProgressSnapshot.php</read_first>
  <action>Refactor `RunOrchestratorService::run()` so the finalization path appends a structured partial record when execution throws or otherwise exits without a normal completed `RunResult`. Mark the payload clearly as partial/incomplete, include the step log captured so far, and persist any available selector/planner/executor usage from `RunProgressSnapshot`. Ensure cleanup still happens and logging is not skipped if cleanup itself warns.</action>
  <verify>php -l app/Services/RunOrchestratorService.php</verify>
  <acceptance_criteria>
    - exception or incomplete paths append one partial structured log record
    - partial records are distinguishable from normal failed runs
    - cleanup warnings do not suppress log persistence
  </acceptance_criteria>
  <done>Mid-run crashes and interruptions now leave a local record instead of silent absence.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Support/RunProgressSnapshot.php
- [ ] php -l app/Services/RunOrchestratorService.php
- [ ] tail -n 1 ~/.copland/logs/runs.jsonl | jq '.status, .partial'
</verification>

<success_criteria>
- All tasks completed
- Mid-run crashes and interruptions append partial records
- Partial records reuse existing snapshot usage data
- Finalization logging survives cleanup warnings and exception paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-structured-run-log/03-04-SUMMARY.md`
</output>
