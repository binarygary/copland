---
phase: 03-structured-run-log
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Services/RunOrchestratorService.php
  - app/Data/RunResult.php
autonomous: true
requirements: [OBS-01]
user_setup: []
must_haves:
  truths:
    - Success, skip, and normal failure outcomes append a structured run record to `~/.copland/logs/runs.jsonl`.
    - Persisted records include repo, issue metadata when known, status, timestamps, failure reason when present, and existing usage/cost fields.
  artifacts:
    - app/Services/RunOrchestratorService.php
    - app/Data/RunResult.php
  key_links:
    - RunOrchestratorService lifecycle -> RunResult metadata -> RunLogStore persisted record
---

<objective>
Persist structured log entries for normal orchestrator outcomes.

Purpose: Phase 3’s main user-visible capability is a local morning-review log for success, skip, and ordinary failure paths.
Output: Orchestrator-owned log assembly for normal outcomes, using `RunLogStore` and existing run metadata instead of ad hoc CLI strings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-run-log/03-CONTEXT.md
@.planning/phases/03-structured-run-log/03-RESEARCH.md
@app/Services/RunOrchestratorService.php
@app/Data/RunResult.php
@app/Support/RunLogStore.php
@app/Support/RunProgressSnapshot.php
@.planning/phases/03-structured-run-log/03-01-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the minimum structured metadata needed for persisted run records</name>
  <files>app/Data/RunResult.php</files>
  <read_first>app/Data/RunResult.php, app/Services/RunOrchestratorService.php, .planning/phases/03-structured-run-log/03-CONTEXT.md</read_first>
  <action>If the existing `RunResult` fields are insufficient for clean log persistence, add only the minimum extra structured metadata needed for Phase 3, such as explicit timestamps or record-completion markers. Prefer extending the existing data contract over building parallel anonymous arrays everywhere, but avoid over-modeling. Keep all existing constructor call sites coherent.</action>
  <verify>php -l app/Data/RunResult.php</verify>
  <acceptance_criteria>
    - `RunResult` cleanly supports the metadata needed for persisted run records
    - Constructor call sites remain internally consistent
  </acceptance_criteria>
  <done>The run result contract is sufficient to persist normal run outcomes structurally.</done>
</task>

<task type="auto">
  <name>Task 2: Append structured log entries for success, skip, and normal failure paths</name>
  <files>app/Services/RunOrchestratorService.php</files>
  <read_first>app/Services/RunOrchestratorService.php, app/Support/RunLogStore.php, app/Data/RunResult.php, app/Support/RunProgressSnapshot.php</read_first>
  <action>Update `RunOrchestratorService::run()` so normal exit paths append a structured record through `RunLogStore`. The persisted record must include repo, selected issue metadata when known, final status, timestamps, failure reason when present, PR metadata when present, the existing step log array, and selector/planner/executor/total usage information derived from existing usage fields. Keep the orchestrator as the single source of truth for these outcomes.</action>
  <verify>php -l app/Services/RunOrchestratorService.php</verify>
  <acceptance_criteria>
    - app/Services/RunOrchestratorService.php writes through `RunLogStore`
    - normal success/skip/fail branches all produce one structured log record
    - stored payload includes repo, status, timestamps, and usage/cost data
  </acceptance_criteria>
  <done>Normal orchestrator outcomes now leave a machine-readable morning-review log entry.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Data/RunResult.php
- [ ] php -l app/Services/RunOrchestratorService.php
- [ ] tail -n 1 ~/.copland/logs/runs.jsonl | jq '.repo, .status'
</verification>

<success_criteria>
- All tasks completed
- Success, skip, and normal failure runs append structured JSONL records
- Persisted records include timestamps and usage/cost details
- Logging is owned by orchestration rather than terminal presentation
</success_criteria>

<output>
After completion, create `.planning/phases/03-structured-run-log/03-03-SUMMARY.md`
</output>
