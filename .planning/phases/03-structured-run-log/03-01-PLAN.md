---
phase: 03-structured-run-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/RunLogStore.php
  - tests/Unit/RunLogStoreTest.php
autonomous: true
requirements: [OBS-01]
user_setup: []
must_haves:
  truths:
    - A dedicated helper can append one structured JSON object per line to `~/.copland/logs/runs.jsonl`.
    - Logged usage/cost data is serialized in a stable machine-readable shape rather than flattened terminal strings.
  artifacts:
    - app/Support/RunLogStore.php
    - tests/Unit/RunLogStoreTest.php
  key_links:
    - HomeDirectory::resolve -> RunLogStore log path -> runs.jsonl append behavior
---

<objective>
Create the append-only storage primitive for structured run logging.

Purpose: Phase 3 needs a reusable filesystem helper before orchestrator control flow can safely persist run records.
Output: A `RunLogStore` helper that appends JSONL records under `~/.copland/logs/runs.jsonl`, plus unit coverage for path creation and stored payload shape.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-run-log/03-CONTEXT.md
@.planning/phases/03-structured-run-log/03-RESEARCH.md
@app/Support/HomeDirectory.php
@app/Data/ModelUsage.php
@app/Support/AnthropicCostEstimator.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RunLogStore with HOME-based JSONL append behavior</name>
  <files>app/Support/RunLogStore.php</files>
  <read_first>app/Support/HomeDirectory.php, app/Data/ModelUsage.php, app/Support/AnthropicCostEstimator.php</read_first>
  <action>Create `App\Support\RunLogStore` that resolves `~/.copland/logs/runs.jsonl` via `HomeDirectory::resolve()`, creates the parent directory on demand, and appends exactly one JSON object per line. Keep the API narrow: accept a structured array payload and handle JSON encoding plus newline termination internally. Include nested serialization for selector/planner/executor/total usage blocks if those arrays are present, but do not depend on terminal-formatted strings.</action>
  <verify>php -l app/Support/RunLogStore.php</verify>
  <acceptance_criteria>
    - app/Support/RunLogStore.php resolves a path ending in `/.copland/logs/runs.jsonl`
    - app/Support/RunLogStore.php creates missing directories before writing
    - app/Support/RunLogStore.php appends one JSON object plus newline per call
  </acceptance_criteria>
  <done>Structured run logs have a dedicated append-only storage helper.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit coverage for JSONL append and payload shape</name>
  <files>tests/Unit/RunLogStoreTest.php</files>
  <read_first>app/Support/RunLogStore.php, tests/Unit/PlanArtifactStoreTest.php</read_first>
  <action>Add `tests/Unit/RunLogStoreTest.php` using a temporary HOME directory to verify that `RunLogStore` creates `~/.copland/logs`, appends newline-delimited JSON records, and preserves expected top-level fields like `repo`, `status`, and nested usage data. Reuse the existing temp-HOME testing style from `PlanArtifactStoreTest`.</action>
  <verify>./vendor/bin/pest tests/Unit/RunLogStoreTest.php</verify>
  <acceptance_criteria>
    - tests/Unit/RunLogStoreTest.php verifies the file path under `.copland/logs/runs.jsonl`
    - tests/Unit/RunLogStoreTest.php verifies appended JSONL output
    - tests/Unit/RunLogStoreTest.php verifies nested usage/cost fields are stored structurally
  </acceptance_criteria>
  <done>Run log persistence behavior is covered without depending on orchestrator integration yet.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Support/RunLogStore.php
- [ ] ./vendor/bin/pest tests/Unit/RunLogStoreTest.php
</verification>

<success_criteria>
- All tasks completed
- A dedicated helper appends JSONL records to `~/.copland/logs/runs.jsonl`
- Log directory creation is automatic
- Stored usage data remains machine-readable
</success_criteria>

<output>
After completion, create `.planning/phases/03-structured-run-log/03-01-SUMMARY.md`
</output>
