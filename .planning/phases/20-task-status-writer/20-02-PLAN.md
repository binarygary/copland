---
phase: 20-task-status-writer
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - app/Services/RunOrchestratorService.php
  - app/Commands/RunCommand.php
autonomous: true
requirements:
  - TASK-01
  - TASK-02
tags:
  - php
  - laravel-zero
  - orchestrator
  - lifecycle
  - composition-root

must_haves:
  truths:
    - "RunOrchestratorService constructor accepts an optional ?TaskDirectoryWriterService $taskWriter = null parameter (backward-compatible with existing tests)"
    - "On task selection (after $selectedIssue is non-null), the orchestrator calls writeNewTask then writeStatus('new') then writeStatus('selected') in order"
    - "writeStatus('planning') is called before $this->planner->planTask(...) executes"
    - "writeStatus('planned') is called only when plan validation passes"
    - "writeStatus('executing') is called before $this->executor->executeWithRepoProfile(...) executes"
    - "writeStatus('verifying') is called before $this->verifier->verify(...) executes"
    - "writeStatus('pr_open') is called after gh pr create returns successfully with a PR URL"
    - "writeBlockedIfNotTerminal is called from the finally block, guarded by $selectedIssue !== null and wrapped in its own try/catch so writer errors never mask the original exception"
    - "RunCommand::runRepo instantiates TaskDirectoryWriterService and passes it to RunOrchestratorService via the new named argument"
    - "The full Pest suite still passes — no existing test regresses"
  artifacts:
    - path: "app/Services/RunOrchestratorService.php"
      provides: "Constructor with new ?TaskDirectoryWriterService dependency + 8 writeStatus call sites + 1 writeBlockedIfNotTerminal call in finally"
      contains: "?TaskDirectoryWriterService"
    - path: "app/Commands/RunCommand.php"
      provides: "Composition root wires a new TaskDirectoryWriterService into the orchestrator"
      contains: "TaskDirectoryWriterService"
  key_links:
    - from: "app/Commands/RunCommand.php"
      to: "app/Services/RunOrchestratorService.php"
      via: "named-argument constructor call with taskWriter: new TaskDirectoryWriterService"
      pattern: "taskWriter:\\s*new TaskDirectoryWriterService"
    - from: "app/Services/RunOrchestratorService.php"
      to: "app/Services/TaskDirectoryWriterService.php"
      via: "9 call sites — 1 writeNewTask + 7 writeStatus + 1 writeBlockedIfNotTerminal"
      pattern: "taskWriter\\?->(writeNewTask|writeStatus|writeBlockedIfNotTerminal)"
---

<objective>
Wire `TaskDirectoryWriterService` (shipped by Plan 01) into the orchestrator's `run()` lifecycle and into the composition root so the writer fires on every real `copland run` invocation. Add one new optional constructor parameter to `RunOrchestratorService`, insert 9 writer call sites at the exact line ranges identified by RESEARCH Pattern 2, and append one named argument to the `RunOrchestratorService` instantiation in `RunCommand::handle()`.

Purpose: Plan 01's writer is silent on its own — only the orchestrator knows when each lifecycle transition fires. This plan connects the writer's three public methods to the 8 transitions defined in D-03 plus the `blocked` crash-recovery write defined in D-11. After this plan ships, a real overnight run materializes `~/.copland/tasks/<repo-dir>/<task-id>/{task.md, status.md}` exactly as `console-godot/scripts/TaskLoader.gd` expects to read them.

Output: Two modified files (`app/Services/RunOrchestratorService.php`, `app/Commands/RunCommand.php`). Net change ~12-15 lines across both files per RESEARCH § Pattern 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-task-status-writer/20-CONTEXT.md
@.planning/phases/20-task-status-writer/20-RESEARCH.md
@.planning/phases/20-task-status-writer/20-PATTERNS.md
@.planning/phases/20-task-status-writer/20-01-SUMMARY.md
@CLAUDE.md
@app/Services/RunOrchestratorService.php
@app/Services/TaskDirectoryWriterService.php
@app/Commands/RunCommand.php

<interfaces>
<!-- Plan 01's writer contract — call sites must match this surface. -->

From app/Services/TaskDirectoryWriterService.php (same namespace App\Services — no `use` import needed):

  public function writeNewTask(
      string $repoSlug,
      string|int $taskId,
      ?string $title,
      ?string $body,
      string $repoPath,
      ?string $sourceUrl,
  ): void

  public function writeStatus(
      string $repoSlug,
      string|int $taskId,
      string $state,
  ): void

  public function writeBlockedIfNotTerminal(
      string $repoSlug,
      string|int $taskId,
  ): void

<!-- The 8-state vocabulary from D-02 (subset of TaskLoader.gd's STATES). -->

  Phase 20 emits these 8 states:
    'new', 'selected', 'planning', 'planned', 'executing', 'verifying', 'pr_open', 'blocked'
  (Does NOT emit 'merged' — deferred per D-17.)

<!-- Per-source value resolution at the call site (RESEARCH § "Code Examples" lines 401-421). -->

  At the selection point inside RunOrchestratorService::run():
    $repoSlug  = $repo;                                  // already in scope, e.g. "binarygary/copland"
    $taskId    = $selectedIssue['number'];               // int (GH) or string (Asana GID)
    $title     = $selectedIssue['title'] ?? '';
    $body      = $selectedIssue['body'] ?? '';
    $repoPath  = $repoProfile['repo_path'];              // already read at line 166 today
    $sourceUrl = $selectedIssue['html_url'] ?? '';       // empty for Asana per RESEARCH Pitfall 6

<!-- Current orchestrator insertion line ranges (RESEARCH § Pattern 2, PATTERNS § Pattern B). -->

  | # | State          | Insertion site                                                                       |
  |---|----------------|--------------------------------------------------------------------------------------|
  | 1 | writeNewTask   | between line 108 (`Selected issue #X: Y` log) and line 110 (`// Step 3: Claude plan`) |
  | 1 | writeStatus(new)      | adjacent to #1                                                                |
  | 2 | writeStatus(selected) | adjacent to #1                                                                |
  | 3 | writeStatus(planning) | between line 111 (`[3/8] Running Claude planner` log) and line 112 (`$plan = ...`) |
  | 4 | writeStatus(planned)  | after line 163 (`Plan validated OK` log) — success branch only                |
  | 5 | writeStatus(executing)| between line 174 (`[6/8] Running Claude executor` log) and line 175 (`$this->executor->...`) |
  | 6 | writeStatus(verifying)| between line 213 (`[7/8] Running verification` log) and line 214 (`$this->verifier->...`) |
  | 7 | writeStatus(pr_open)  | after line 263 (`Draft PR opened: {$prUrl}` log)                              |
  | 8 | writeBlockedIfNotTerminal | inside the finally block (lines 300-320), between the workspace-cleanup arm (301-308) and the run-log append arm (310-319) |

  NOTE: Line numbers reference the current `RunOrchestratorService.php`. The executor MUST grep for the literal anchor strings ("Selected issue #", "[3/8] Running Claude planner", "Plan validated OK", "[6/8] Running Claude executor", "[7/8] Running verification", "Draft PR opened:", and the existing `finally {` opening line) — line numbers are guidance; the anchor text is authoritative.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ?TaskDirectoryWriterService $taskWriter constructor parameter to RunOrchestratorService and insert 8 writer call sites + finally-block blocked write</name>

  <read_first>
    - app/Services/RunOrchestratorService.php lines 1-50 (file header, imports, constructor at lines 19-31)
    - app/Services/RunOrchestratorService.php lines 75-170 (selection block + planner + validator + worktree creation — insertion points 1-5 live here)
    - app/Services/RunOrchestratorService.php lines 200-270 (verifier + executor branches + PR creation — insertion points 6-7 live here)
    - app/Services/RunOrchestratorService.php lines 295-320 (the existing try/catch/finally — insertion point 8 lives in the finally arm)
    - app/Services/RunOrchestratorService.php lines 355-385 (existing partialPayload — reference only, DO NOT MODIFY per PATTERNS Pattern E)
    - app/Services/TaskDirectoryWriterService.php (Plan 01's deliverable — read the public method signatures so call sites use correct argument types)
    - .planning/phases/20-task-status-writer/20-CONTEXT.md D-03 (the 8 lifecycle transitions; sequence is non-negotiable) and D-11 (the blocked write lives in the finally arm)
    - .planning/phases/20-task-status-writer/20-RESEARCH.md § "Pattern 2" (lines 305-321, exact insertion points with anchor strings) and § "Common Pitfalls" (especially Pitfall 1 on guarding $selectedIssue !== null in finally, and Pitfall 6 on Asana html_url absence)
    - .planning/phases/20-task-status-writer/20-PATTERNS.md § "Pattern B / C / D" (lines 212-282) — exact insertion-point table + nullsafe operator guidance
    - tests/Unit/RunOrchestratorServiceTest.php (existing orchestrator test fixture — confirm backward compatibility; new optional parameter must default to null so existing constructions stay valid without modification)
  </read_first>

  <files>app/Services/RunOrchestratorService.php</files>

  <behavior>
    - The orchestrator's constructor accepts one new TRAILING optional parameter `?TaskDirectoryWriterService $taskWriter = null`, placed after `?RunLogStore $runLogStore = null` so existing named-argument constructions and existing tests stay valid without modification.
    - Existing test `tests/Unit/RunOrchestratorServiceTest.php` continues to pass without any test-side changes — proves backward compatibility.
    - Every writer call uses the nullsafe operator `$this->taskWriter?->writeXxx(...)` so a null writer is a silent no-op (NOT a TypeError).
    - The 9 call sites fire in this exact order during a successful run:
      1. `writeNewTask($repo, $selectedIssue['number'], $selectedIssue['title'] ?? '', $selectedIssue['body'] ?? '', $repoProfile['repo_path'], $selectedIssue['html_url'] ?? '')` — immediately after the "Selected issue #" pushLog (line 108-ish), before "// Step 3: Claude plan".
      2. `writeStatus($repo, $selectedIssue['number'], 'new')` — immediately after #1.
      3. `writeStatus($repo, $selectedIssue['number'], 'selected')` — immediately after #2.
      4. `writeStatus($repo, $selectedIssue['number'], 'planning')` — between the "[3/8] Running Claude planner" pushLog and the `$plan = $this->planner->planTask(...)` call.
      5. `writeStatus($repo, $selectedIssue['number'], 'planned')` — after the "Plan validated OK" pushLog (success branch only — must NOT fire if validation failed and returned early).
      6. `writeStatus($repo, $selectedIssue['number'], 'executing')` — between the "[6/8] Running Claude executor" pushLog and the `$this->executor->executeWithRepoProfile(...)` call.
      7. `writeStatus($repo, $selectedIssue['number'], 'verifying')` — between the "[7/8] Running verification" pushLog and the `$this->verifier->verify(...)` call.
      8. `writeStatus($repo, $selectedIssue['number'], 'pr_open')` — after the "Draft PR opened:" pushLog.
      9. `writeBlockedIfNotTerminal($repo, $selectedIssue['number'])` — inside the existing `finally { }` block, between the workspace-cleanup arm and the run-log append arm. Guarded by `if ($this->taskWriter !== null && $selectedIssue !== null)` per RESEARCH Pitfall 1 (selector "skip_all" or pre-selection exceptions leave `$selectedIssue` null).
    - The finally-block writer call is wrapped in its OWN `try/catch (\Throwable $e)` arm — writer errors NEVER mask the original exception propagating from the `try` block. Caught errors are surfaced via `pushLog("      Warning: blocked-state write failed: {$e->getMessage()}")` matching the existing cleanup-arm style at lines 305-307.
    - The other 8 writer calls (the happy-path ones) are NOT wrapped in try/catch — they live in the main `try` block and any writer failure should fail-loud during a run (matches the existing orchestrator's behavior for plan-validator failures, executor crashes, etc.).
    - `partialPayload()` (lines 355-385) is NOT modified. Its concern (writing to `runs.jsonl`) is independent of `writeBlockedIfNotTerminal`'s concern (writing to `status.md`).
    - `pushLog` calls are NOT added for the writer transitions — D-12 / CONTEXT § Specifics: the writer is silent and the orchestrator already narrates each step (`[3/8] Running Claude planner` etc.). Double-narration would clutter overnight logs.

    Anti-behaviors (must NOT happen):
    - The writer is NOT called from inside any failure branch that returns early (e.g., the "Selected task #X not found" branch at lines 86-101, the "decline" branch at lines 118-134, the "validation failed" branch at lines 142-161, the "execution failed" branch at lines 186-206). The `finally` arm's `writeBlockedIfNotTerminal` catches these via the in-memory last-state tracker on the writer side.
    - `writeNewTask` is called EXACTLY ONCE per run (after $selectedIssue is non-null). Per D-08, task.md is singular.
    - The orchestrator does NOT branch on task source (GitHub vs Asana) when calling the writer — per RESEARCH Anti-Patterns, the writer is source-agnostic. The orchestrator passes `$selectedIssue['html_url'] ?? ''` for the sourceUrl, which is naturally empty for Asana.
  </behavior>

  <action>
    Modify `app/Services/RunOrchestratorService.php`.

    Implements TASK-01 (the writeNewTask call at selection time) and TASK-02 (the 7 writeStatus calls + writeBlockedIfNotTerminal in finally).

    Step 1 — Constructor parameter:
    - Locate the constructor (currently lines 19-31). At the END of the parameter list, AFTER `private ?RunLogStore $runLogStore = null,`, append a new line: `private ?TaskDirectoryWriterService $taskWriter = null,`. Keep the trailing comma per the existing multi-line shape. The class is in namespace `App\Services` (same as the writer), so NO new `use` statement is required.

    Step 2 — Selection block (insertion points 1, 2, 3):
    - Find the line `$this->pushLog("      Selected issue #{$selectedIssue['number']}: {$selectedIssue['title']}");` (currently line 108). Immediately AFTER that line, insert three calls in this exact order:
      1. `$this->taskWriter?->writeNewTask($repo, $selectedIssue['number'], $selectedIssue['title'] ?? '', $selectedIssue['body'] ?? '', $repoProfile['repo_path'], $selectedIssue['html_url'] ?? '');`
      2. `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'new');`
      3. `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'selected');`
    - These three lines live OUTSIDE any `if ($snapshot !== null)` block — they fire whenever the orchestrator reaches a non-null $selectedIssue regardless of snapshot state. Place them after the existing `$this->pushLog(...)` and before the `// Step 3: Claude plan` comment.

    Step 3 — Planning transition (insertion point 4):
    - Find the line `$this->pushLog('[3/8] Running Claude planner');` (currently line 111). Immediately AFTER that line, BEFORE `$plan = $this->planner->planTask(...);`, insert:
      `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'planning');`

    Step 4 — Planned transition (insertion point 5):
    - Find the line `$this->pushLog('      Plan validated OK');` (currently line 163). Immediately AFTER that line, insert:
      `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'planned');`
    - This line is INSIDE the success branch only (after the `if (! empty($validationErrors))` early-return block). Confirm by checking that the existing line above it is the success-path log, not the failure-path log.

    Step 5 — Executing transition (insertion point 6):
    - Find the line `$this->pushLog('[6/8] Running Claude executor');` (currently line 174). Immediately AFTER that line, BEFORE `$executionResult = $this->executor->executeWithRepoProfile(`, insert:
      `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'executing');`

    Step 6 — Verifying transition (insertion point 7):
    - Find the line `$this->pushLog('[7/8] Running verification');` (currently line 213). Immediately AFTER that line, BEFORE `$verificationResult = $this->verifier->verify(`, insert:
      `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'verifying');`

    Step 7 — PR-open transition (insertion point 8):
    - Find the line `$this->pushLog("      Draft PR opened: {$prUrl}");` (currently line 263). Immediately AFTER that line, insert:
      `$this->taskWriter?->writeStatus($repo, $selectedIssue['number'], 'pr_open');`

    Step 8 — Blocked-write in finally (insertion point 9):
    - Locate the existing `} finally {` block (currently starts at line 300). Inside the finally arm, AFTER the existing workspace-cleanup arm (which closes at line 308 with `}`) and BEFORE the existing run-log append arm (which opens at line 310 with `try {`), insert this block (matches PATTERNS § "New arm to insert" lines 263-269):

      ```
      if ($this->taskWriter !== null && $selectedIssue !== null) {
          try {
              $this->taskWriter->writeBlockedIfNotTerminal($repo, $selectedIssue['number']);
          } catch (\Throwable $e) {
              $this->pushLog("      Warning: blocked-state write failed: {$e->getMessage()}");
          }
      }
      ```

      Note: this is one of two places where a fully-qualified `$this->taskWriter !== null` check is used instead of the nullsafe `?->` — that's intentional because the `try/catch` wrapper would be wasted on a no-op nullsafe call, AND because `writeBlockedIfNotTerminal` needs to be invoked via direct method call so the `try { ... } catch` semantics work cleanly.

    Step 9 — Verify no other arms touched:
    - `partialPayload()` (lines 355-385) is NOT modified.
    - The existing `runs.jsonl` append in finally (lines 310-319) is NOT modified.
    - The catch arm (lines 295-298) is NOT modified — the writer call lives in finally so it fires whether the catch arm re-threw or the try arm fell through.
    - No new `use` statements added — `TaskDirectoryWriterService` is in the same `App\Services` namespace and `Throwable` is already imported at the top of the file.

    Style: 4-space indent (matches existing file). No new blank lines between adjacent writer calls — the three-line block at the selection point should be a tight cluster matching the surrounding pushLog density. Use the nullsafe `?->` operator at all 8 happy-path sites (PATTERNS Pattern D); use the explicit null-check at the finally site (because of the try/catch wrapper). Do NOT add any inline comments explaining what the writer does — the call sites are self-documenting via the method names. Per CLAUDE.md "Explain WHY, not WHAT": the method names are the what; no why-comment is needed.

    Do NOT modify `tests/Unit/RunOrchestratorServiceTest.php` in this task — the new parameter defaults to null so existing constructions stay valid. Test-side coverage of orchestrator wiring is Phase 21 per D-15. Do NOT add Mockery mocks. Do NOT touch any data class (`RunResult`, `SelectionResult`, etc.) — TASK-01/02 do not change their shapes. Do NOT touch `runs.jsonl` (D-16 additive-only). Do NOT emit any new pushLog lines for the writer transitions (D-12 silent-writer invariant).
  </action>

  <verify>
    <automated>./vendor/bin/pest tests/Unit/RunOrchestratorServiceTest.php &amp;&amp; php -l app/Services/RunOrchestratorService.php &amp;&amp; ./vendor/bin/pint --test app/Services/RunOrchestratorService.php</automated>
  </verify>

  <acceptance_criteria>
    - source: `grep -c "private ?TaskDirectoryWriterService \\\$taskWriter" app/Services/RunOrchestratorService.php` == 1 (new constructor parameter present)
    - source: `grep -c "taskWriter?->writeNewTask(" app/Services/RunOrchestratorService.php` == 1 (exactly one writeNewTask call — task.md is singular per D-08)
    - source: `grep -c "taskWriter?->writeStatus(" app/Services/RunOrchestratorService.php` == 7 (the 7 happy-path writeStatus calls)
    - source: `grep -c "taskWriter->writeBlockedIfNotTerminal" app/Services/RunOrchestratorService.php` == 1 (the finally-arm call, non-nullsafe because wrapped in try/catch)
    - source: `grep -c "'new'\\|'selected'\\|'planning'\\|'planned'\\|'executing'\\|'verifying'\\|'pr_open'" app/Services/RunOrchestratorService.php` >= 7 (the 7 state strings appear at least once each; some may share lines)
    - source: `grep -c "'merged'" app/Services/RunOrchestratorService.php` == 0 (merged state is deferred per D-17 — must NOT be written by Phase 20)
    - source: `awk '/} finally {/,/^    }$/' app/Services/RunOrchestratorService.php | grep -c "writeBlockedIfNotTerminal"` == 1 (the blocked write lives inside the finally arm, not elsewhere)
    - source: `awk '/} finally {/,/^    }$/' app/Services/RunOrchestratorService.php | grep -c "\\\$selectedIssue !== null"` >= 1 (the guard from RESEARCH Pitfall 1 is present in the finally arm)
    - source: `grep -B1 "writeBlockedIfNotTerminal" app/Services/RunOrchestratorService.php | grep -c "try {"` >= 1 (the finally-arm writer call is wrapped in try/catch per PATTERNS Pattern C)
    - source: order check — `grep -n "writeNewTask\\|writeStatus(.*'new'\\|writeStatus(.*'selected'" app/Services/RunOrchestratorService.php | sort -n` shows writeNewTask FIRST, then 'new', then 'selected' (D-03 ordering)
    - command: `php -l app/Services/RunOrchestratorService.php` outputs `No syntax errors detected`
    - command: `./vendor/bin/pint --test app/Services/RunOrchestratorService.php` exits 0
    - command: `./vendor/bin/pest tests/Unit/RunOrchestratorServiceTest.php` exits 0 (backward compatibility: existing orchestrator tests still pass with the new optional parameter defaulting to null)
    - behavior: the diff against the previous version of this file consists of one constructor-parameter addition and 9 single-or-few-line insertions at the documented anchor points — net change ~10-15 lines added, 0 lines deleted from existing logic
  </acceptance_criteria>

  <done>
    `app/Services/RunOrchestratorService.php` declares the new `?TaskDirectoryWriterService $taskWriter = null` constructor parameter, contains exactly 1 `writeNewTask(` call, exactly 7 `writeStatus(` calls covering states `new, selected, planning, planned, executing, verifying, pr_open`, exactly 1 `writeBlockedIfNotTerminal(` call inside the finally arm wrapped in its own try/catch and guarded on `$selectedIssue !== null`, AND the existing `RunOrchestratorServiceTest` still passes without modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire TaskDirectoryWriterService into RunCommand composition root</name>

  <read_first>
    - app/Commands/RunCommand.php lines 1-50 (file header + existing `use` block — confirm imports are alphabetized or grouped per CLAUDE.md convention)
    - app/Commands/RunCommand.php lines 270-305 (the $repoProfile construction at 276-287 + RunOrchestratorService instantiation at 289-299 + `$orchestrator->run(...)` invocation at 301)
    - .planning/phases/20-task-status-writer/20-PATTERNS.md § "RunCommand.php (MODIFIED — composition root)" lines 307-344 (the exact named-argument addition)
    - app/Services/TaskDirectoryWriterService.php (Plan 01) — confirm constructor signature so we know whether `new TaskDirectoryWriterService` (with no args) works (it does — both seam parameters default to null)
    - app/Services/RunOrchestratorService.php after Task 1 of this plan (the new constructor parameter must already exist in the orchestrator before this task — Task 1 of this plan is the prerequisite, even though they run in the same execution wave; if executor runs Task 2 first, the named argument will be rejected by PHP)
  </read_first>

  <files>app/Commands/RunCommand.php</files>

  <behavior>
    - `RunCommand` imports `TaskDirectoryWriterService` via a `use App\Services\TaskDirectoryWriterService;` line in the existing `use` block at the top of the file.
    - The existing `$orchestrator = new RunOrchestratorService(...)` call at lines 289-299 gains ONE additional named argument `taskWriter: new TaskDirectoryWriterService` appended after `verifier: new VerificationService($git),` (the current last argument).
    - The new line maintains the existing named-argument calling style and the trailing-comma multi-line shape.
    - No other lines in `RunCommand.php` change — the `$repoProfile` construction at lines 276-287 is unchanged (`repo_path` is already present at line 286), and the `$orchestrator->run(...)` invocation at line 301 is unchanged.
    - All existing `copland` commands (`run`, `plan`, `console`, `issues`, `status`, etc.) continue to work unchanged.
    - The full Pest suite continues to pass (132+ tests).
  </behavior>

  <action>
    Modify `app/Commands/RunCommand.php`.

    Implements TASK-01 + TASK-02 (the composition-root wiring that makes the writer actually fire on real runs — without this, Plan 01's writer is never instantiated and the orchestrator's new parameter stays at its `null` default).

    Step 1 — Add the import:
    - Locate the existing `use` block near the top of the file. Insert `use App\Services\TaskDirectoryWriterService;` in alphabetical position among the existing `App\Services\...` imports (the existing block already imports `App\Services\ClaudeExecutorService`, `App\Services\ClaudePlannerService`, `App\Services\ClaudeSelectorService`, `App\Services\GitHubService`, `App\Services\GitService`, `App\Services\IssuePrefilterService`, `App\Services\PlanValidatorService`, `App\Services\RunOrchestratorService`, `App\Services\VerificationService`, `App\Services\WorkspaceService` — confirm exact list by reading the current file; insert `TaskDirectoryWriterService` between `RunOrchestratorService` and `VerificationService` alphabetically). If imports are grouped differently than alphabetical (some projects use length-ordering or insertion-order), match whichever style the file currently uses.

    Step 2 — Append the named argument:
    - Locate the existing `$orchestrator = new RunOrchestratorService(` call. Currently the last named argument is `verifier: new VerificationService($git),` (line 298). On the line AFTER that, BEFORE the closing `);`, append:
      `taskWriter: new TaskDirectoryWriterService,`
    - Note: `new TaskDirectoryWriterService` with no parentheses is PHP 8.0+ idiomatic for argument-less construction. The class accepts two optional nullable parameters that both default to null, so no arguments are needed in production wiring — `HomeDirectory::resolve()` resolves at write time, and the default `gmdate(...)` clock fires per-write.
    - Maintain the trailing-comma multi-line shape used by the existing arguments. The closing `);` stays on its own line.

    Style: match existing 4-space indent. Do NOT change argument order of the prior named arguments. Do NOT add a comment explaining why this dependency is added — the named argument is self-documenting. Per CLAUDE.md: no commentary unless preserving a non-obvious invariant.

    Do NOT modify the `$repoProfile` array (line 276-287) — `repo_path` is already present at line 286 and the orchestrator reads it from there. Do NOT modify the `$orchestrator->run(...)` call signature (line 301). Do NOT modify any other command (`plan`, `console`, `issues`, `status`) — the writer is `copland run`-specific (per CONTEXT.md Phase Boundary: "When `RunOrchestratorService::run()` picks a task..."). Do NOT pass `clock:` or `homeOverride:` arguments to `new TaskDirectoryWriterService` in production — the defaults are correct for real runs; the seams are designed for tests, not production wiring.
  </action>

  <verify>
    <automated>./vendor/bin/pest &amp;&amp; php -l app/Commands/RunCommand.php &amp;&amp; ./vendor/bin/pint --test app/Commands/RunCommand.php</automated>
  </verify>

  <acceptance_criteria>
    - source: `grep -c "use App\\\\Services\\\\TaskDirectoryWriterService;" app/Commands/RunCommand.php` == 1 (import added exactly once)
    - source: `grep -c "taskWriter: new TaskDirectoryWriterService" app/Commands/RunCommand.php` == 1 (named-argument wiring exactly once)
    - source: `grep -c "RunOrchestratorService(" app/Commands/RunCommand.php` == 1 (still exactly one orchestrator construction — no duplication)
    - source: `grep -c "'repo_path' => \\\$path," app/Commands/RunCommand.php` == 1 ($repoProfile['repo_path'] still wired, unchanged from current state)
    - source: line count of `app/Commands/RunCommand.php` increased by exactly 2 lines (one import + one named argument) compared to its pre-task state
    - command: `php -l app/Commands/RunCommand.php` outputs `No syntax errors detected`
    - command: `./vendor/bin/pint --test app/Commands/RunCommand.php` exits 0
    - command: `./vendor/bin/pest` (full suite) exits 0 — all existing tests pass; TASK-04 / D-16 additive-only invariant preserved
    - command: `./vendor/bin/copland --help` (or `php copland --help` from project root) exits 0 and lists all existing commands without errors
    - behavior: the diff against the previous version consists of exactly 2 added lines (one `use` import, one named argument) and 0 deletions or modifications to existing logic
  </acceptance_criteria>

  <done>
    `app/Commands/RunCommand.php` imports `TaskDirectoryWriterService`, passes a no-args `new TaskDirectoryWriterService` as the `taskWriter:` named argument to `RunOrchestratorService`, and the full Pest suite passes. Net diff: +2 lines, 0 lines deleted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.copland.yml` repo config → orchestrator | `$repoProfile['repo_path']` originates from developer-supplied per-repo config; flows into the writer as a path component |
| External API → orchestrator | `$selectedIssue['title']`, `['body']`, `['html_url']` originate from GitHub or Asana API responses; flow into the writer as frontmatter scalars |
| Orchestrator → writer | All threat handling lives in Plan 01's writer (escape, atomic rename); orchestrator just forwards values |
| Orchestrator catch arm → finally arm | An attacker-induced exception path must not block the `blocked` status write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | Issue title/body from GitHub or Asana API contains literal `"` or `\` characters | mitigate | Plan 01's `renderFrontmatter` escapes both characters. Orchestrator forwards the raw values; the writer is the canonical escape point. No additional escaping at the orchestrator layer. |
| T-20-08 | Tampering | `$selectedIssue['number']` is an attacker-controlled string from Asana (e.g., a GID with embedded `/`) | mitigate | Asana GIDs are documented as numeric strings (verified at AsanaService.php:46-53). GitHub `number` is `int`. Both flow through `(string) $taskId` in the writer; the writer's `taskDir()` does NOT inject `$taskId` into a directory path component beyond the trailing segment. A malicious Asana GID containing `..` is RESEARCH Pitfall 5 — currently accepted (developer-supplied Asana config, not a Phase-20 attack surface). |
| T-20-09 | Repudiation | Crash during run leaves stale `executing` state in `status.md`; operator can't tell whether the run reached PR-open | mitigate | D-11: finally-arm `writeBlockedIfNotTerminal` writes `blocked` whenever the in-memory last-state is non-terminal. Wrapped in its own try/catch so writer-side errors never mask the original exception (Pitfall 1). PATTERNS Pattern C anchors the wrap pattern. |
| T-20-10 | Information Disclosure | Exception message captured by the existing `pushLog("Run crashed: {$e->getMessage()}")` could leak secrets if a service returned them in error text | accept | Pre-existing behavior — not introduced by Phase 20. The new `Warning: blocked-state write failed: {$e->getMessage()}` line mirrors the existing cleanup-arm warning at lines 305-307; same disclosure surface. |
| T-20-11 | Denial of Service | A writer failure on one of the 7 happy-path writeStatus calls would crash the orchestrator mid-run | accept | Acceptable per "fail-loud" design — a writer failure indicates a corrupt or full filesystem; better to surface than swallow. The finally-arm runs after the throw, so `blocked` is still attempted. Worst case: filesystem is so broken that even `blocked` write fails, and the wrapped try/catch in finally logs a warning but does not re-throw. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan adds zero new packages. No install gate needed. |
</threat_model>

<verification>
- `./vendor/bin/pest tests/Unit/RunOrchestratorServiceTest.php` exits 0 (orchestrator's existing test still passes with the new optional parameter defaulting to null).
- `./vendor/bin/pest tests/Feature/TaskDirectoryWriterServiceTest.php` exits 0 (Plan 01's smoke test still passes — wiring did not break the writer).
- `./vendor/bin/pest` (full suite) exits 0 with all 132+ existing tests green.
- `php -l app/Services/RunOrchestratorService.php` and `php -l app/Commands/RunCommand.php` both output `No syntax errors detected`.
- `./vendor/bin/pint --test app/Services/RunOrchestratorService.php app/Commands/RunCommand.php` exits 0.
- Manual integration smoke (post-merge check, runs against a real test repo with `.copland.yml`): `./builds/copland run <test-repo>` — after the run completes (whether success, failure, or SIGINT), `~/.copland/tasks/<test-repo-slug-collapsed>/<issue-number>/task.md` exists with valid YAML frontmatter and `status.md` contains a terminal state (`pr_open` or `blocked`) and at least 2 transitions in its transitions table.
- Manual integration smoke (failure path): `kill -INT` the `copland run` process between `executing` and `pr_open` states; verify `cat ~/.copland/tasks/.../status.md` shows `state: "blocked"`.
</verification>

<success_criteria>
- TASK-01 implemented end-to-end: a real `copland run` writes `task.md` exactly once at the selection point with frontmatter containing the 6 required keys per D-08.
- TASK-02 implemented end-to-end: a real `copland run` writes/updates `status.md` on each of the 7 forward transitions AND writes `blocked` from the finally arm on any pre-`pr_open` exception or SIGINT (per D-11).
- The full Pest suite passes (132+ tests) — TASK-04 / D-16 additive-only invariant is preserved.
- The new orchestrator parameter is backward-compatible: `tests/Unit/RunOrchestratorServiceTest.php` passes without test-side modifications.
- The Godot console (shipped in Phase 19) can be launched via `copland console` after a real run and renders task rows with correct title, state, and repo_slug (manual verification — listed in VALIDATION.md § "Manual-Only Verifications").
</success_criteria>

<output>
Create `.planning/phases/20-task-status-writer/20-02-SUMMARY.md` when done. Summary must list:
- The exact diff size for each of the two modified files (added/removed line counts).
- The 9 writer call sites with file:line references (after-modification line numbers).
- The exact named-argument wiring line in RunCommand.php.
- Confirmation that the full Pest suite passes.
- Confirmation that no data classes, no other services, no other commands were modified.
- Any line-number drift from the planned insertion points (the planner's anchors were authoritative — line numbers were guidance; the executor's actual line numbers may differ slightly if intervening commits shifted the file).
</output>
</content>
</invoke>