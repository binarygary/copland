---
phase: 12-multi-repo-failure-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Commands/RunCommand.php
  - tests/Feature/RunCommandTest.php
autonomous: true
requirements: [OBS-01, SCHED-02]
user_setup: []
must_haves:
  truths:
    - "A repo failure caught in `RunCommand` before `RunOrchestratorService` starts still appends a structured `runs.jsonl` record."
    - "The failure record includes repo, failed status, timestamps, and failure reason."
    - "Multi-repo execution continues to later repos after such a failure."
  artifacts:
    - path: "app/Commands/RunCommand.php"
      provides: "Pre-orchestrator failure logging for repo-level exceptions in the multi-repo loop"
    - path: "tests/Feature/RunCommandTest.php"
      provides: "Regression coverage proving failed repo logging and fail-and-continue behavior"
---

<objective>
Close the milestone audit integration gap where repo failures handled in `RunCommand` never reach structured run logging.

Purpose: Phase 12 restores `runs.jsonl` as the source of truth even when a repo fails before `RunOrchestratorService` begins.
Output: A minimal `RunCommand` logging path plus regression coverage for the multi-repo fail-and-continue case.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-multi-repo-failure-logging/12-CONTEXT.md
@app/Commands/RunCommand.php
@app/Services/RunOrchestratorService.php
@app/Support/RunLogStore.php
@app/Data/RunResult.php
@tests/Feature/RunCommandTest.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Log pre-orchestrator repo failures through RunLogStore</name>
  <files>app/Commands/RunCommand.php</files>
  <read_first>app/Commands/RunCommand.php, app/Services/RunOrchestratorService.php, app/Support/RunLogStore.php, app/Data/RunResult.php</read_first>
  <action>Add the smallest clean seam needed so the repo-level `catch` path in `RunCommand` appends a structured JSONL record when an exception occurs before `RunOrchestratorService` starts. Reuse the existing run-log payload shape as closely as possible, including repo, status, timestamps, and failure reason. Do not change the existing multi-repo control flow, exit-code behavior, or aggregated usage output.</action>
  <verify>php -l app/Commands/RunCommand.php</verify>
  <acceptance_criteria>
    - The repo-level `catch` path appends a structured run-log record
    - The failure record includes repo, failed status, timestamps, and failure reason
    - Normal orchestrator-owned logging remains untouched
  </acceptance_criteria>
  <done>`RunCommand` closes the audit logging gap without broad refactoring.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for fail-and-continue logging</name>
  <files>tests/Feature/RunCommandTest.php</files>
  <read_first>tests/Feature/RunCommandTest.php, app/Commands/RunCommand.php, app/Support/RunLogStore.php</read_first>
  <action>Add an offline regression test that exercises `RunCommand` directly with multiple configured repos, forces one repo to fail before orchestrator startup, and proves both that a structured `runs.jsonl` entry is written for the failed repo and that later repos still execute. Keep the test deterministic with temp-path filesystem setup or a narrow seam rather than touching the real user home directory.</action>
  <verify>./vendor/bin/pest tests/Feature/RunCommandTest.php</verify>
  <acceptance_criteria>
    - The test proves a pre-orchestrator repo failure creates a run-log entry
    - The test proves execution continues to the next repo
    - The regression runs offline with no network or real user-path dependence
  </acceptance_criteria>
  <done>The missing integration is locked by a focused regression test.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Commands/RunCommand.php
- [ ] ./vendor/bin/pest tests/Feature/RunCommandTest.php
</verification>

<success_criteria>
- All tasks completed
- Pre-orchestrator repo failures produce structured run-log entries
- Multi-repo fail-and-continue behavior remains intact
- Regression coverage protects the audit gap from reopening
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-repo-failure-logging/12-01-SUMMARY.md`
</output>
