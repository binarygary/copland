---
phase: 08-retry-wrapper-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/AnthropicApiClient.php
  - tests/Unit/AnthropicApiClientTest.php
autonomous: true
requirements: [TEST-03]
user_setup: []
must_haves:
  truths:
    - "`AnthropicApiClient` retries retryable failures without real sleep in tests."
    - "`AnthropicApiClient` fails immediately for non-retryable 4xx responses."
    - "Retry tests run offline with scripted fake client responses."
  artifacts:
    - path: "app/Support/AnthropicApiClient.php"
      provides: "retry wrapper with a testable backoff seam"
    - path: "tests/Unit/AnthropicApiClientTest.php"
      provides: "wrapper-focused retry, backoff, and fail-fast coverage"
  key_links:
    - from: "tests/Unit/AnthropicApiClientTest.php"
      to: "app/Support/AnthropicApiClient.php"
      via: "scripted fake client and delay capture assertions"
---

<objective>
Add direct automated coverage for the Anthropic retry wrapper.

Purpose: Phase 8 closes the highest-value testing gap by proving the shared retry contract instead of re-testing each consumer service indirectly.
Output: A testable `AnthropicApiClient` backoff seam and a unit test suite covering retryable, non-retryable, and network-style failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-retry-wrapper-tests/08-CONTEXT.md
@.planning/phases/01-api-retry-backoff/01-CONTEXT.md
@app/Support/AnthropicApiClient.php
@tests/Feature/ClaudeServicesTest.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a no-sleep backoff seam to AnthropicApiClient</name>
  <files>app/Support/AnthropicApiClient.php</files>
  <read_first>app/Support/AnthropicApiClient.php, .planning/phases/08-retry-wrapper-tests/08-CONTEXT.md, .planning/phases/01-api-retry-backoff/01-CONTEXT.md</read_first>
  <action>Update `App\Support\AnthropicApiClient` so tests can observe retry delays without calling real `sleep()`. Keep the public `messages(...)` API unchanged, but add a minimal injected delay seam such as a callable that receives the computed delay seconds. The default production behavior must still sleep for the computed exponential backoff (`baseDelaySeconds * 2 ** (attempt - 1)`). Preserve the current retry classification rules: retry on `429`, retry on `5xx`, retry on missing-response/network-style failures, and fail immediately on non-429 `4xx` responses.</action>
  <verify>php -l app/Support/AnthropicApiClient.php</verify>
  <acceptance_criteria>
    - app/Support/AnthropicApiClient.php preserves the public `messages(` method
    - app/Support/AnthropicApiClient.php contains an injectable delay seam instead of hardcoded test-time sleep only
    - app/Support/AnthropicApiClient.php still retries `429`
    - app/Support/AnthropicApiClient.php still treats non-response failures as `network_error`
  </acceptance_criteria>
  <done>The retry wrapper remains production-safe and becomes deterministic to test.</done>
</task>

<task type="auto">
  <name>Task 2: Add wrapper-focused unit tests for retry classification and backoff</name>
  <files>tests/Unit/AnthropicApiClientTest.php</files>
  <read_first>app/Support/AnthropicApiClient.php, tests/Unit/GlobalConfigTest.php, .planning/phases/08-retry-wrapper-tests/08-CONTEXT.md</read_first>
  <action>Create `tests/Unit/AnthropicApiClientTest.php` with lightweight fake client/messages objects that script response sequences for the wrapper. Add at least these test cases: one `429` then success sequence that asserts the wrapper retries and captures the expected delay; one `5xx` then success sequence that asserts retries happen up to the configured attempt count; one non-429 `4xx` failure that asserts no retry or delay occurs; and one network-style exception without `getResponse()` that asserts the wrapper retries and eventually fails after the configured attempt limit. Keep all tests offline and do not instantiate the real Anthropic SDK transport.</action>
  <verify>./vendor/bin/pest tests/Unit/AnthropicApiClientTest.php</verify>
  <acceptance_criteria>
    - tests/Unit/AnthropicApiClientTest.php contains a `429` retry case
    - tests/Unit/AnthropicApiClientTest.php contains a `5xx` retry case
    - tests/Unit/AnthropicApiClientTest.php contains a non-429 `4xx` fail-fast case
    - tests/Unit/AnthropicApiClientTest.php contains a network-style retry case
    - tests/Unit/AnthropicApiClientTest.php asserts captured backoff delay values without using wall-clock time
  </acceptance_criteria>
  <done>Anthropic retry behavior is regression-tested without real HTTP calls.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] php -l app/Support/AnthropicApiClient.php
- [ ] ./vendor/bin/pest tests/Unit/AnthropicApiClientTest.php
</verification>

<success_criteria>
- All tasks completed
- `AnthropicApiClient` retry behavior is covered directly
- Tests verify backoff timing without real sleeping
- Tests run offline and cover retryable plus non-retryable failure classes
</success_criteria>

<output>
After completion, create `.planning/phases/08-retry-wrapper-tests/08-01-SUMMARY.md`
</output>
