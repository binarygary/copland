---
phase: 05-cache-aware-cost-model
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [app/Services/ClaudeSelectorService.php, app/Services/ClaudePlannerService.php, app/Services/ClaudeExecutorService.php]
autonomous: true
requirements: [COST-02]

must_haves:
  truths:
    - "Selector and Planner services report cache usage to the cost estimator"
    - "Executor loop accumulates cache tokens across rounds"
  artifacts:
    - path: "app/Services/ClaudeExecutorService.php"
      provides: "Total cache write/read accumulation"
  key_links:
    - from: "app/Services/ClaudeExecutorService.php"
      to: "app/Support/AnthropicCostEstimator.php"
      via: "passing cache tokens to forModel()"
---

<objective>
Update the Selector, Planner, and Executor services to extract and accumulate cache tokens from API responses.

Purpose: End-to-end integration of the new cache-aware cost model.
Output: Updated services passing accurate usage data to the cost estimator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cache-aware-cost-model/05-01-SUMMARY.md
@app/Services/ClaudeSelectorService.php
@app/Services/ClaudePlannerService.php
@app/Services/ClaudeExecutorService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Selector and Planner usage extraction</name>
  <files>app/Services/ClaudeSelectorService.php, app/Services/ClaudePlannerService.php</files>
  <action>
    Update usageFromResponse() in both ClaudeSelectorService and ClaudePlannerService to extract cache creation and read tokens:
    - Access $response->usage->cacheCreationInputTokens ?? 0
    - Access $response->usage->cacheReadInputTokens ?? 0
    - Pass these values to AnthropicCostEstimator::forModel().
  </action>
  <verify>
    <automated>php -l app/Services/ClaudeSelectorService.php && php -l app/Services/ClaudePlannerService.php</automated>
  </verify>
  <done>Selector and Planner accurately extract cache tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Update Executor to accumulate cache tokens</name>
  <files>app/Services/ClaudeExecutorService.php</files>
  <action>
    In ClaudeExecutorService::executeWithPolicy():
    - Initialize $totalCacheWriteTokens = 0 and $totalCacheReadTokens = 0 before the while(true) loop.
    - Inside the loop, when usage is present, increment these accumulators using values from $response->usage.
    - Update all calls to AnthropicCostEstimator::forModel() (in loop, in return statements, and in updateSnapshot()) to include these accumulators.
  </action>
  <verify>
    <automated>php -l app/Services/ClaudeExecutorService.php</automated>
  </verify>
  <done>Executor accumulates and reports cache tokens correctly across rounds.</done>
</task>

</tasks>

<verification>
Check syntax of modified services. Functional verification depends on unit tests in Plan 01.
</verification>

<success_criteria>
- Selector, Planner, and Executor correctly pass cache tokens to the cost estimator.
- Multi-round executor runs correctly sum cache tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cache-aware-cost-model/05-02-SUMMARY.md`
</output>
