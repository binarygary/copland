---
phase: 17-asana-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Data/SelectionResult.php
  - app/Data/RunResult.php
  - app/Support/RunProgressSnapshot.php
  - app/Services/RunOrchestratorService.php
  - app/Services/ClaudeSelectorService.php
  - app/Commands/RunCommand.php
  - app/Commands/PlanCommand.php
  - resources/prompts/selector.md
autonomous: true
requirements:
  - ASANA-04
  - ASANA-05

must_haves:
  truths:
    - "SelectionResult has a selectedTaskId property typed string|int|null (not selectedIssueNumber)"
    - "RunResult has a selectedTaskId property typed string|int|null (not selectedIssueNumber)"
    - "RunProgressSnapshot has a selectedTaskId property typed string|int|null (not selectedIssueNumber)"
    - "No file in app/ or resources/ contains the string 'selectedIssueNumber'"
    - "All 99 existing tests pass after the rename"
  artifacts:
    - path: "app/Data/SelectionResult.php"
      provides: "SelectionResult data class"
      contains: "string|int|null $selectedTaskId"
    - path: "app/Data/RunResult.php"
      provides: "RunResult data class"
      contains: "string|int|null $selectedTaskId"
    - path: "app/Support/RunProgressSnapshot.php"
      provides: "RunProgressSnapshot mutable snapshot"
      contains: "string|int|null $selectedTaskId"
  key_links:
    - from: "app/Services/ClaudeSelectorService.php"
      to: "app/Data/SelectionResult.php"
      via: "constructor named argument"
      pattern: "selectedTaskId:"
    - from: "app/Services/RunOrchestratorService.php"
      to: "app/Data/SelectionResult.php"
      via: "->selectedTaskId property access"
      pattern: "->selectedTaskId"
    - from: "app/Commands/RunCommand.php"
      to: "app/Data/RunResult.php"
      via: "->selectedTaskId property access"
      pattern: "selectedTaskId:"
---

<objective>
Rename `selectedIssueNumber` to `selectedTaskId` and widen its type from `?int` to `string|int|null` across all call sites. This is the foundational data migration for Asana integration — Asana GIDs are 16-digit numeric strings that cannot be stored as PHP int without truncation.

Purpose: Unblocks all Asana pipeline work. GitHub integer issue numbers continue to work via `int` (still accepted by `string|int|null`). Asana GID strings flow through without type coercion.
Output: All data classes and call sites updated; zero references to `selectedIssueNumber` remain; full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-asana-integration/17-CONTEXT.md
@.planning/phases/17-asana-integration/17-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename selectedIssueNumber → selectedTaskId in data classes and snapshot</name>
  <read_first>
    - app/Data/SelectionResult.php
    - app/Data/RunResult.php
    - app/Support/RunProgressSnapshot.php
    - tests/Unit/RunOrchestratorServiceTest.php
  </read_first>
  <files>app/Data/SelectionResult.php, app/Data/RunResult.php, app/Support/RunProgressSnapshot.php</files>
  <behavior>
    - SelectionResult::$selectedTaskId is string|int|null (was ?int $selectedIssueNumber)
    - RunResult::$selectedTaskId is string|int|null (was ?int $selectedIssueNumber)
    - RunProgressSnapshot::$selectedTaskId is string|int|null (was ?int $selectedIssueNumber)
    - Constructing SelectionResult with selectedTaskId: '1206654088086513' (string GID) succeeds without type error
    - Constructing SelectionResult with selectedTaskId: 42 (int) succeeds (GitHub issue number)
    - Constructing RunResult with selectedTaskId: null succeeds
  </behavior>
  <action>
Update three files with the exact changes below:

**app/Data/SelectionResult.php** — Change line 9:
```php
// Before
public readonly ?int $selectedIssueNumber,

// After
public readonly string|int|null $selectedTaskId,
```

**app/Data/RunResult.php** — Change line 12:
```php
// Before
public readonly ?int $selectedIssueNumber,

// After
public readonly string|int|null $selectedTaskId,
```

**app/Support/RunProgressSnapshot.php** — Change line 13:
```php
// Before
public ?int $selectedIssueNumber = null;

// After
public string|int|null $selectedTaskId = null;
```

Note: `RunProgressSnapshot` uses public properties (not readonly constructor params), so it is a simple property declaration change.
  </action>
  <verify>
    <automated>grep -rn "selectedIssueNumber" app/Data/SelectionResult.php app/Data/RunResult.php app/Support/RunProgressSnapshot.php && echo "FAIL: old name still present" || echo "PASS: old name absent"</automated>
  </verify>
  <done>
    - `app/Data/SelectionResult.php` contains `string|int|null $selectedTaskId`
    - `app/Data/RunResult.php` contains `string|int|null $selectedTaskId`
    - `app/Support/RunProgressSnapshot.php` contains `string|int|null $selectedTaskId`
    - No file contains `selectedIssueNumber`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update all call sites in services and commands</name>
  <read_first>
    - app/Services/ClaudeSelectorService.php
    - app/Services/RunOrchestratorService.php
    - app/Commands/RunCommand.php
    - app/Commands/PlanCommand.php
    - resources/prompts/selector.md
    - tests/Unit/RunOrchestratorServiceTest.php
  </read_first>
  <files>app/Services/ClaudeSelectorService.php, app/Services/RunOrchestratorService.php, app/Commands/RunCommand.php, app/Commands/PlanCommand.php, resources/prompts/selector.md</files>
  <behavior>
    - ClaudeSelectorService constructs SelectionResult with `selectedTaskId:` named argument
    - RunOrchestratorService references `$selection->selectedTaskId` (not selectedIssueNumber)
    - RunOrchestratorService constructs RunResult with `selectedTaskId:` named argument
    - RunOrchestratorService sets `$snapshot->selectedTaskId` (not selectedIssueNumber)
    - RunCommand constructs RunResult with `selectedTaskId: null`
    - RunCommand references `$result->selectedTaskId` in runLogPayload
    - PlanCommand references `$selection->selectedTaskId` in issue matching loop
    - selector.md prompt returns `selected_task_id` field name in JSON schema
    - Full test suite passes (./vendor/bin/pest --no-coverage exits 0)
  </behavior>
  <action>
Apply the following targeted edits:

**app/Services/ClaudeSelectorService.php** — Line 60, change named argument:
```php
// Before
selectedIssueNumber: $json['selected_issue_number'] ?? null,

// After
selectedTaskId: $json['selected_task_id'] ?? null,
```
Note: The JSON key changes from `selected_issue_number` to `selected_task_id` to match the updated selector prompt.

**app/Services/RunOrchestratorService.php** — 12 call sites (search for all occurrences):

1. Line 68 (skip_all RunResult): `selectedIssueNumber: null,` → `selectedTaskId: null,`
2. Line 80 (issue match): `$issue['number'] === $selection->selectedIssueNumber` → `(string) $issue['number'] === (string) $selection->selectedTaskId`
3. Line 92 (not found RunResult): `selectedIssueNumber: $selection->selectedIssueNumber,` → `selectedTaskId: $selection->selectedTaskId,`
4. Line 93 (failure reason string): `"Selected issue #{$selection->selectedIssueNumber} not found"` → `"Selected task #{$selection->selectedTaskId} not found"`
5. Line 105 (snapshot assignment): `$snapshot->selectedIssueNumber = $selectedIssue['number'];` → `$snapshot->selectedTaskId = $selectedIssue['number'];`
6. Line 124 (decline RunResult): `selectedIssueNumber: $selectedIssue['number'],` → `selectedTaskId: $selectedIssue['number'],`
7. Line 151 (validation failed RunResult): `selectedIssueNumber: $selectedIssue['number'],` → `selectedTaskId: $selectedIssue['number'],`
8. Line 198 (execution failed RunResult): `selectedIssueNumber: $selectedIssue['number'],` → `selectedTaskId: $selectedIssue['number'],`
9. Line 232 (verification failed RunResult): `selectedIssueNumber: $selectedIssue['number'],` → `selectedTaskId: $selectedIssue['number'],`
10. Line 282 (success RunResult): `selectedIssueNumber: $selectedIssue['number'],` → `selectedTaskId: $selectedIssue['number'],`
11. Line 328 (payloadFromResult): `'number' => $result->selectedIssueNumber,` → `'number' => $result->selectedTaskId,`
12. Line 360 (partialPayload): `'number' => $selectedIssue['number'] ?? $snapshot?->selectedIssueNumber,` → `'number' => $selectedIssue['number'] ?? $snapshot?->selectedTaskId,`

**app/Commands/RunCommand.php** — 3 call sites:

1. Line 340 (failedResultFromException): `selectedIssueNumber: null,` → `selectedTaskId: null,`
2. Line 363 (runLogPayload): `'number' => $result->selectedIssueNumber,` → `'number' => $result->selectedTaskId,`
3. Line 275 (RunOrchestratorService constructor): Change named arg `github: new GitHubService,` → `taskSource: new \App\Services\GitHubTaskSource(new GitHubService),`
   Also add import at top: `use App\Services\GitHubTaskSource;`

**app/Commands/PlanCommand.php** — 2 call sites (lines 75, 82):
```php
// Line 75 — Before
if ($issue['number'] === $selection->selectedIssueNumber) {
// After
if ((string) $issue['number'] === (string) $selection->selectedTaskId) {

// Line 82 — Before
$this->error("Selected issue #{$selection->selectedIssueNumber} not found in prefiltered list.");
// After
$this->error("Selected task #{$selection->selectedTaskId} not found in prefiltered list.");
```

**resources/prompts/selector.md** — Line 29, change JSON schema field:
```
// Before
  "selected_issue_number": 123 | null,

// After
  "selected_task_id": "1234567890" | null,
```
Also update the preamble from "GitHub issues" to "tasks" on the first line and update "candidate issues" section header — but only if those changes are natural. The key change is the JSON field name from `selected_issue_number` to `selected_task_id`.

After all edits, run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/binarygary/projects/binarygary/copland && grep -rn "selectedIssueNumber" app/ resources/ && echo "FAIL: old name still present" || echo "PASS: checking tests..." && ./vendor/bin/pest --no-coverage 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `grep -rn "selectedIssueNumber" app/ resources/` returns zero matches
    - `./vendor/bin/pest --no-coverage` shows "Tests: 99 passed" (or more if tests were updated in RunOrchestratorServiceTest)
    - RunCommand constructs `RunOrchestratorService` with `taskSource: new GitHubTaskSource(new GitHubService)` (not `github:`)
    - selector.md contains `selected_task_id` (not `selected_issue_number`)
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "selectedIssueNumber" app/ resources/` — must return zero lines
2. `./vendor/bin/pest --no-coverage` — must show all tests passing
3. `grep "selectedTaskId" app/Data/SelectionResult.php app/Data/RunResult.php app/Support/RunProgressSnapshot.php` — must show the property in all three files
4. `grep "GitHubTaskSource" app/Commands/RunCommand.php` — must appear (confirming RunCommand wiring fix)
</verification>

<success_criteria>
- Zero occurrences of `selectedIssueNumber` anywhere in app/ or resources/
- Full test suite green (99+ tests pass)
- `SelectionResult`, `RunResult`, `RunProgressSnapshot` all declare `string|int|null $selectedTaskId`
- `RunCommand` wires `GitHubTaskSource` (not bare `GitHubService`) into `RunOrchestratorService`
</success_criteria>

<output>
After completion, create `.planning/phases/17-asana-integration/17-01-SUMMARY.md`
</output>
